# 已知约束（Known Constraints）

## 数据约束（Tushare / ODS 事实）

- ODS 全部为 Hive 分区外部表，**查询必须带 `partition_date`/`endpoint` 过滤**（强制分区裁剪），否则报错。`partition_date` 是 `YYYYMMDD` 字符串。
- OQ-005 采集 manifest 中 `endpoint` 表示 Tushare API endpoint，`partition_endpoint` 表示写入 GCS Hive 分区 `endpoint=` 的值；两者不得混用。普通接口二者相同，`stock_basic` 必须按 request variant 写入 `stock_basic_listed` / `stock_basic_delisted` 两个 `partition_endpoint`，以匹配 `dim_stock` 的 `endpoint IN (...)` 消费口径。
- OQ-005 raw GCS Hive 路径 canonical 口径固定为 `a-share/tushare/raw_data/api=<api>/endpoint=<partition_endpoint>/partition_date=<YYYYMMDD>/data.parquet`。`api=` 使用实际 Tushare API 名（财务为 `*_vip`，指数/股票基础信息按 manifest `api`），不是 `api=tushare`；`endpoint=` 使用 ODS 外部表读取的 partition endpoint / variant。2026-06-04 已用 BigQuery `INFORMATION_SCHEMA.TABLE_OPTIONS` 复核当前 14 张 ODS 与 10 张 schema repair 表的 `source_uris` 均符合该口径。
- 2026-06-03 复核确认 10 张 ODS 外部表存在 Parquet schema mismatch：`ods_tushare_daily_info`、`ods_tushare_dividend`、`ods_tushare_fina_audit`、`ods_tushare_limit_list_d`、`ods_tushare_margin`、`ods_tushare_margin_detail`、`ods_tushare_moneyflow`、`ods_tushare_stk_limit`、`ods_tushare_stk_rewards`、`ods_tushare_sz_daily_info`。2026-06-05 `sql/qa/06_ods_parquet_schema_checks.sql` 对 P0 与 all 范围只读复核已通过，当前 BigQuery 读层未再暴露 mismatch。新增 endpoint / ingestion Parquet 写入必须按 schema contract 显式 cast，并运行对应 ODS schema QA。若后续做修复/回填/防复发，仍按 `docs/prd/PRD_20260603_04_ODS外部表ParquetSchema修复.md` 执行：先从 GCS 原 Parquet 按 schema contract 做 schema-preserving rewrite，API 重拉只作为原文件损坏、缺失或 owner 明确要求的补救路径；修复过程只改 Parquet 物理 schema，不补数据、不改业务值口径、不写伪空 Parquet；backup 必须 write-once，已匹配 contract 的文件跳过重写 / 发布。
- **财务表 `partition_date == 报告期(end_date)`，不是公告日**：禁止用 partition_date 当数据可见时间，必须用 `ann_date_eff = COALESCE(f_ann_date, ann_date)` 做 PIT。
- 财务表同一 `(sec_code, 报告期)` 有多条（不同 `report_type`/修正/`update_flag`）：P0 默认消费合并报表 `report_type='1'`；带 `report_type` 的 DWD 版本事实表保留源 `report_type` 并派生 `report_caliber`/`is_default_report_caliber`，P0 DWS 默认过滤默认口径，多口径特征后续另建/扩展。
- **三大报表 `income/balancesheet/cashflow` 已落地**（`sql/dwd/06/08/10` + `_latest` 07/09/11，OQ-003/DECISION-20260601-05）：实测当前 ODS 三表**仅含 `report_type='1'`**（合并报表，分 `comp_type` 1/2/3/4/7）；版本事实键 `(sec_code, report_period, report_type, ann_date_eff, update_flag)`，可见日 `COALESCE(f_ann_date, ann_date)`；默认 `_latest` 与 `dws_stock_feature_fin_daily` 只消费默认合并口径。`report_type>'1'` 的 caliber 映射为前向兼容、当前不触发。金额单位为元（Tushare 原始口径，未换算），income/cashflow 为累计/YTD、balancesheet 为时点值。
- `dws_stock_feature_fin_daily`（`feature_version='fin_default_v0_20260602'`）把 `dwd_fin_indicator` + 三大报表 as-of 到 universe 每个交易日：`report_caliber`/`is_default_report_caliber` 是**消费口径契约**（恒 consolidated/TRUE），实际可用性看 `has_fin_*` 与 `*_report_period`；as-of 有界 `visible_trade_date ∈ [trade_date-900d, trade_date]`，超窗视为缺失（DECISION-20260602-03）。行数 = universe，主键唯一，零 PIT 泄露。
- `dwd_fin_*_latest` / `dwd_fin_indicator_latest` 是便捷最新表，不用于 PIT 回测 join；其版本排序按 `update_flag DESC, ann_date_eff DESC, ingested_at DESC, source_partition_date DESC`，优先保留修正版。
- `stock_basic` 用 `endpoint` 分 `listed`/`delisted`：**必须 UNION 两者**，否则丢失退市股 → 幸存者偏差。
- 行情历史自 **1990-12-19**；行情表单分区内 `(ts_code, trade_date)` 已唯一、无需去重。
- 2019 年前数据范围分三类，不能混作“默认全历史写入”：财务/事件按分区前移到 `20170101`；日常 `daily_current` 与全量 CTAS 默认仍以行情 DWD/DWS `trade_date >= 2019-01-01` 为生产下限，构建时按最大滚动窗口读取 2018 lookback buffer；维度/日历取最新快照或全量历史事件。例外：owner 明确要求的历史训练窗口补数可通过 `warehouse_mode=backfill` 显式写入 2019 年以前窗口，且必须保留 `date_from/date_to` 分区过滤与窗口 QA。历史 backfill 行一旦进入 DWD/DWS，core smoke / qa_only 不得再用“全表无 2019 前行”作为不变量；`daily_current` 的 2019+ 写入下限必须由窗口 SQL 与窗口 QA 负责。
- 行情 lookback buffer 不落最终 DWD/DWS；`ret_1d` 至少读到 2018 年最后一个交易日，120/250 日滚动特征按窗口多读。
- 当前已物化的策略 1 价格 DWS 只读取最终 DWD/DIM，不直接打 ODS；最终 DWD 价格表不落 2018 buffer 行，因此 2019 年初 60 日窗口以 `has_full_history_60d=FALSE` 显式标记，默认样本掩码剔除这些不完整窗口。若需要 2019-01 起完整 60 日特征，需补专用 lookback-capable 构建输入或调整 DWD/DWS 构建方式。
- **`fina_indicator` 无 `f_ann_date`**（实测）：其可见日只能用 `ann_date`；可见日规则**按表定义**，不可用统一 `COALESCE(f_ann_date,ann_date)` 公式覆盖所有财务表（见 docs §4.3）。
- `stock_basic_delisted.delist_date` 已由上游统一为 `STRING` 并可直读解析；`dim_stock` 应优先使用 ODS 退市日，只有缺值时才用 `daily` 最后交易日加一天兜底（OQ-007 已关闭）。
- **停牌日 `daily` 无该股行**：价格 DWD 必须以「交易日历开市日 × 在市股票」为骨架（保留停牌日空行），不能从 `daily` 起表，否则停牌日整行消失、`t+k` 标签错位。
- **`suspend_d.suspend_type` 区分停牌/复牌**：`S`=停牌，`R`=复牌。价格 DWD 只可用 `S` 标记停牌事件；`R` 复牌日若 daily 有成交，不能判 `is_suspended=TRUE`。
- `S` 事件也可能是盘中临停。若当日 `daily` 有 close 且 volume > 0，不能把该行标为全天停牌；DWD 用 `has_intraday_halt` 标记盘中临停，用 `has_open_halt` 标记开盘时段或未知时段临停并影响开盘建仓掩码。
- Tushare 各接口金额/量单位不一（手/千元/万股/万元）：落库须按「表+字段」归一到元/股。
- OQ-006 已实现：`ashare_meta.ods_field_unit_map` 是单位换算唯一事实来源；`dwd_index_eod.volume/amount` 已按 `vol*100` / `amount*1000` 换算并迁移为 `volume_share/amount_cny`；新增或修改 DWD 标准字段的 PR 必须更新 `ods_field_unit_map` 并运行 `sql/qa/05_unit_contract_checks.sql`；OQ-006 最小实现已先于 P1 资金流、财务扩展等高单位风险 DWD 正式落地。
- `dwd_stock_dividend_event` 是 ledger 可消费的分红送转事件入口；ledger 不得直读 `ods_tushare_dividend`。Phase A 仅覆盖现金分红、送股、转增；A 股无美股式 split/并股机制，配股/增发等未采集公司行为不纳入本表。`sql/qa/14_corporate_action_event_checks.sql` 会把 hfq 因子跳变无同日事件的行落入 `qa_stock_dividend_event_hfq_mismatch` 并归类为 `same_day_orphan_corporate_action`，Phase B/Phase C 必须显式披露该类数量，不能静默混入 `unexplained_residual`。当前 `dividend` endpoint 不在 ODS 日常采集 scope；owner 对采集 scope / 编排接入做出决策前，CA-on 实验的 `predict_end` 不得超过事件表可见上界，确需更晚窗口时必须先补采 `dividend` ODS 分区，再重跑 `sql/dwd/12_dwd_stock_dividend_event.sql` 与 `sql/qa/14_corporate_action_event_checks.sql`，最后重跑 `qa_corporate_action_ledger_outputs`。2026-06-12 Phase C true-five-year CA-on run 在 `2021-01-04..2026-06-09` 窗口披露 factor_to_event orphan `350`、unclassified `0`；2026-06-12 dividend 补采后 resume 修正（child `bt_s1_dividend_backfill_resume_20260528_20260609_v20260612_01`，详见 `docs/分析-dividend-ODS补采与CA-Resume补跑-20260612.md`）后，展示口径 CAGR `0.15357789449949522`、CA-on contract Sharpe `0.668539787795112`、Calmar `0.41030930550903105`、MaxDD 不变，仍不得标 accepted / promotion。
- 部分数值字段在 ODS 是 STRING（如 `moneyflow_hsgt`、`ccass_hold`）：落库须 `SAFE_CAST`。
- 北向数据（hk_hold / moneyflow_hsgt）2024 年后部分口径变化/停更，需做可用性标记。
- `index_member_all` / `ci_index_member` 已在 ODS 中可用，是最新分区中的全量历史行业归属区间快照；历史 join 必须用 `in_date/out_date`，不能用 `is_new` 回填历史。默认区间口径为 `[in_date, out_date)`，落地前需 QA `out_date` 当天有效性、区间重叠/缺口和 2019+ 覆盖率。
- `dim_index` 是指数 canonical 映射、ODS 实际代码、端点可用性和 benchmark 候选状态的事实来源。`dwd_index_eod.sec_code` 应输出 canonical 指数代码；若 ODS 实际代码不同，用 `source_sec_code` 保留来源代码（如 ODS `399300.SZ` → canonical `000300.SH`），DWS/ADS 基准 join 只使用 canonical `sec_code`。
- runner 使用 `benchmark_sec_code` 前必须校验该代码在 `dim_index` 中 `has_daily=TRUE AND is_benchmark_candidate=TRUE`，并校验完整 NAV 窗口内 `dwd_index_eod` 对每个开市日有且只有一条非空价格记录。依赖指数 PE/PB/市值字段的市场状态特征必须要求 `dim_index.has_dailybasic=TRUE`。

## 平台约束（BigQuery）

- **单表最多 4000 个分区**：行情表必须按月分区（`DATE_TRUNC(trade_date, MONTH)`），不能按天。
- **只有分区列能 `require_partition_filter` 强制过滤；聚簇列无法强制**。
- 对本项目所有启用 `require_partition_filter=TRUE` 的 BigQuery 分区表，任何 `SELECT` / `ASSERT` / `DELETE` / `UPDATE` / `MERGE` / 清理脚本都必须在对应表引用上显式写分区列过滤（如 `trade_date`、`rebalance_date`、`predict_date`、`partition_date`、`DATE_TRUNC(...)` 对应分区列等）。只按 `run_id`、`backtest_id`、`strategy_id`、`sec_code` 等聚簇/普通列过滤不满足 BigQuery 必要条件，会直接报错；脚本中的子查询和验证计数也必须带分区范围，不能只在外层查询过滤。
- `CREATE TABLE AS SELECT` 不能在 SELECT 列上内联 description：维度表用内联列定义 DDL，事实表用 CTAS + 后置 `ALTER COLUMN SET OPTIONS`。
- 核心表重建后必须执行 `sql/metadata/01_core_table_column_descriptions.sql`，集中恢复/补齐表级和字段级中文说明。财务三表 DWD（`dwd_fin_income/balancesheet/cashflow` + `_latest`）与 `dws_stock_feature_fin_daily` CTAS 重建后必须执行 `sql/metadata/02_finance_table_column_descriptions.sql`（CTAS 不保留列描述）；`sql/qa/04_finance_caliber_checks.sql` 内置字段 description 缺失断言，漏跑会被 QA 拦截。
- 外部表的列描述/分区元数据通过 `bq show`/`INFORMATION_SCHEMA` 获取。
- `bq query` 执行本项目建表/QA 脚本时显式指定 `--location=asia-east2`，避免无源表脚本或跨数据集 CTAS 使用错误 job 区域。

## 建模约束（PIT / 量化正确性）

- 前复权价（`_qfq`）隐含未来除权信息，**不得作为训练特征**；技术指标/收益用后复权（`_hfq`）。
- 标签必须与特征时间错位：`t` 日特征 → 标签从 `t+1` 起算（入场价用 `t+1`）。
- universe 必须含退市股历史区间，并打标停牌/一字板/上市未满 N 日/ST 做样本掩码。
- 涨跌停价直接用 `stk_limit.up_limit/down_limit`（已按板块算好），不要自己按板块硬编码。

## 安全约束（红线）

- 仓库、记忆文件、文档、日志中**绝不可出现**：BigQuery service account key、Tushare token、任何 API key / 凭据 / 个人隐私。
- 凭据通过环境变量 / 受信环境注入；`.gitignore` 已忽略 `*.key`/`credentials*.json`/`.env` 等。
- OQ-005 Cloud Run Jobs 部署只允许通过 Secret Manager 将 Tushare token 注入为运行时环境变量 `TUSHARE_TOKEN`；Tushare 官方或兼容 API 地址用 `TUSHARE_HTTP_URL` 运行时配置，不把 token、代理认证信息或临时凭据写入仓库。

## 工程约束

- 需要代码在工作树中改，改完推 PR。
- owner 已明确授权：本项目执行过程中如缺失必要本机 / 运行依赖，Agent 可直接安装最小必要依赖并继续任务，无需再次询问。适用范围包括系统包、Python / Node 依赖、CLI 工具和运行时库（例如 LightGBM 所需 `libomp`）。该授权不覆盖密钥、token、service account key、OAuth 凭据、个人隐私材料、未脱敏敏感日志、显著云成本增加、生产权限边界变更、生产 job spec / IAM 变更或破坏性数据操作；这些仍需按既有安全约束和 owner 确认处理。
- 调度运行代码、Composer DAG、告警配置、生产 QA 文件名 / task_id、Dataform action / tag 等运行时命名不得绑定 OQ、Phase、P0/P1 等阶段性编号；使用 `pipeline`、`ingestion`、`warehouse`、`core`、`contract` 等长期业务语义命名。历史 PRD / archive 可保留原问题编号作为审计记录。
- `dataform/definitions/**/*.sqlx` 是由 canonical `sql/` 和 `dataform/action_manifest.json` 生成的产物；任何修改 manifest 覆盖范围内 `sql/` 的 PR 都必须运行 `python3 scripts/dataform/generate_sqlx_from_sql.py` 并用 `python3 scripts/dataform/generate_sqlx_from_sql.py --check` 验证无漂移。
- OQ-005 Workflows runtime 约束：Google Cloud Workflows `http.*` 单 step timeout 上限是 `1800s`，不能配置成 `3300` 或其他更大值；长耗时 BigQuery / Cloud Run 调用必须通过 `ashare-pipeline-control` 这类服务端轮询到终态，而不是继续放大 Workflow HTTP timeout。凡调用窗口刷新 / 窗口 QA SQL 的 Workflow BigQuery task，必须显式透传 `warehouse_mode`，避免 live 部署时因 SQL 参数缺失才暴露运行期错误。
- OQ-005 Workflows flag 约束：凡后续在 Workflow 内用 `== "true"` 判断的布尔开关（如 `pipeline_dry_run`、`scheduled_run`、`skip_ingestion`、`skip_downstream_refresh`、`trigger_downstream_refresh`、`force_non_trading_day_gate`），都必须在 resolve 阶段统一归一为 `${text.to_lower(string(input_flag))}`，不能直接透传 `args` 原值；否则 Cloud Scheduler 或手工 API 以 JSON boolean 传参时会静默落入错误分支。
- DWD/DIM 物化后，下游一律查 DWD/DIM，不直接打 ODS。
- OQ-005 Cloud Run Job 模板仍默认 `--dry-run`，生产写入只能由 Composer 在 `ashare_pipeline_dry_run=false` 时显式传入 `--allow-gcs-write`，不得在脚本里硬编码 token 或绕过 dry-run guard。
- 当前采集 Cloud Run 入口为 `ashare-ingest-current-scope` 单 execution 顺序执行 14 个 ODS endpoint，并通过 Direct VPC egress + Cloud NAT 固定出口；已完成 `2026-05-20` 至 `2026-06-05` 当前范围 ODS 生产 GCS 写入和 `sql/qa/09_ods_daily_partition_readiness.sql` 验证。
- 2026-06-06 PR #86 部署后，生产 scheduled DAG 为 `ashare_ods_ingestion_daily`，旧 `ashare_daily_pipeline_v0` 已暂停并仅作迁移期回滚参考；每日 20:00 CST scheduled run 默认 `business_date` 必须来自 `data_interval_end.in_timezone('Asia/Shanghai')`，不能用 Airflow `ds`，手动 `dag_run.conf` 覆盖优先。
- 每日 DAG 默认主链只读业务日分区或窗口影响范围，不得默认执行 2019+ 全历史 schema 检查或完整 ODS→DIM/DWD/DWS/ADS 重建；`pipeline_dry_run=true` 时窗口分支不得写 DIM/DWD/DWS，readiness 也不得写 `pipeline_task_status` warning 行。
- `sql/qa/09_ods_daily_partition_readiness.sql` 的 blocking strong endpoint 固定为 `daily`、`daily_basic`、`adj_factor`、`stk_limit`、`index_daily`；weak endpoint 缺失和 API 单次返回行数正好命中上限只写 `pipeline_task_status.status='warning'`，不得阻断下游；warning 写入必须发生在阻断 ASSERT 前，确保 strong 缺失失败后仍可观测；非交易日不因 weak endpoint 缺失写 warning。
- `ashare_ods_ingestion_daily` 成功后通过 `TriggerDagRunOperator` 触发 `ashare_warehouse_window_refresh`；跨 DAG 血缘必须写入 `pipeline_run.upstream_pipeline_run_id` / `triggered_by_dag_id`，并由 `v_pipeline_refresh_missing` / `warehouse_refresh_missing` 监控 ingestion success 后完全没有 linked warehouse run 的异常。
- `ashare_warehouse_window_refresh` 必须 `max_active_runs=1` 串行执行，`warehouse_mode=daily_current` / `backfill` 只允许刷新 DIM 小表、恢复 P0 metadata、按 `sql/incremental/01_refresh_stock_dwd_dws_window.sql` 窗口化刷新股票 DWD 与策略 1 DWS，并执行 `sql/qa/10_windowed_stock_refresh_checks.sql`；`daily_current` 若 `date_to` / `business_date` 落在非交易日，窗口 SQL 和 QA 必须先归一到不晚于请求日期的最近 SSE 开市日，`backfill` 保持显式日期口径。
- scheduled `daily_current` 非交易日 gate 必须在 ingestion 前查 `ashare_dim.dim_trade_calendar`，确认 SSE 当天不开市后跳过 ingestion、readiness 和 transform，并通过 `skip_non_trading_day` 实体 Python task 写 `pipeline_task_status.status='skipped'`；普通手工触发不走 gate，只有 smoke-only `force_non_trading_day_gate=true` 可强制测试该分支；日历缺行或 `is_open` 为空均 fail-closed；上一交易日修复必须显式 `backfill`。
- `manual_smoke_skip_non_trading_day_pr83_20260606_02` 和拆分后 `manual_split_skip_gate_20260606_01` 均已验证 `skip_non_trading_day` 状态落库、ingestion/readiness/transform skipped、Cloud Run 未触发。
- 窗口 QA 的估值覆盖范围在 `daily_current` 默认使用最近 20 个交易日，在 `backfill` 使用实际写入窗口；最终宽表的 `has_valuation_data` 只按 price-driven feature universe 内 `total_mv_cny/circ_mv_cny` 非空行断言，不要求所有 valuation 行进入宽表。
- 窗口 DML 使用 BigQuery transaction 包裹，日常小窗口失败时整体回滚，大区间 backfill 按年/季/月分块执行；价格特征读取 60 个交易日，估值特征按每只股票写入窗口首日前的实际 60 条估值观测推导读取边界，标签/宽表/样本写入向前回补 20 个交易日。
- full-vs-window parity 工具已于 2026-06-12 按 DECISION-20260612-02 / PRD_20260612_01 退役；窗口刷新正确性此后依赖 `sql/qa/10_windowed_stock_refresh_checks.sql` 窗口 QA 与发布前 review。
- 如未来再次大规模重写历史窗口需要 full/window parity，应从 git history 恢复本实现 PR 删除的脚本（实现 PR parent commit 即恢复入口）并另行评估。
- ADS runner/backtest/report 产物不得由每日 DAG 自动写入。
- 全量转换只能通过无 schedule 的 `ashare_warehouse_full_rebuild` 手工触发，并要求 `confirm_full_rebuild=true`；legacy `ashare_enable_full_refresh=true` 必须记录为 `full_rebuild_compat`，不得伪装成 `daily_current` 增量。
- `warehouse_mode=qa_only` 只能执行只读 QA；ADS 契约初始化只能通过 `enable_ads_contract_init=true` 手工启用。
- PR #86 部署后 Composer DAG/SQL/观测视图已与 `main` 同步；`manual_split_qa_only_20260605_01`、`manual_split_backfill_20260605_01` 和 refresh-missing synthetic transaction smoke 均通过。
- 完整 Dataform 生产链路和新 DAG 连续 scheduled 观察仍未完成；旧 Composer-era 补跑 helper `scripts/pipeline/run_warehouse_refresh.py` 已在 Composer 删除后清理，后续补跑应直接按 Workflows runbook 手工触发 `ashare_warehouse_window_refresh` 的 `backfill` 或 `qa_only`，不得再恢复 `gcloud composer environments run` 路径。
- `v_pipeline_refresh_missing` / `warehouse_refresh_missing` 只监控“应该触发下游刷新但完全没有 linked warehouse run”的异常。非交易日 scheduled skip（`pipeline_task_status.task_id='skip_non_trading_day' AND status='skipped'`）和显式 ODS-only 跳过下游刷新（`task_id='skip_downstream_refresh' AND status IN ('success','skipped')`）必须作为豁免，不得报警为缺失下游刷新。`skip_downstream_refresh` 必须用实体 `PythonOperator` 显式写 task-status 行，不能依赖 `EmptyOperator` 的 success callback 作为唯一证据。
- OQ-005 live ingestion 成功/失败/空返回必须写入 `ashare_meta.ingestion_run` 和 `ashare_meta.ingestion_partition_status`；dry-run 与 `--skip-gcs-write` API 只读 smoke 不写生产 meta 表。`ingestion_partition_status.endpoint` 存 `partition_endpoint`，避免指数 variant 等同一 API + 同一日期互相覆盖状态。GCS ingestion publish 使用 staging 校验后覆盖正式 `data.parquet`，不做 write-once backup；该 overwrite 是采集重跑口径，若未来历史回填需要可回滚，应另加 backup 开关或独立回填流程。
- 采集 Cloud Run 镜像（`asia-east2-docker.pkg.dev/data-aquarium/ashare/ingestion`）把 `scripts/ingestion/**` 与 `configs/ingestion/**`（含采集 manifest）一并打包进镜像，5 个采集 job 引用 `:latest` tag、execution 创建时解析 digest。因此**任何改动这两个路径的 PR 合并后必须按 `orchestration/cloud_run_jobs/cloudbuild.ingestion.yaml` 重建并推送镜像**，否则生产采集继续运行旧代码/旧 manifest 且不会有任何显式报错。2026-06-12 事故实证：镜像自 2026-06-04 11:50 UTC 后未重建，导致 `60fb242` status_writer 接线从未上线（meta 审计表 8 天 0 行、采集级告警静默）、`2e4d29b` 新增的 000001.SH variant 从未生效（ODS 000001.SH 06-10 起停更，market-state `sse_composite_*` 退化为 NULL）。已于 2026-06-12 重建为 digest `5c78e862...` 并补采修复；`gcloud run jobs execute --args` 不允许重复 flag，需要多 `--variant` 过滤时应拆成多次 execution（每次单 variant）。
- Composer DAG SQL 文件同步到 Composer bucket 的 `data/sql/`；不得把项目 SQL 树同步到 `dags/sql/`，避免 DAG 解析器把 SQL 文件树当作 DAG bundle 处理并造成 worker/DagBag 状态异常。
- 大范围回填建议分批（按年/季）跑并记录批次状态；P0 行情日常/全量默认仍写 2019+，但 owner 显式 `warehouse_mode=backfill` 可按历史训练窗口写入 2019 年以前 DWD/DWS 行，财务/事件从 2017 起。
- `dim_stock` 若遇到 latest `stock_basic` 缺失但 ODS daily 有历史记录的代码，应作为 `derived_from_daily` 兜底；若 `stock_basic.list_date` 晚于已存在的首个日线交易日，生命周期下限用首个日线交易日兜底，避免 2015-2018 等历史 backfill 漏掉已有成交股票。派生退市边界用 ODS 最新交易日减宽限期判断，不能用系统当前日期直接判退市。
- PR 合并后，若 owner 未要求保留工作分支，应删除已合并且不再使用的 `codex/*` 本地分支、对应远端分支，并移除为该分支创建的独立 `git worktree`，保持分支和工作树列表干净；若对应 worktree 仍有未提交或未合并改动，先暂停并请 owner 决策，不得强删。
- 提交（commit/push）仅在用户明确要求时进行。
- 历史 SQL ledger `08_run_backtest.sql` 已退役并删除；当前 Strategy1 回测默认由 Cloud Run Python `ledger_exec_v1_lot100` 执行，继续遵守 `docs/prd/PRD_20260604_01_策略1LedgerV1交易执行语义.md` 的日级账户 ledger 语义：`rebalance_date` 是信号日，下一开市日按开盘价加滑点执行；每日维护现金、实际持仓和 pending sell；卖出先于买入；买单按可用现金缩放；同股新目标与旧仓 netting；非目标旧仓留在 NAV 中每日 mark-to-market；卖不出进入 pending sell 并在后续每个开市日继续尝试；买不进不候补；订单状态显式写入执行 / skipped / pending / cancel / noop。`10` 继续守卫现金、暴露、持仓唯一性、NAV 覆盖、状态枚举、skipped 零成交、同日同股方向和 pending sell 延续等不变量。旧 FLOAT 股数实现仅可通过 Python `--use-float-ledger` 显式作为 legacy/audit 路径运行。
- Ledger state resume 必须显式设置 `p_initial_state_mode='resume_from_backtest'`、`p_parent_backtest_id` 和 `p_state_as_of_date`；`p_predict_start` 必须等于 `state_as_of_date` 后下一 SSE 开市日。父回测 summary/NAV/position、`ledger_version`、现金/持仓估值一致性、active target、pending sell 和初始持仓估值价格任一不满足时必须 fail-fast，禁止静默回退 fresh-start。分段 resume 的 `10` QA 若复用原实验候选/组合，biweekly 必须显式设置 `p_rebalance_anchor_start` 为原实验调仓锚点，避免按 segment start 重算双周奇偶；resume 首日 `daily_return` 必须用父 `state_as_of_date` NAV 作为 LAG 锚点，避免拼接收益序列断点。2026-06-11 PRD_08 已用 research-only 真实数据验收 Cloud Run Python resume：等价参照必须是 full fresh continuous parent 的同窗口切片，不得用 cut 后重新 fresh-start 的短段作为 full 输入；正式结果若采用 resume segment，仍需 owner 显式批准并重跑两套 resume QA。
- 2026-06-05 owner 决策：策略 1 后续执行层不再使用旧 BQML-only 训练 / 选型 / 预测路径、旧 SQL ledger fallback 或旧 SQL runner 调度器作为默认 / fallback / 新增开发路线。既有 BQML / SQL runner 运行结果仅作为 historical reference / audit；不得登记为新的默认 baseline。“停止 SQL runner”特指停止旧 BQML-only 与 SQL-ledger execution path，不等于删除所有 Strategy1 SQL。按 DECISION-20260612-02 / PRD_20260612_02 §3-C，`ads_ml_training_panel_daily` 中 `run_id LIKE 's1_bqml%'` 的 historical BQML training panel 行将裁剪；裁剪后 historical BQML run 复算需先按 PRD §3-C-3 从 git history 恢复 `90ccc41:sql/ml/strategy1/01_build_training_panel.sql`，用 registry 参数重建面板并与删除前快照对账。

- 2026-06-05 owner 决策：策略 1 后续执行层不再使用旧 BQML-only 训练 / 选型 / 预测路径、旧 SQL ledger fallback 或旧 SQL runner 调度器作为默认 / fallback / 新增开发路线。既有 BQML / SQL runner 运行结果仅作为 historical reference / audit；不得登记为新的默认 baseline。“停止 SQL runner”特指停止旧 BQML-only 与 SQL-ledger execution path，不等于删除所有 Strategy1 SQL。按 DECISION-20260612-02 / PRD_20260612_01 §3-C，`ads_ml_training_panel_daily` 中 `run_id LIKE 's1_bqml%'` 的 historical BQML training panel 行将裁剪；裁剪后 historical BQML run 复算需先按 PRD §3-C-3 从 git history 恢复 `90ccc41:sql/ml/strategy1/01_build_training_panel.sql`，用 registry 参数重建面板并与删除前快照对账。
- 2026-06-10 Strategy1 结构重构约束：当前 active/shared Strategy1 SQL 命名空间为 `sql/strategy1/**`，路径与参数契约由 `configs/strategy1/active_step_catalog.yml` 统一维护。`sql/ml/strategy1/**` 与 `sql/cloudrun/strategy1/**` 只保留 historical/audit note，不得作为 active 执行路径重新引用；Cloud Run Python wrapper 可继续保留，但 active SQL 调用应通过 catalog step 解析。任何新增或修改 Strategy1 SQL 中的 run-scoped ADS 表引用，都必须同步 catalog step 的 `inputs` / `outputs`，并通过 pytest 校验 research 渲染不残留 `data-aquarium.ashare_ads.`。Python 无 step 上下文的全局 research rewrite 仍必须 fail-fast 或排除歧义 role，避免 `ads_model_registry` 同时对应 `model_registry` / `acceptance_result` 时误替换；D2 后 `run_sql_script` / `render_sql` 等兼容入口若传入未登记 catalog 的 SQL 路径，默认 research 模式会拒绝渲染，临时 ADS 审计脚本必须显式传 `dataset_role="ads"`，active research 路径必须先登记 catalog step。
- 2026-06-10 Strategy1 Cloud Run entrypoint 迁移约束：五个正式 Cloud Run jobs 已迁移到 package entrypoint，job args 为 `quant_ashare.strategy1.train_predict` / `prepare_matrix` / `train_candidate_task` / `select_register_predict` / `backtest_report`；当前线上 image digest 属滚动运维状态，不写入长期约束，需以 `.agent/memory/IMPLEMENTATION_STATUS.md` 最新 Strategy1 main 镜像部署记录为准。代码侧 cutover 已随 PR #156 合并并重建 main 镜像部署：pipeline-control / native search / annual rolling 的 Cloud Run override args、`active_step_catalog.yml` 的 backtest/report `caller` 字段和 active runbook / 示例命令均指向 package entrypoint。旧五 job wrapper 文件 `scripts/strategy1_cloudrun/train_predict.py` / `prepare_matrix.py` / `train_candidate_task.py` / `select_register_predict.py` / `backtest_report.py` 已删除；旧五模块路径仍必须保留在 retired-reference linter ban-list 和测试清单中，防止 active scopes 回流。非 job wrapper 兼容层可继续按单独迁移节奏保留。
- 2026-06-10 Strategy1 D2 research-first 约束：普通 Strategy1 runner、SQL runner、report、diagnosis、QA、acceptance/comparison 和 factor attribution 默认写/读 `data-aquarium.ashare_research.research_*`；`configs/strategy1/active_step_catalog.yml` 的 `current_dataset_role` 为 `research`，`research.enabled_by_default=true`。历史 ADS audit 或后续 promotion target 必须显式传 `--output-dataset-role ads` / `dataset_role="ads"`，此时 run-scoped 表解析到 `ashare_ads`，`experiment_run_status` 仍按 per-role override 解析到 `ashare_meta.strategy1_experiment_run_status`。Cloud Run orchestrator / 子命令必须始终显式下发 `--output-dataset-role=research|ads`，避免 job 镜像滚动更新期间继承错误默认值。D2 不实现 promotion job，普通 runner 不得隐式写 ADS；owner-approved promotion 必须等 Phase D3。
- 2026-06-10 Strategy1 D3 promotion 约束：research 到 ADS 的正式发布只能通过显式 owner-approved promotion job `python -m scripts.strategy1.promote_research_to_ads` 或等价 package entrypoint `quant_ashare.strategy1.promotion`；普通 runner/report/QA/acceptance 不得隐式写 ADS。
- Promotion CLI 默认 review-only，真实写 ADS / manifest 必须显式传 `--execute`；单独 `--print-sql` 不得执行写入。
- Promotion 必须记录 `promotion_id`、source run/backtest/model、date window、approval metadata、acceptance contract version/hash、target ADS tables 和 promotion code version；默认只允许 accepted research，默认不复制大体量 training panel，且 ADS 目标已有行时必须 fail-fast，除非 owner 显式批准 `--force-replace`。
- `--allow-unaccepted` 只绕过 accepted guard，promotion 不得把 `acceptance_status` 或 `research_status` 反写为 `accepted`；promotion 只能写 promotion lifecycle 字段和 manifest。
- Promotion 必须断言 source trade / NAV 对同一 `backtest_id` 不存在 promotion window 外遗漏行，并检查 summary `start_date` / `end_date` 被本次窗口覆盖；ASSERT 失败整体回滚且不会写 manifest 成功行，失败 attempt 需通过 BigQuery job history / Cloud Run logs 审计。
- Promotion 写 ADS 后必须更新 research promotion lifecycle 并写 `research_promotion_manifest`，但不追溯迁移历史 ADS。
- 2026-06-11 Strategy1 annual rolling official continuous 约束：2021-2026 正式结果不得拼接年度 fresh-run NAV，必须来自单一 continuous ledger（或另行验收的 resume-continuous segment ledger）。PRD_03 P0 路径使用 `quant_ashare.strategy1.synthetic_continuous` 从逐年 refit prediction window 合成一个 research-only synthetic prediction run：synthetic registry 必须只有一行 `status='selected'`，prediction 行必须统一改写到 synthetic `model_id` / `run_id`，逐年真实模型溯源写入 manifest/hash 和 `year_model_map`。Synthetic run 没有 training panel、没有真实 model artifact，因此 continuous backtest 必须用 `backtest_report --skip-diagnosis --skip-tail-risk --skip-qa`，随后外接 `qa_continuous_backtest_outputs` 和 `qa_lot_aware_ledger_outputs`；不得对 synthetic run 强行跑默认 `10` / `12` / `20` QA 或模型诊断。
- 2026-06-10 Strategy1 IAM 收敛决策：owner 已选择接受现状但保留流程约束。五个普通 Strategy1 Cloud Run jobs 暂继续使用 `241358486859-compute@developer.gserviceaccount.com`，且 `ashare_ads` dataset 暂继续授予该 SA WRITER，以保留 ADS audit / 历史报告重渲染兼容路径；不要在没有新的 owner 决策和替代 audit 路径前 revoke 该权限。真实线上流程仍要求普通实验默认 research-first，ADS 正式发布只走 owner-approved promotion job；显式 `--output-dataset-role ads` / `dataset_role="ads"` 仅作为历史 ADS audit / 兼容路径，不得作为普通新实验写入路径。

- 2026-06-11 Strategy1 annual rolling source-panel / true-five-year 覆盖约束：PR #166 的 `2019-04-03` final-refit 起点 override 只能解释为“对齐当时 DWS / source panel 中首个可训练开市日”，不是 2019Q1 根因修复。
- 2026-06-11 审计确认 2021/2022/2023 source selection panel 均缺 `2019-01-02..2019-04-02`，且存在 selection split / label-embargo 造成的内部年末缺口；旧 `qa_refit_register_predict_outputs` 只检查 min/max 端点，因此这些缺口不会使旧结果失败。
- PR #173 已把 effective-window annual refit 改为 dedicated refit panel，且后续 effective-window plan 仍必须按 `effective_refit_train_start=max(nominal_refit_train_start, DATE '2019-04-03')` 写入；不得再用 selection panel 作为 refit `source_panel_run_id`。
- PRD_06 live 修复已把 2010-2014、2015Q1、`2019-01-02..2019-04-02` 覆盖写入 DWD/DWS，并补采 57 个早期 `daily_basic` 市值字段全空开市日；后续任何 true-five-year refit / continuous 重跑仍必须先通过 stock/index/market overlap parity、`sql/qa/13_true5y_historical_coverage_checks.sql`（含 ODS `daily_basic` 市值字段 open-day 覆盖）和逐年 refit panel coverage QA。
- Overlap parity 口径：stock/DWD/DWS 表应以严格 `float_tolerance=1e-8` 零 mismatch；market-state 这类大额 FLOAT 聚合允许记录明确的微小 roundoff 容忍度（本轮验收为 `1e-4` 元级），但 regime/action/布尔触发字段必须一致，不得把业务字段差异归为 roundoff。
- 年度 orchestrator 的 `--true-five-year-refit` 必须配套非默认 `--final-refit-run-suffix`，不得覆盖现有 `__refit01` effective-window 产物；selected candidate lineage 不变，更长窗口重新选参属于另一个实验。
- 当前 true-five-year continuous result 改善回撤和 CAGR，但只读 summary 查询确认 v3 contract Sharpe=`0.6075887294330015`、contract Calmar=`0.3724820349585642`，仍未通过 v3 hard gates（contract Sharpe `<0.70`，contract Calmar `<1.0`），不得自动标 accepted baseline 或 promotion source；legacy summary Sharpe=`0.6834026126199905` 不得当作 v3 contract Sharpe 引用。
- 2026-06-12 DECISION-20260612-02：true-five-year continuous（`s1_annual_roll_synth_continuous_true5y_2021_2026_n20_w075_v20260611_01` / `bt_s1_annual_roll_continuous_true5y_2021_2026_n20_w075_v20260611_01`）已采纳为研究 baseline，新实验的 prediction 流与对照 backtest 一律从记忆解析 true5y ids、报告注明基线版本；effective-window continuous 降级为历史参照；baseline ≠ accepted、不得 promotion 的约束不变。
- 2026-06-12 DECISION-20260612-03：baseline 数字进一步切换为 CA-on 口径（`bt_s1_annual_roll_continuous_true5y_2021_2026_n20_w075_v20260611_01_ca01`；dividend 补采后展示数字为 CAGR `15.36%` (`0.153578`) / contract Sharpe `0.6685` / Calmar `0.4103`，`DECISION_LOG.md` 原数字文本不改）；v1 ledger 的"未复权口径、持有期除权简化"约定 superseded；**后续所有实验必须显式传 `corporate_actions=cash_div_and_split_v1` / `dividend_tax_mode=flat_10pct`**（代码默认值保持 `none_v1` 以保历史可复现，纪律由 run 参数与 QA 断言执行）；Sharpe 距 0.70 门约 `0.0315`、Calmar 未过，baseline ≠ accepted。
- 2026-06-10 Strategy1 research schema migration 约束：`sql/research/01_research_strategy1_tables.sql` 是新环境 canonical contract，但由于使用 `CREATE TABLE IF NOT EXISTS`，不会把新增列传播到已存在的 `ashare_research` 表。任何既有 research 表的 additive schema 变更必须同步写入 `sql/research/02_research_strategy1_additive_migrations.sql`，使用 idempotent `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`；不得用 `CREATE OR REPLACE` 重建已有 populated research 表，除非 owner 明确批准 destructive reset。每次 research contract / migration 变更后，以及重建部署应默认写 research 的 Cloud Run jobs 前，必须运行 `sql/research/03_qa_research_schema_readiness.sql`，确认 15 张 research 表、关键列/类型、分区、聚簇、lifecycle DEFAULT、partition filter 和 runtime 关键列仍与契约一致。
- 策略 1 Cloud Run 训练回测执行器按 `docs/prd/PRD_20260604_04_策略1CloudRun训练回测.md` 实现：scikit-learn / 后续 Python 模型库替代策略模型训练 / 预测，不替代 BigQuery DWS/ADS、GCS artifact、报告诊断和 QA；P0 仍需 BigQuery / BigQuery Storage / GCS 客户端、`pyarrow`、`polars` 或 `pandas`、`joblib` 等依赖。Cloud Run Python ledger 必须实现同一 `ledger_exec_v1` 交易语义；历史 BigQuery SQL ledger 只保留为已写产物和历史说明，不再保留可重跑 fallback。Cloud Run 多实验 orchestrator 的默认并发数必须等于 manifest 可执行实验数量；`--max-parallel-experiments` 未设置或为 0 时不得隐式降到 2、1 或其他保守默认值，owner 显式传 N 时才限流。sklearn P0 默认 `class_weight=None`，正则网格按 sklearn 原生 `C` / `penalty` / `l1_ratio` 重新定义；后续 accepted baseline 由 Cloud Run / native acceptance gate 决定，不再以 BQML parity 作为硬门槛。
- 2026-06-05 策略 1 Cloud Run 真实 smoke 约束：训练/预测 job 与回测/报告 job 至少按 16Gi/4CPU、`--max-retries=0` 部署；runtime service account 必须具备源数据读取、BigQuery jobUser、`ashare_ads` dataset 写权限、`gs://ashare-artifacts` objectAdmin 和日志写入权限。容器镜像需安装 CJK 字体（当前使用 `fonts-noto-cjk`）并在 `render_report.py` 中配置可用中文字体候选，否则中文报告图表会出现 glyph missing。真实 smoke `s1_cloudrun_sklearn_smoke_20260604_02` 可运行但 `model_quality_status=model_quality_not_equivalent`；该结论只说明 sklearn 首版未复刻 BQML，不再阻断后续按 native acceptance gate 寻找新的 Cloud Run baseline。
- 2026-06-05 sklearn native search 约束：当前 `asia-east2` Cloud Run 配额已提升到约 40 vCPU / 160Gi，`strategy1-train-candidate-fanout-job` 可按 `1 CPU / 4Gi`、`--tasks=36` 并发跑第一轮 36 个 LogisticRegression 候选；更大规模（如 100 候选）仍应按配额和实际 OOM 情况设置 owner 显式 `--candidate-parallelism`。sklearn native baseline 不再要求通过 BQML parity gate；BQML reference 只作历史对照，accepted 只能按 `docs/prd/PRD_20260605_03_策略1Sklearn模型实验.md` 的 valid-only Top 5、native acceptance gate、`test_reuse_wave_no` / owner approval / final holdout 规则决定。
- 2026-06-06 PRD04 LightGBM search 真实资源约束：LightGBM candidate smoke 后，40 候选不再按 `40 * 1 vCPU / 4Gi` 全并发执行，当前 P0 运行口径为 `candidate_count=40`、`candidate_parallelism=20`、单 task `2 vCPU / 8Gi`；Cloud Run Job spec 的 `parallelism=20` 是实际并发上限，manifest 默认不应再额外分批，只有 owner 显式传入更小 `--candidate-parallelism` 时才分批。Wave 2 binary LightGBM search `cloudrun_python_lgbm_pvfq_n30_bw_h5_20260605_01` 已按该口径完成，Top5 均 rejected，未建立 `cloud_run_python_baseline_v1`。
- 2026-06-06 策略 1 验收门 v2 约束：`configs/strategy1/model_acceptance_contract_v2.yml` 是 v2 阈值、边界和指标定义唯一事实来源；v2 诊断、后续 Python acceptance 和 `22_qa_acceptance_gate_v2_outputs.sql` 必须写入并校验同一 `acceptance_contract_version` / `acceptance_contract_sha256`，且 `22` QA 的 standalone placeholder hash 必须 fail-loud，生产/真实验证需注入契约真实 hash。`scripts/strategy1/diagnose_acceptance_gate_v2.py` 只能只读 ADS/DWD/DWS 并写 local/GCS `acceptance_gate_v2/` artifact，不得训练、不改 prediction、不写 ADS。组合候选固定为 `target_holdings=10/20/30/40`，不得加入 `50/100/150`；`10/5%` 是 `diagnostic_cash_control`，不参与 accepted production baseline 和满仓现金 hard gate；可进入 accepted 判定的候选只含 `20/5%`、`30/5%`、`40/5%`。当前 extended reference run `s1_bqml_baseline_pvfq_n30_bw_h5_extended_20260604_01` / `bt_s1_bqml_baseline_pvfq_n30_bw_h5_extended_20260604_01` 在 v2 下为 `rejected`，拒绝范围只针对当前 top-30 long-only 组合实现，不否定信号家族。
- 2026-06-06 策略 1 production acceptance 的交易执行约束：后续 v2 accepted production baseline 必须使用 Cloud Run Python lot-aware ledger（当前实现目标版本 `ledger_exec_v1_lot100`，或后续明确升级版），不得用 FLOAT-shares backtest 判定 accepted。买入必须按 100 股整数手向下取整且最小买入 1 手；清仓卖出允许 odd-lot 全部退出；部分卖出非清仓必须向下取整到 100 股并保留残股；P0 不做余现金二次分配。旧 BQML / SQL ledger / FLOAT-shares 结果只保留为 historical reference / audit，报告和 acceptance artifact 必须显式标记，QA 必须阻止未标记 legacy/audit 的 FLOAT result 进入 v2 accepted 判定。`ledger_exec_v1_lot100` 已成为 Cloud Run Python 默认回测路径；legacy FLOAT 只能通过显式 `--use-float-ledger` 运行。fixed-prediction lot-aware reference `s1_lotaware_ref_pvfq_n30_bw_h5_20260606_01` / `bt_s1_lotaware_ref_pvfq_n30_bw_h5_20260606_01` 已完成并通过 `23` 与 acceptance gate v2 QA，但 v2 状态仍为 `rejected`。
- 2026-06-07 fixed-prediction / portfolio-only runner QA 约束：`10_qa_runner_outputs.sql` 的 split 参数必须匹配 source `ads_ml_training_panel_daily.split_tag`，不能为了 v2 acceptance 的解释口径强行传不同 split。当前 source prediction run `s1_bqml_baseline_pvfq_n30_bw_h5_extended_20260604_01` 的 training panel 只有 `valid=2024-01-02..2024-12-31` 和 `test=2025-01-02..2026-04-30`，没有单独 `final_holdout` split；因此复用该 prediction stream 的 portfolio-only / lot-aware runner `10` QA 应传 `p_test_end=2026-04-30`、`p_final_holdout_start/end=NULL`。2026 final_holdout 证据由 `diagnose_acceptance_gate_v2.py` 和 `22_qa_acceptance_gate_v2_outputs.sql` 按 `model_acceptance_contract_v2` 日期单独切分，不依赖 training panel 的 split_tag。
- 2026-06-06 策略 1 尾部风险 P0 诊断约束：`scripts/strategy1/analyze_tail_risk.py` 只能只读 `ashare_ads` / `ashare_dwd` / `ashare_dim` 并写 local/GCS `tail_risk/` artifact；不得写入或更新 ADS 核心表，不得改变 candidate、target、order、trade、position、NAV、summary 或 acceptance 结果。读取 `dws_stock_feature_daily_v0` 必须按当前 experiment 的 `feature_version` 过滤，禁止多版本特征行静默扇出。诊断必须记录并校验 ADS pre/post summary/NAV hash；若 hash 变化必须 fail-fast，不能按普通诊断异常降级。`backtest_report.py` 对非 guard 类尾部风险诊断失败可 fail-soft，写 `tail_risk_failure.json` 并跳过 `20`，不回滚已成功的报告 / 模型诊断 / `10` / `12`。`sql/strategy1/qa/qa_tail_risk_outputs.sql` 只复算 ADS/DWD 派生不变量，不承担 artifact 文件存在性校验；artifact manifest 和只读 guard 由 Python 脚本强制。
- 2026-06-06 策略 1 尾部风险 P1 profile 约束：`tail_risk_profile_id` 默认必须是 `diagnostic_only`，不得改变选股；只有 manifest / experiment 显式设置 `individual_risk_guard_v0` 时，`05_build_candidates.sql` 才可在 candidate selection 层写入 `tail_risk:*` 风险标记。
- P1 风险规则固定为 `ret_20d < -30%`、`drawdown_20d < -30%`、`limit_down_days_20d >= 2`、`one_word_limit_days_20d >= 1`、`total_mv_cny < 30e8`、`circ_mv_cny < 20e8`，且必需风险字段 NULL 记为 `tail_risk_required_field_null`；`vol_20d` p95 和 `turnover_rate_ma20` p98 仅用于诊断标记，不得硬排除。
- 风险标记股票仍可进入目标组合，避免候选层全量重构把已持仓风险股强制卖出；真正的 P1 拦截发生在 Ledger v1 执行层：若目标股带 `tail_risk:*` 且执行前没有持仓，则写 `BUY_SKIPPED_TAIL_RISK`，不成交、不候补、现金保留。
- 已有持仓不因 P1 风控标记被强制卖出，仍按 Ledger v1 目标组合、实际持仓 netting 和 pending sell 语义处理。
- P1 必须由 `10_qa_runner_outputs.sql` / `20_qa_tail_risk_outputs.sql` 验证未持仓风险目标没有真实买入成交并留下 `BUY_SKIPPED_TAIL_RISK`。
- 2026-06-11 full-period Overlay A/B 已完成：A1/A3 能改善 2024-01~02 crunch 段 vs `000852.SH` 超额，但全周期 CAGR / Calmar 损耗过大；A2 能降低 MaxDD 但 CAGR 下降且 Calmar 未改善。
- owner 作出新决策前，不得把 `individual_risk_guard_v0`、`market_risk_off_v0` 或组合 profile 设为默认，也不得据此 promotion。
- sklearn native search 的 36 个 candidate task 可并发训练，但 TopK select/register/predict 会同时写 `ads_model_registry` / prediction 等 ADS 表；BigQuery 同表 load/table update 可能触发短时 429 `TooManyRequests`。Python BigQuery load helper 必须对该类限流做退避重试；TopK 排名阶段只需要 candidate metrics/status，不应下载或反序列化 `model.joblib`，避免本地 orchestrator 被不必要的模型 artifact 传输和 import 开销拖住。
- **历史 BQML runner 并发事故记录，仅作审计，不再作为修复 runbook**（2026-06-02 实跑事故教训）：
  - 旧 `02_train_bqml_logistic_candidates.sql` 曾用 BigQuery ML `CREATE OR REPLACE MODEL` 顺序训练候选；同一 run_id 的两个 `02` 执行重叠会触发 `Concurrent model update with retrain is not supported`，且 CLI 报错不代表后台 job 已停止。
  - 事故经过：误判第一个 `02`「被拒绝/失败」而重跑，导致两执行重叠；随后又依据过期 `bq ls` 快照删除仍在运行 job 正在训练的模型，造成 registry 指向已删模型。
  - 现行约束：BQML-only `02/03/04` 已删除，禁止再按“重跑 02”修复 registry / 模型不一致。历史 BQML 结果只作为 ADS / GCS / 文档审计引用；若未来确需恢复或重建 BQML 历史对象，必须另写 PRD 并经 owner 明确批准。当前 Cloud Run Python path 的重跑 / 覆盖只能使用对应 `scripts.strategy1_cloudrun.*` 入口和其显式 `--force-replace` 语义。
- **旧 OQ-010 SQL/BQML 并发调度器约束已退役，仅作历史审计**：`scripts/strategy1/run_oq010_experiments.py`、BQML-only `02/03/04` 与 SQL ledger fallback `08_run_backtest.sql` 已删除，不再是现行基础设施；因此旧 Phase 2-4（portfolio-only 并发执行、`08` ledger 并发、retrain 训练锁）不再推进。当前 Strategy1 多候选搜索和回测调度入口为 Cloud Run Python `scripts.strategy1_cloudrun.orchestrate_*` 系列；仍保留 `sql/meta/02_strategy1_experiment_run_status.sql` 作为 Cloud Run orchestrator 审计 / resume 输入，GCS lock 与状态表约束适用于现行 Cloud Run orchestrator，不得恢复旧本地多进程 SQL runner 或绕过 Cloud Run orchestration 裸跑已退役路径。

## 2026-06-08 OQ-005 Composer monitoring / full rebuild constraints

- `airflow_monitoring` 是 Cloud Composer 托管的环境健康 DAG，不由仓库 DAG 代码控制；在 Composer 仍存在时不能靠改 repo 把它降频到每小时以内。要消除这部分 run/底座费用，必须 cutover 后删除 Composer 环境。
- 2026-06-08 起，生产业务调度唯一入口是 `Cloud Scheduler -> Cloud Workflows`：`ashare-ods-ingestion-daily`（`0 20 * * *`）负责日常 ODS + child warehouse refresh，`ashare-pipeline-alert-checker`（`0 * * * *`）负责小时级告警检查。Composer 业务 DAG 已停用，不应再把 Composer 当生产入口恢复或继续追加业务变更。
- `ashare-composer` 环境已于 2026-06-08 删除完成；后续若需要重新引入 Composer，必须视为新的架构决策，而不是当前 OQ-005 路径上的默认运维动作。
- `orchestration/composer/**` 现仅保留为历史审计快照；当前生产部署、调度变更、runbook 更新和 smoke 路径都应落在 `orchestration/workflows/**`，不再向 Composer DAG / README 叠加新生产逻辑。
- `ashare_pipeline_alert_checker` 在迁移期与迁移后统一按“最多每小时 1 次”设计：schedule `0 * * * *`，lookback `70` 分钟，heartbeat 缺失告警窗口 `120` 分钟，避免 1 小时 cadence 下出现空窗误报。
- 2026-06-08 真实 cutover 后，生产定时入口已改为 Cloud Scheduler jobs `ashare-pipeline-alert-checker` 与 `ashare-ods-ingestion-daily`，两者统一由 `ashare-scheduler-invoker@data-aquarium.iam.gserviceaccount.com` 调 Workflows Executions API。ODS daily 固定 `0 20 * * *` / `Asia/Shanghai`，payload 固定 `pipeline_dry_run=false`、`scheduled_run=true`、`run_label=scheduled_daily_ingestion`；`ashare_warehouse_window_refresh` 不建立独立 daily scheduler，继续只作为 `ashare_ods_ingestion_daily` 的同步 child workflow，避免双入口写生产窗口。
- OQ-005 scheduler least-privilege 约束：`ashare-workflows-runtime@data-aquarium.iam.gserviceaccount.com` 不应再持有项目级 `roles/run.developer`。它在 Cloud Run 侧的最小集合是：
  - `ashare-pipeline-control` service 上的 `roles/run.invoker`
  - `ashare-ingest-current-scope` Cloud Run Job 上的 `roles/run.jobsExecutorWithOverrides`，因为 workflow 会用 overrides 传入生产参数
  - 项目级 `roles/run.viewer`，用于读取 Cloud Run operation / execution 状态；没有 `run.operations.get` 时，job 启动后会在 operation polling 阶段 403
  任何后续 cutover / bootstrap 脚本都应显式移除项目级 `run.developer`，避免该 runtime SA 获得任意 Cloud Run service/job 的创建、修改或删除能力。
- OQ-005 cutover 顺序约束：cutover helper 不得先 enable Scheduler jobs、后 pause Composer。安全顺序必须是：先创建/更新 Scheduler jobs 为 `PAUSED`，再 pause Composer 业务 DAG，完成至少一次真实 fire 验证后，才显式 `resume` Scheduler jobs。
- `ashare_warehouse_full_rebuild` 的 Workflows 路径现已改为 `ashare-pipeline-control` 服务端 async `submit + poll`，不能再退回同步 `job.result()`；否则会重新暴露 Cloud Run request timeout / Workflows `http.*` step timeout 风险。共享 warehouse-write lock 仍为同一把 `ashare_warehouse_window_refresh` 锁，full rebuild lease 现为 `21600s`。
- `ashare_warehouse_full_rebuild` 的 async poll 循环本身也必须续租共享写锁，不能只在每个 BigQuery step 结束后 heartbeat；否则单个 step 时长超过 lease 时，会被 stale-lock reclaim 打开并发写窗口。对 `get_job(...)` 的瞬时错误，不得首错即落 `failed`；必须先在控制面重试。只有两种情况允许写 task `failed` 终态：`job.error_result` 真实存在，或 `get_job(...)` 在全部内部重试耗尽后仍无法读取 job 状态，避免 `pipeline_task_status` 长时间卡在 `running`。
- `ashare-pipeline-control` 的 BigQuery poll 重试当前使用同步退避 `time.sleep`，一次 `/v1/tasks/bigquery/poll` 最坏会因 `get_job(...)` 内部重试额外阻塞约 `15s`（`1+2+4+8`）；这是当前 thin-adapter 设计的显式容量约束。若 shadow run / cutover 后出现 worker 饱和，再考虑把 poll 从“同步 sleep + 单请求重试”拆成更细的状态机。
- 标准 `deploy_workflows.sh` 仍默认不部署 `ashare_warehouse_full_rebuild`；必须显式 `DEPLOY_FULL_REBUILD=true` 才会下发该 workflow。即使已部署，它也只允许手工触发，且必须显式传入 `confirm_full_rebuild=true` 与 `date_from/date_to`；本轮只验证了 direct async control-plane smoke 和 workflow `pipeline_dry_run=true`，未执行真实全量写入。
- 启用 `Cloud Scheduler -> ashare-pipeline-control /v1/tasks/alert-check` 时，必须同步 pause / delete Composer DAG `ashare_pipeline_alert_checker`，避免双跑、重复 heartbeat 和重复 alert 日志。
