# 实现状态（Implementation Status）

这是实现状态的唯一事实来源。面向「已完成/进行中/受阻的整体状态」；「下一步要做什么」见根目录 `TODO.md`。

Last updated: 2026-06-06

## 当前状态

> 最新补充（2026-06-06）：OQ-005 scheduled 非交易日 skip gate 已在工作树 `/Users/luna/Desktop/git/quant-ashare-oq005-nontrading-skip`、分支 `codex/oq005-nontrading-skip` 实现。`orchestration/composer/dags/ashare_daily_pipeline_v0.py` 在 `pipeline_start_status` 后新增 `non_trading_day_gate`：仅对 scheduled `daily_current` 生效，查询 `ashare_dim.dim_trade_calendar` 的 SSE 当日开市状态；若当天不开市，则进入 `skip_non_trading_day`，跳过 ingestion、ODS readiness 和 transform，并向 `ashare_meta.pipeline_task_status` 写入 `status='skipped'` 的 `skip_non_trading_day` task 行。手工触发、`backfill`、`qa_only`、`full_rebuild` 和 legacy full refresh 继续走原链路；上一交易日修复仍必须显式 `backfill`。本分支同步更新 Composer README、OQ-005 runbook、TODO 和记忆；尚未部署到 Composer，合并后需用周末/节假日 scheduled 口径 smoke 验证状态写入和 Cloud Run 未触发。

> 最新补充（2026-06-06）：PRD04 Cloud Run Python baseline search PR #79 review follow-up 与 residual follow-up 已在分支 `codex/implement-prd04-cloudrun-python-baseline` 完成。采纳 H1/H2/M1/M2/M3/L1/L2：SQL `18`/`19` QA 阈值改为由共享契约 `configs/strategy1/model_acceptance_contract_v1.yml` 注入；final_holdout 缺失改为 `needs_more_evidence` 而非 hard `rejected`；`19` 的 NULL 漏过断言和 `data_end_date` 上界断言已修；valid/test 起点对齐 PRD 为 `2024-01-02` / `2025-01-02`；auto-next-wave 改为当前 wave QA 后触发且下一波失败不让父 wave 失败；`allowed_score_orientation` / `weak_valid_rank_ic_threshold` 已接入；`unmatched_acceptance_state` 兜底和 QA 已补。LightGBM smoke 暴露 `1 vCPU / 4Gi` 不足，P0 资源口径已收敛为 40 候选、Job spec `parallelism=20`、candidate task `2 CPU / 8Gi`；`strategy1-train-candidate-fanout-job` 当前线上配置已复核为 `taskCount=40` / `parallelism=20` / `2 CPU / 8Gi`，运行手册也已同步为 `20 / 2 CPU / 8Gi`。真实 wave 2 search `cloudrun_python_lgbm_pvfq_n30_bw_h5_20260605_01` 已完成，Top5 全部 `rejected`，不建立 `cloud_run_python_baseline_v1`；已修正 ADS/状态表审计元数据并跑通真实 `19_qa_cloudrun_python_baseline_search_outputs.sql` 和 `18_qa_sklearn_native_search_outputs.sql`。本轮 residual follow-up 继续对齐 `config.py` / `ledger.py` / `01` / `10` 的 fallback split 默认日期，给 `18` / `19` 的 `LOGICAL_AND` 增加 `qa_required()` 严格 NULL 处理，并注明 SQL `DECLARE` 默认只是 standalone fallback、生产必须由 orchestrator 从共享契约注入。下一步：合并 PR #79 后构建/部署新镜像，再执行 wave 3 `lightgbm_regression`。

> 最新补充（2026-06-06）：PR #80 已合并并完成 Composer 生产部署 / smoke 验收。已同步 `orchestration/composer/dags/ashare_daily_pipeline_v0.py` 到 Composer `dags/`，同步仓库 `sql/` 到 `gs://asia-east2-ashare-composer-b2629133-bucket/data/sql/`；部署后复核 DAG 与 `sql/qa/09_ods_daily_partition_readiness.sql` 的本地 / bucket SHA256 完全一致。`manual_pr80_daily_current_20260605_20260606_01` 使用 `business_date=2026-06-05`、`pipeline_dry_run=false`、`warehouse_mode=daily_current` 成功完成 current_scope 采集、ODS readiness、窗口 DIM/DWD/DWS 刷新和 `10_windowed_stock_refresh_checks`；采集后 2026-06-05 strong endpoint 行数为 daily 5514、daily_basic 5514、adj_factor 5526、stk_limit 7634、index_daily 7。`manual_pr80_qa_only_20260605_20260606_01` 使用 `skip_ingestion=true`、`warehouse_mode=qa_only` 成功完成 readiness + P0 / strategy1 / OQ004 / finance / OQ006 五个只读 QA。数据复核：`2026-06-05` DWD/DWS 主键均唯一，`2026-05-11..2026-06-05` 最近 20 个交易日 ODS daily_basic → DWD valuation → DWS valuation 行数均为 110,035，错配天数 0。`orchestration/composer/README.md` 已同步修正手工触发默认日期说明为 `data_interval_end` 口径。OQ-005 仍 open，剩余 Dataform 生产链路、补跑/resume 自动化和完整 ODS→ADS 运维观测闭环；非交易日自动 skip gate 已在 `codex/oq005-nontrading-skip` 实现，待合并部署 smoke。

> 最新补充（2026-06-06）：PR #80 readiness review follow-up 已在工作树 `/private/tmp/oq005-scheduler-fix`、分支 `codex/oq005-scheduler-daily-fix` 完成。`sql/qa/09_ods_daily_partition_readiness.sql` 将 non-blocking warning MERGE 前置到阻断 ASSERT 前，保证 strong endpoint 缺失导致任务失败时，weak endpoint 缺失 / API 行数上限风险仍可写入 `pipeline_task_status`；同时 `pipeline_dry_run=true` 完全跳过 warning 写入，非交易日不因 weak endpoint 缺失写 warning。实际验证：`manual_verify_oq005_warning_before_assert_20260606_01` 在 `2026-06-05` strong 缺失失败前写入 4 条 weak warning；`manual_verify_oq005_dryrun_no_warning_20260606_01` 在 dry-run 强制分区失败后状态表 0 行；`manual_verify_oq005_nontd_weak_suppressed_20260606_01` 在 `2026-06-06` 非交易日强门禁失败后状态表 0 行。同步更新 runbook、KNOWN_CONSTRAINTS、TODO、OPEN_QUESTIONS 和本交接；该 follow-up 已随 PR #80 合并并在上方部署记录中同步到 Composer bucket。

> 最新补充（2026-06-05）：OQ-005 scheduler daily-date / readiness follow-up 已完成验证并部署到 Composer bucket。`ashare_daily_pipeline_v0` 的 scheduled run 默认 `business_date` / `date_to` 已从 Airflow `ds` 改为 `data_interval_end.in_timezone('Asia/Shanghai')`，20:00 CST scheduled run 取当天日期，手动 `dag_run.conf.business_date/date_to` 仍最高优先级；Airflow task callback 状态回写也已同步改用同一 `data_interval_end` 口径，避免 `pipeline_task_status` 继续记录前一天。`sql/qa/09_ods_daily_partition_readiness.sql` 已按 strong/weak endpoint 固化：strong 为 `daily/daily_basic/adj_factor/stk_limit/index_daily`，缺失阻断；weak 缺失和 API 单次返回上限命中写入 `pipeline_task_status` 的 `warning` 行，不覆盖主 readiness task。已同步 `orchestration/composer/dags/ashare_daily_pipeline_v0.py`、`sql/qa/09_ods_daily_partition_readiness.sql`、`sql/meta/01_create_meta_tables.sql` 到 Composer bucket。smoke `manual_smoke_scheduler_fix_20260605_04` 使用 `business_date=2026-06-05` / `warehouse_mode=qa_only` / `skip_ingestion=true`，最终按预期在 `ods_daily_partition_readiness` 以 `QA-ODS-DAILY-2` 失败并阻断下游；状态表记录 `business_date=2026-06-05`，task_type=`qa`，ODS 复核确认 `daily` / `daily_basic` 只有 `partition_date=20260604`、没有 `20260605`。该时点 DAG 尚未实现非交易日自动 skip gate；后续已由 `codex/oq005-nontrading-skip` 分支实现，待合并部署 smoke。

> 历史补充（2026-06-05）：新增 `docs/prd/PRD_20260605_04_策略1CloudRunPython模型基线搜索.md`，作为 OQ-010 在 sklearn native 首轮失败后的下一轮模型搜索方案。本 PRD 固定本轮数据截止 `2026-04-30`，切分为 train `2019-04-03..2023-12-31`、valid 2024、test 2025、final_holdout `2026-01-05..2026-04-30`；固定 `pv_fin_quality + 30/5% + biweekly + 5d`、沪深主板股票池、成本 profile 和 Ledger v1，不改交易参数。P0 推荐 LightGBM wave 2，候选排序使用 2021/2022/2023 purged walk-forward CV + 2024 valid confirmation，2025 test 做硬接受门，2026 final holdout 只做明显坏结果 veto / holdout watch；验收状态机为互斥完整、机器可校验，并对齐共享验收契约 `configs/strategy1/model_acceptance_contract_v1.yml`、Sharpe `>=0.70`、max drawdown `>=-25%` 等风险门槛。若 binary LightGBM rejected，下一步优先试 `lightgbm_regression` 再考虑 XGBoost / 特征增强。

> 最新补充（2026-06-05）：OQ-005 alert checker liveness follow-up 已完成生产部署与验收。PR #75 合并后，已同步 `orchestration/composer/dags/oq005_alert_checker.py` 到 Composer `dags/`，同步 `scripts/alerting/check_alerts.py` 到 Composer `data/scripts/alerting/`；新增 `oq005_alert_checker_heartbeat` log-based metric 和 `OQ-005: Alert Checker Heartbeat Missing` 30 分钟 absence policy，policy 已启用并绑定 1 个 Email 通知渠道。部署 smoke：`manual_oq005_alert_checker_heartbeat_20260605_01` 成功后发现 Composer 默认日志 resource 为 `k8s_container`，与现有 OQ-005 policy 的 `resource.type="global"` 不一致；已修复 `check_alerts.py` 显式用 global resource 写业务告警与 heartbeat，并重新同步到 Composer。最终 smoke `manual_oq005_alert_checker_heartbeat_global_20260605_01` 成功，随后的 scheduled run 也成功；Cloud Logging 中 heartbeat 为 `resource.type=global`、`lookback_minutes=20`、`alerts_count=0`，Cloud Monitoring `logging.googleapis.com/user/oq005_alert_checker_heartbeat` 的 global timeSeries 已有点。同步修复 `setup_alerts.py`：正确导入 Logging Metrics client / LogMetric，设置 `policy.combiner=OR`，threshold/absence filter 加 `resource.type="global"`，用稳定 `user_labels.oq005_policy` 做幂等键并兼容旧 display name 迁移，避免旧名环境重复创建新旧两份 policy。

> 最新补充（2026-06-05）：PR #75 review follow-up 已在分支 `codex/oq005-alerts-ops` 修复告警 checker 静默失效风险。本次修改 `orchestration/composer/dags/oq005_alert_checker.py`，将 `check_alerts.py` 生产调用窗口从 10 分钟放宽到 20 分钟，并把返回码处理改为只接受 `0/1/2`：`0` 无异常成功、`1` 查询/写日志失败并让 DAG fail、`2` 发现业务告警但 DAG 不失败，其他返回码一律 fail-closed。`scripts/alerting/check_alerts.py` 新增 `--write-heartbeat`，每次成功查询且必要告警日志写入成功后写入 checker heartbeat；`scripts/alerting/setup_alerts.py` 新增 `oq005_alert_checker_heartbeat` log-based metric 和 30 分钟 absence alert policy，作为告警链路自身 dead-man's-switch，并修复 Monitoring API duration / condition enum / condition field 写法以保证真正 apply 时可创建策略。`scripts/alerting/README.md`、`TODO.md`、`OPEN_QUESTIONS.md` 与 `AGENT_HANDOFF.md` 已同步。该 follow-up 只改代码和文档，尚未同步 Composer bucket 或应用新增 liveness policy；合并后需部署并验收 checker heartbeat absence 告警。

> 最新补充（2026-06-05）：OQ-005 告警/观测生产闭环已部署并验收完成。8 个 BigQuery 观测视图已创建（v_pipeline_recent_runs / v_pipeline_failed_tasks / v_pipeline_qa_failures / v_ingestion_failures / v_ingestion_empty_returns / v_pipeline_daily_health / v_alert_summary / v_alert_probe）。3 个 Cloud Logging log-based metrics 已创建（oq005_pipeline_failure / oq005_task_failure / oq005_ingestion_failed）。3 个 Cloud Monitoring alert policies 已启用并关联 Email 通知渠道：OQ-005: Pipeline Run Failed (ERROR) / OQ-005: Task Failed (ERROR) / OQ-005: Ingestion Failed (WARNING)。Ingestion severity 已从 CRITICAL 修正为 WARNING。定时 checker DAG `oq005_alert_checker` 已部署到 Composer（每 10 分钟），手动触发 smoke 成功。三类告警 smoke 均在 Cloud Monitoring timeSeries 中验证 value=1：pipeline_failure 10:25:47 UTC、task_failure 10:55:20 UTC、ingestion_failed 10:55:20 UTC。`check_alerts.py --lookback-minutes 10 --json` 查询正常返回 `[]`。下一步进入 Dataform definitions、策略 runner/report 可选分支、补跑和生产运维观测闭环。

> 最新补充（2026-06-05）：owner 已确认策略 1 后续不再使用 BQML 以及 `sql/ml/strategy1` SQL runner 作为训练、预测、回测、报告、诊断、月度滚动重训或多实验搜索的默认 / fallback / 新增开发路线。既有 BQML baseline、SQL runner、报告和 QA 结果保留为 historical reference / audit，不再作为未来实现目标。BigQuery SQL 仍继续用于 ODS→DIM/DWD/DWS/ADS 数仓转换、metadata、单位契约、QA、状态表和只读分析；本决策只废弃策略执行层 SQL runner。OQ-010 下一步应先寻找可接受的 Cloud Run Python 模型 / backend baseline，并在实现前把 `docs/prd/PRD_20260604_02_策略1月度滚动重训.md` 改造为 Cloud Run Python / backend-neutral 口径。PR #76 follow-up 已在该 PRD 文首补 `DECISION-20260605-03` superseded banner，防止按旧 BQML 口径直接实现；正文仍待后续正式改造。

> 最新补充（2026-06-05）：OQ-010 Ledger v1 P1/P2 已完成现有结果复核和收尾。先取消误启动的重复 2026 扩展 BigQuery job `bqjob_r2337986e7fdea586_0000019e9752fad3_1`，并清理 run_id `s1_bqml_baseline_pvfq_n30_bw_h5_ext20260430_v20260605_01` / backtest_id `bt_s1_bqml_baseline_pvfq_n30_bw_h5_ext20260430_v20260605_01` 在 ADS 训练面板、registry、prediction、candidate、portfolio、order 和 backtest 相关表中的残留，复核 10 张相关表均为 0 行。已确认已有 P1 extended fresh run `s1_bqml_baseline_pvfq_n30_bw_h5_extended_20260604_01` / `bt_s1_bqml_baseline_pvfq_n30_bw_h5_extended_20260604_01` 覆盖 `2024-01-02` 至 `2026-04-30`，total_return 35.16%、excess_return -7.22% vs `000852.SH`、Sharpe 0.819、max_drawdown -14.46%，报告和诊断 uploaded 到 GCS。2026 段（`2026-01-05` 至 `2026-04-30`）策略收益 -2.45%，同期中证1000 +10.36%、沪深300 +3.83%、上证50 -1.51%，分别跑输 12.81pct / 6.28pct / 0.95pct。已确认 P2 resume run `s1_bqml_baseline_pvfq_n30_bw_h5_resume_20260604_01` / `bt_s1_bqml_baseline_pvfq_n30_bw_h5_resume_20260604_01` 从父回测 `2025-12-31` 状态恢复，`sql/ml/strategy1/15_qa_ledger_resume_consistency.sql` 6 个断言全部通过，full fresh 与 resume segment 在 NAV、现金、日收益、持仓和成交事实上一致。OQ-010 后续重心转为 Cloud Run Python 可接受 baseline、月度滚动重训 PRD 改造，以及后续模型实验路线。

> 最新补充（2026-06-05）：OQ-005 告警与运维观测 PR #72 在工作树 `/Users/luna/Desktop/git/quant-ashare-oq005-alerts-runbook`、分支 `codex/oq005-alerts-runbook` 完成 review follow-up。新增/修订 `sql/observability/01_pipeline_status_views.sql`（pipeline / task / QA / ingestion / daily health / alert summary / probe 视图）、`scripts/alerting/setup_alerts.py`、`scripts/alerting/check_alerts.py`、`scripts/alerting/README.md` 和 `docs/OQ005-Pipeline-补跑与故障恢复-Runbook.md`。本轮按 comment 修复：Cloud Logging `LogMetric` 使用正确 `filter` 字段；`check_alerts.py` 查询失败继续 exit 1，`--write-log` 缺 `google-cloud-logging` 时 exit 1；默认查询窗口收敛为 10 分钟并用稳定 `insert_id` 降低重复日志；runbook §9 的 `task_failure` / `ingestion_failed` 口径与 `v_alert_summary` 对齐；`v_alert_probe` 改为明确的 24 小时手工健康检查视图。验证：Python `py_compile` 通过，观测 SQL BigQuery dry-run 通过，`git diff --check` 通过；本地未安装 `google-cloud-logging`，因此只验证缺依赖 fail-closed 路径，待 PR 合并后仍需部署视图、配置 log-based metrics / alert policies 和定时 checker production smoke。

> 最新补充（2026-06-05）：PR #70 合并后，OQ-005 window hardening 已在当前 `main` 工作树 `/Users/luna/Desktop/git/quant-ashare` 完成只读部署复核与生产 smoke 验收。Composer bucket `gs://asia-east2-ashare-composer-b2629133-bucket/data/sql/` 中的 `sql/incremental/01_refresh_stock_dwd_dws_window.sql` 和 `sql/qa/10_windowed_stock_refresh_checks.sql` 与当前 `main` 哈希一致。Airflow run `manual_oq005_backfill_smoke_pr70_20260605_01`（`warehouse_mode=backfill`，`2026-06-03..2026-06-04`）成功，`windowed_dim`、`windowed_transform.stock_dwd_dws_window`、`windowed_stock_refresh_checks` 和 finalize 全部 success；`manual_oq005_daily_current_nontd_20260606_01`（`business_date=2026-06-06`）按非交易日归一到 `2026-06-05` 后，因 `2026-06-05` ODS 未采集在 `ods_daily_partition_readiness` 以 `QA-ODS-DAILY-2` 阻断，窗口写入未执行，符合生产门禁预期。`2026-06-03..2026-06-04` 的 QA-WIN-18 只读复核结果为 expected rows 11,022、false/missing 0；有 `pe/pb` 的 valuation 行进入特征宽表缺失数为 0。OQ-005 仍 open，剩余 Dataform definitions、告警、补跑和完整运维观测闭环。

> 最新补充（2026-06-05）：OQ-012 已完成当前读层只读复核。`sql/qa/06_ods_parquet_schema_checks.sql` 对 `priority_filter=P0` 和 `priority_filter=all` 均通过；`ods_tushare_stk_limit` 2019+ 可读行数 10,662,140，`up_limit` / `down_limit` 非空覆盖 10,662,140 行，`pre_close` 可读范围 `0.0..1862.52`。当前 BigQuery 外部表读层未再暴露 schema mismatch；OQ-012 不再按“P0 `stk_limit` 待实际修复”推进，后续待 owner 决定是否归档关闭，或保留 schema contract / ingestion 显式 cast 防复发流程作为工程任务。

> 最新补充（2026-06-05）：sklearn native search 首轮真实执行已收口。PR #73 已合并并部署镜像 `sklearn-native-topk-fix-6856a46-20260605154227`；本次没有从头重训，复用 `search_id=sklearn_native_pvfq_n30_bw_h5_20260605_01` 已完成的训练面板、`prepare_matrix` execution `strategy1-prepare-matrix-job-d697c` 和 36-task candidate fan-out execution `strategy1-train-candidate-fanout-job-tpl9v`。Top5 select/register/predict 与 backtest/report/diagnosis 已全部完成；因本地 orchestrator 中断导致 3 条 backtest 状态曾停在 `cancelled`，已核对 Cloud Run execution 成功后修正状态表并清理 stale GCS locks。comparison artifacts 已刷新到 `gs://ashare-artifacts/reports/strategy1/ml_pv_clf_v0/search_id=sklearn_native_pvfq_n30_bw_h5_20260605_01`，`sql/ml/strategy1/18_qa_sklearn_native_search_outputs.sql` 通过。结论：Top5 均被 native acceptance 拒绝，统一拒绝原因为 `test_year_excess_return<=0.0`；Top1 `elastic_saga_c_0_1_l1_0_5_balanced` full-period total_return 45.31%、excess_return 16.30%、Sharpe 1.089、test RankIC 0.034，但 2025 test-year excess_return -14.92%。本轮不建立 `cloud_run_sklearn_native_baseline_v1`；后续应继续寻找新的 Cloud Run Python baseline，BQML 只保留为历史对照。

> 历史补充（2026-06-05）：PR #71 review follow-up 已在分支 `codex/implement-sklearn-native-search` 完成。修复内容：`fetch_topk_ads_outputs` 现在计算 test 期 `test_top_minus_bottom_fwd_ret_mean`，`decide_acceptance` 和 `18` QA 均要求 accepted 候选满足 valid/test top-minus-bottom 不能同时为负；QA-SKN-13 用 `IFNULL(..., '') != 'passed'` 修复 wave>3 final holdout NULL 漏洞；Python acceptance 的 `test_rank_ic_mean` 与 QA 统一为严格 `> 0`；`rank_candidates` 把 fallback 限定为“全员 valid RankIC 非正”且只允许通过 hard filters 的候选进入 fallback，并写 `failed_no_positive_valid_signal`，同时排除 `convergence_status != 'converged'` 的候选；Top5 单候选失败改为 fail-soft，其他候选继续执行，但 `18` QA 仍要求完整 Top5；另外修复 runtime fetch SQL 中无 `predict_date` 分区过滤的多余 prediction distinct join；最终 follow-up 将 test 侧 `top_minus_bottom_fwd_ret_mean` 从 10 分桶改为 5 分桶，和 valid 侧 `q=5` 口径一致。该条为合并前验证记录，真实部署、Top5 完整回测和 acceptance 结论见上方 sklearn native search 收口条。

> 最新补充（2026-06-05）：OQ-005 daily_current 20 个交易日窗口与状态记录修复已在工作树 `/private/tmp/quant-ashare-oq005-daily-window-hardening`、分支 `codex/oq005-daily-window-hardening` 完成到本地可验证阶段。本分支从最新 `origin/main` 补入本地 `5b62895` 的 20 交易日窗口与 QA-WIN-16/17/18 估值覆盖检查，并进一步硬化非交易日口径：`warehouse_mode=daily_current` 时，`sql/incremental/01_refresh_stock_dwd_dws_window.sql` 和 `sql/qa/10_windowed_stock_refresh_checks.sql` 会先把 `date_to` / `business_date` 归一为不晚于请求日期的最近 SSE 开市日，再计算最近 20 个交易日窗口；`backfill` 继续保持显式 `date_from` / `date_to`。PR #70 review follow-up 已修复 QA-WIN-18：触发口径对齐 `has_valuation_data` 的 `total_mv_cny/circ_mv_cny`，并限制在 price-driven feature universe 内检查；估值覆盖 QA 在 `daily_current` 默认检查 20 个交易日窗口，在 `backfill` 检查实际写入窗口。已同步 `sql/README.md`、`orchestration/composer/README.md`、`ARCHITECTURE_MEMORY.md`、`KNOWN_CONSTRAINTS.md`、`OPEN_QUESTIONS.md` 和 `TODO.md`，去掉 OQ-005 “尚未部署 Composer / 待 smoke”的过期状态，改为记录已完成 Composer SQL/DAG 部署验收、backfill / qa_only / daily_current smoke、`2026-05-08..2026-06-04` 20 个交易日估值链路 110,012 键零缺失。验证：窗口 SQL 和 QA SQL 对 `daily_current business_date=2026-06-06`、`backfill date_from=2026-06-03 date_to=2026-06-04` 的 BigQuery dry-run 均通过；只读窗口计算确认 `2026-06-06` 归一为 `2026-06-05`，`dwd_write_start_date=2026-05-11`，`valuation_coverage_start_date=2026-05-11`；`backfill 2026-06-03..2026-06-04` 的 `valuation_coverage_start_date=2026-06-03`。该分支随后通过 PR #70 合并，Composer bucket 部署与生产 smoke 见上方最新补充。

> 历史补充（2026-06-05）：`docs/prd/PRD_20260605_03_策略1Sklearn模型实验.md` 的 P0 实现已在工作树 `/Users/luna/Desktop/git/quant-ashare-sklearn-native-search`、分支 `codex/implement-sklearn-native-search` 完成到本地可验证阶段。新增 `configs/strategy1/sklearn_native_pvfq_n30_bw_h5_v0.yml`（固定 `pv_fin_quality + 30/5% + biweekly + 5d`、训练窗口 `2019-04-03` 至 `2023-12-31`、36 个 sklearn LogisticRegression 原生候选）、`scripts/strategy1_cloudrun/orchestrate_sklearn_native_search.py`（一次 prepare_matrix、36-task candidate fan-out、valid-only Top 5、Top 5 独立 select/predict/backtest/report/diagnosis、native acceptance 回写和 comparison artifact）、`sql/ml/strategy1/18_qa_sklearn_native_search_outputs.sql`（search/Top-K/native acceptance/test reuse QA）。同步扩展 `train_predict.py` 支持 candidate 级 sklearn 原生参数和 convergence metadata；扩展 `select_register_predict.py` 支持 candidate 强选、valid-only 排名、Top-K metadata、训练面板 alias 和 native acceptance 初始状态；`orchestrate_experiments.py` 已把 `--config` 传给 Cloud Run candidate task，避免非默认候选网格被容器端回退为 5 候选。该条为实现分支 dry-run 记录，真实部署、Top5 完整回测和 acceptance 结论见上方 sklearn native search 收口条。

> 最新补充（2026-06-05）：新增 `docs/prd/PRD_20260605_03_策略1Sklearn模型实验.md`，定义 Cloud Run sklearn backend 在 BQML parity 未通过后的 native baseline 实验方案。方案固定历史最优交易口径 `pv_fin_quality + 30/5% + biweekly + 5d`，使用 task fan-out 并发训练 36 个 sklearn 原生 LogisticRegression 候选，valid-only 选 Top 5 进入完整预测/组合/回测/报告/诊断，并通过独立 native acceptance gate 决定是否建立 `cloud_run_sklearn_native_baseline_v1`；BQML baseline 仅保留为历史 reference / audit。PR #69 review 后已补充：训练窗口钉死为 `2019-04-03` 至 `2023-12-31`；`accepted` 候选必须 `valid_signal_status=stable`，valid 弱但 test 过门只能 `needs_more_evidence`；跨模型族复用 2025 test 必须记录 `test_reuse_wave_no` / owner 批准，超过 3 个波次后必须新增最终 holdout 证据。本次只写 PRD 和记忆/TODO，未实现代码、未部署 Cloud Run Job、未执行 BigQuery。

> 最新补充（2026-06-05）：PR #65 合并后、部署 Composer 和生产 DML 前，OQ-005 windowed refresh 真实 scratch 等价 QA 在 `ashare_qa_windowed_equivalence` 暴露两个发布阻断问题并已在热修复分支 `codex/fix-windowed-refresh-equivalence` 修复。`scripts/qa/run_windowed_refresh_equivalence.py` 复制 `_full` 到 `_window` shadow 表时已增加 `trade_date` 分区过滤，避免 canonical full shadow 表 `require_partition_filter=true` 时失败；`sql/incremental/01_refresh_stock_dwd_dws_window.sql` 将估值特征读取边界从固定交易日窗口改为按每只股票写入窗口首日前的实际 60 条估值观测推导，覆盖 `daily_basic` 缺口下 `ROWS BETWEEN 59 PRECEDING` 需要的观测窗口；QA runner 也新增生产 DWD build-start guard，避免 full/window shadow 被同样截断后假通过。修复后真实 scratch QA 对 9 张目标表 full-vs-window mismatch 均为 0；guard 结果为 `required_build_start_date<=2025-01-23`、`sec_code_count=5407`、`less_than_60_obs=32`；同时通过 QA runner py_compile、QA runner dry-run、窗口 SQL backfill/daily_current BigQuery dry-run。尚未部署 Composer，尚未执行生产 DML。

> 最新补充（2026-06-05）：策略 1 Cloud Run task fan-out 已完成正式全量验收 run `s1_cloudrun_taskfanout_pvfq_n30_bw_h5_20260605_01` / `bt_s1_cloudrun_taskfanout_pvfq_n30_bw_h5_20260605_01`。已将 `strategy1-prepare-matrix-job` 从 `4 CPU / 16Gi` 提升到 `8 CPU / 32Gi`；新训练面板 3,055,781 行（train 1,999,065 / valid 476,346 / test 580,370），`prepare_matrix` execution `strategy1-prepare-matrix-job-q4zd5` 全量 4m10s 成功；candidate fan-out execution `strategy1-train-candidate-fanout-job-fpk9d` 的 5 个 task 全部成功；`strategy1-select-register-predict-job-d5kj2`、`strategy1-backtest-report-job-4shcl` 成功，`10`/`12` 在 report job 内部通过，`16` smoke/evidence 模式与 `17` 通过。正式 `16_qa_cloudrun_runner_outputs.sql` 在 `QA-CR-4` 失败：selected sklearn model `elastic_c_1_l1_0_5` 的 oriented valid RankIC `0.06665`，BQML reference `0.09676`，delta `-0.03011`，`model_quality_status=model_quality_not_equivalent`。回测结果 total_return `46.29%`、excess_return `17.28%` vs `000852.SH`、Sharpe `1.111`、max_drawdown `-13.94%`，报告和诊断均 uploaded 到 GCS。结论：Cloud Run 执行链路和 full-panel prepare 内存问题已验证通过，但 sklearn backend 不能声明正式等价替代 BQML。

> 最新补充（2026-06-05）：PR #65 review follow-up 已在 `codex/windowed-dwd-dws-refresh` 完成。针对 L-1，新增 `scripts/qa/run_windowed_refresh_equivalence.py`，作为发布前/定期 full-vs-window 数值等价 QA：脚本把 canonical full SQL 渲染到 scratch `_full` 表，复制成 `_window` 表，重写 `sql/incremental/01_refresh_stock_dwd_dws_window.sql` 刷 `_window`，再逐列比较受影响窗口内 `_window` 与 `_full`；已通过 `python3 -m py_compile` 和 `--dry-run` 计划输出。针对 L-2，`sql/incremental/01_refresh_stock_dwd_dws_window.sql` 已增加目标表存在性 ASSERT，并把 9 张 DWD/DWS 的窗口 DML 包在 BigQuery transaction 中；日常小窗口失败会整体回滚，大区间 backfill 按文档要求分块执行。`sql/README.md`、TODO 和记忆已同步。验证新增通过：窗口 SQL backfill/daily_current BigQuery dry-run、DAG py_compile、等价 QA runner dry-run、`git diff --check`。

> 最新补充（2026-06-05）：OQ-005 Phase 2.2 股票 DWD/DWS 窗口刷新已在工作树 `/private/tmp/quant-ashare-windowed-refresh`、分支 `codex/windowed-dwd-dws-refresh` 完成到本地可验证阶段。新增 `sql/incremental/01_refresh_stock_dwd_dws_window.sql`，用 `date_from` / `date_to` / `business_date` 参数对 `dwd_stock_eod_price`、`dwd_stock_eod_valuation` 和策略 1 DWS（universe、价格/估值/财务特征、label、feature wide、sample）做窗口化 DELETE/INSERT；标签、宽表和样本按 SSE 交易日历向前回补 20 个交易日，价格特征向前读取 60 个交易日，估值特征按每只股票实际 60 条估值观测推导读取边界；不写 ADS runner/backtest 产物。新增 `sql/qa/10_windowed_stock_refresh_checks.sql` 做窗口内主键、生命周期、退市日、ODS daily 表示性、停牌语义和 trainable sample 轻量 QA。Composer DAG 新增 `daily_current/backfill` 窗口分支：`pipeline_dry_run=true` 不写表，真实写入后刷新 DIM 小表、恢复 P0 metadata、执行股票 DWD/DWS 窗口刷新和窗口 QA；`full_rebuild/full_rebuild_compat` 仍走原全量兼容路径，`qa_only` 仍只读。同步纳入 Composer 静态 project/region/location 热修复和 meta DDL 多列 `ADD COLUMN` 合并，避免 parse-time location 模板和连续 table update 限流问题。验证已通过 `python3 -m py_compile orchestration/composer/dags/ashare_daily_pipeline_v0.py`、`sql/incremental/01` BigQuery dry-run（backfill 与 daily_current 参数各一次）、`sql/qa/10` BigQuery dry-run（backfill 与 daily_current 参数各一次）、`sql/meta/01_create_meta_tables.sql` dry-run、`sql/metadata/01_p0_table_column_descriptions.sql` dry-run。尚未部署 Composer、未执行生产 DML、未写 BigQuery/GCS/ADS 产物。

> 最新补充（2026-06-05）：策略 1 Cloud Run task fan-out P0 已完成低成本真实 smoke。已构建并部署镜像 `asia-east2-docker.pkg.dev/data-aquarium/quant-ashare/strategy1-cloudrun-runner@sha256:8cc8470014cb3b54272d4c7f47afb396d91cf7b97967f0c3ffab947f7432c38a` 到 `strategy1-prepare-matrix-job`、`strategy1-train-candidate-fanout-job`、`strategy1-select-register-predict-job`、`strategy1-backtest-report-job`；当前资源配置为 prepare/select/backtest `4 CPU / 16Gi`，candidate task `1 CPU / 4Gi`，受 `asia-east2` 当前 20 vCPU / 40Gi 配额约束，candidate job 已设置 `parallelism=10`。smoke run `s1_cloudrun_taskfanout_smoke_20260605_03` / `bt_s1_cloudrun_taskfanout_smoke_20260605_03` 使用 2023 train、2024H1 valid、2024H2 test/predict，完成 `cloudrun_prepare_matrix`、5 个 candidate task fan-out、`cloudrun_select_register_predict`、`cloudrun_backtest_report`；状态表 4 步均 succeeded，`10_qa_runner_outputs.sql`、`12_qa_model_diagnosis_outputs.sql`、`16_qa_cloudrun_runner_outputs.sql`、`17_qa_cloudrun_orchestrator_status.sql` 全部通过。产物：matrix URI `gs://ashare-artifacts/models/strategy1/ml_pv_clf_v0/run_id=s1_cloudrun_taskfanout_smoke_20260605_03/matrix_id=s1_cloudrun_taskfanout_smoke_20260605_03__matrix_5294c2780a86`，model URI `gs://ashare-artifacts/models/strategy1/ml_pv_clf_v0/run_id=s1_cloudrun_taskfanout_smoke_20260605_03/model_id=s1_sklearn_s1_cloudrun_taskfanout_smoke_20260605_03__l2_c_10`，report URI `gs://ashare-artifacts/reports/strategy1/ml_pv_clf_v0/run_id=s1_cloudrun_taskfanout_smoke_20260605_03/backtest_id=bt_s1_cloudrun_taskfanout_smoke_20260605_03`。本次修复了 task fan-out smoke 暴露的两个问题：orchestrator 不再把 `--force-replace` 传给 `prepare_matrix`；`10`/`12` QA 改用实验实际 split 窗口和训练面板 `split_tag`，不再写死 2024/2025 DWS split。

> 最新补充（2026-06-05）：`docs/prd/PRD_20260605_02_策略1CloudRun轻量Task并发.md` 的 P0 实现已在分支 `codex/cloudrun-task-fanout` 完成到本地可验证阶段。新增 `prepare_matrix` frozen matrix 构建、`train_candidate_task` 单候选 Cloud Run task、`select_register_predict` reducer、orchestrator `--train-mode task_fanout` / `--candidate-parallelism` 计划展开、BigQuery job labels/audit、`16` / `17` task fan-out QA 和运行手册。PR #64 review follow-up 已修复 QA-TASK-8 空断言：`select_register_predict` 现在通过 `JOBS_BY_PROJECT` 写入真实 `candidate_task_bq_*` 审计计数，`16` 也直接查询 `JOBS_BY_PROJECT` 兜底断言 candidate task 未扫全量训练表，并已让 SQL 版 run_id label 归一化与 Python `bq_label_value()` 完全一致。验证已通过 Python py_compile、`prepare_matrix` / candidate task / reducer dry-run、orchestrator `--tasks` dry-run（`candidate-parallelism=0` 生成单批全并发，显式 `2` 生成 2/2/1 批）、`16_qa_cloudrun_runner_outputs.sql` / `17_qa_cloudrun_orchestrator_status.sql` BigQuery dry-run、实际 Python INFORMATION_SCHEMA audit 查询、`git diff --check` 和本机 `gcloud run jobs execute --help` 参数核验。该条为 PR #64 合并前 dry-run 阶段记录；真实 task fan-out smoke 见 2026-06-05 最新补充。

> 最新补充（2026-06-05）：新增 `docs/prd/PRD_20260605_02_策略1CloudRun轻量Task并发.md`，作为 `docs/prd/PRD_20260604_04_策略1CloudRun训练回测.md` 的训练侧并发补充 PRD。该 PRD 定义 `prepare_matrix` 一次性生成 GCS frozen matrix、`strategy1-train-candidate-fanout-job` 以 Cloud Run Jobs task fan-out 并发训练 35/100 个 candidate / experiment work units、`select_register_predict` 统一汇总选型写 registry/prediction，以及 owner 不限流时单批全并发、显式限流时由 orchestrator 分批执行的并发语义。PR #63 review follow-up 已补 frozen features 为已预处理矩阵、BigQuery job label / audit 机制和 candidate task 不读 predict features 约束。本次只写文档，未实现代码、未部署 Cloud Run Job、未执行 BigQuery。

> 最新补充（2026-06-05）：策略 1 Cloud Run runner 与 orchestrator 状态/锁增强已在 `main` 基础上完成真实 smoke 验收。已部署 Artifact Registry 镜像 `asia-east2-docker.pkg.dev/data-aquarium/quant-ashare/strategy1-cloudrun-runner@sha256:6564434f9f216aec6c86cae3923bc44450c3ca26ead14a248b05ca77087d8ead` 到 `strategy1-train-predict-job` / `strategy1-backtest-report-job`（16Gi/4CPU/`--max-retries=0`），并跑通 `run_id=s1_cloudrun_sklearn_smoke_20260604_02` / `backtest_id=bt_s1_cloudrun_sklearn_smoke_20260604_02`。`16_qa_cloudrun_runner_outputs.sql`（smoke 模式 `p_require_model_quality_parity_passed=FALSE`）和 `17_qa_cloudrun_orchestrator_status.sql` 通过，报告 uploaded 到 GCS。该 smoke 证明 Cloud Run 执行链路可运行，但 sklearn vs BQML parity 未通过，当前只能标记 `model_quality_not_equivalent`，不能声明替代 BQML baseline。

> 最新补充（2026-06-05）：OQ-005 Phase 2.0 BigQuery SQL 兼容路径已在 PR #61 分支 `codex/oq005-scheduler-phase2` 实现并跟进 review comment，待部署 smoke：`ashare_daily_pipeline_v0` 新增 `pipeline_run` / `pipeline_task_status` 状态回写、task callback、`warehouse_mode` 分支、legacy `ashare_enable_full_refresh=true` 到 `full_rebuild_compat` 的记录映射、`skip_ingestion` 跳过采集 smoke、`qa_only` 只读 QA、ADS 契约手工初始化隔离；已移除模块顶层 `Variable.get()`，支持单次 DAG run 用 `pipeline_dry_run` / `dry_run` 覆盖采集 dry/write 分支，`09_ods_daily_partition_readiness.sql` 按 dry-run 运行期参数推导分区门禁，QA TaskGroup 已抽出 helper；`sql/meta/01_create_meta_tables.sql` 扩展 pipeline 状态表和 task/job/execution URL 字段；OQ-006 单位映射脚本从 `sql/meta/01_ods_field_unit_map.sql` 整理为 `sql/meta/04_ods_field_unit_map.sql` 并同步引用。验证已通过 Python py_compile、meta DDL BigQuery dry-run、单位映射 SQL dry-run、`09` readiness dry-run（dry/write 参数各一次）和 `git diff --check`。尚未部署 Composer、未执行生产 BigQuery 转换、未关闭 OQ-005。

项目处于**P0 DIM/DWD 已物化并通过 smoke QA，OQ-004/OQ-006/OQ-003 已实现并关闭，策略 1 DWS/ADS 与历史 BigQuery ML runner 已端到端跑通，OQ-010 交易成本 profile、中文报告与归因分析、GCS uploaded 模式、模型质量诊断、valid/test live-available 预测池口径、score orientation 校准均已实现并通过相关 QA；`docs/prd/PRD_20260603_02_策略1首轮质量迭代实验.md` 已作为 OQ-010 第一轮实验方案由 PR #35 合并进入 `main`，且 owner 已确认阶段 A/B/C 不做 `4 * 3 * 3` 全量笛卡尔积；OQ-010 首轮实验 runner 参数化与同阶段并发调度已由 PR #37/#45/#48 合并进入 `main`，3*2*2*2 全因子 24 组合已跑完并全部通过 `12_qa_model_diagnosis_outputs`；历史最优 BQML 组合 `pv_fin_quality + 30/5% + biweekly + 5d` 已完成重训 run `s1_bqml_baseline_pvfq_n30_bw_h5_v20260604_01` / backtest `bt_s1_bqml_baseline_pvfq_n30_bw_h5_v20260604_01`，01-12 全部成功，`10`/`12` QA 通过，中文报告和模型诊断均 uploaded 到 GCS；2026-06-04 已新增策略 1 Ledger v1 交易执行语义 PRD、月度滚动重训 PRD、策略 1 因子贡献度分析 PRD 和策略 1 Cloud Run 训练回测执行器 PRD；策略 1 因子贡献度分析 P0 已实现，新增独立脚本、`14_qa_factor_attribution_outputs.sql`、主报告摘要接入和 README 运行说明，并已对正式 baseline local-only 生成 `factor_attribution/` artifact，覆盖 selected model 55 个非截距特征、13 个因子组，`14` QA 全部通过；Ledger v1 P0 交易执行语义已进入 `main`，Ledger v1 P1 fixed-model extended fresh run 与 P2 state resume 一致性验收已完成，`15_qa_ledger_resume_consistency.sql` 全部通过；策略 1 Cloud Run 训练回测执行器、orchestrator 状态/锁增强、轻量 task fan-out 和 sklearn native search 首轮均已完成真实运行，Cloud Run 链路可运行但尚未找到 accepted Python baseline；2026-06-05 owner 已决定后续不再使用 BQML / `sql/ml/strategy1` SQL runner 作为策略执行层默认 / fallback / 新增开发路线，历史 BQML 结果仅作 reference / audit**。已产出 DWD/DIM 建模方案、DWS/ADS 表设计方案、策略方案、策略 1 PRD、runner 设计与实现 PRD、OQ-003/OQ-004/OQ-006/OQ-010 子项 PRD、策略 1 中文报告与归因分析 PRD、策略 1 报告 GCS 上传运行手册、策略 1 模型质量诊断 PRD、策略 1 预测池口径修正 PRD、策略 1 分数方向校准 PRD、策略 1 首轮质量迭代实验 PRD、策略 1 实验并发调度与隔离 PRD、策略 1 Ledger v1 交易执行语义 PRD、策略 1 月度滚动重训 PRD、策略 1 因子贡献度分析 PRD、策略 1 Cloud Run 训练回测执行器 PRD、OQ-005 GCP 数据流水线 PRD、OQ-005 剩余 ODS→ADS 生产调度链路 PRD、Phase 0 采集实现、Phase 1 endpoint worker、Cloud Run Jobs 与 `ashare-ingest-current-scope` 生产入口、Direct VPC egress + Cloud NAT 固定出口、GCS 小范围写入 smoke、`2026-05-20` 至 `2026-06-05` 当前范围 ODS 生产采集、ODS 外部表可读性 QA、Composer 3 环境与 `ashare_daily_pipeline_v0` DAG 部署，以及 ODS 外部表 Parquet schema 修复 PRD。下一步建议先寻找 accepted Cloud Run Python baseline，并在实现前把月度滚动重训 PRD 改造为 Cloud Run Python / backend-neutral 口径；OQ-005 scheduled 非交易日 skip gate 已在本分支实现，合并后需部署 smoke；后续仍需按 `docs/prd/PRD_20260605_01_OQ005剩余调度链路.md` 接入/验证 Dataform 生产链路、增量影响窗口、补跑/resume 自动化和完整 ODS→ADS 生产闭环；也可补 `dws_market_state_daily` 或按 schema 修复 PRD 保留防复发流程。

## 已完成（Completed）

- ODS 探查：`ashare_ods` 当前 57 张外部表的字段与分区语义已摸清（三类分区：A 行情增量 / B 财务报告期 / C 维度快照；`index_member_all`/`ci_index_member` 为最新分区全量历史区间快照）。
- DWD/DIM 建模方案文档 `docs/数据仓库建模方案-DWD-DIM.md` 定稿：分层、57 表映射、五条铁律、DIM/DWD 逐表设计、DWS 衔接、工程建议、路线图、风险项。
- 命名规范敲定：`sec_code` 主键、`trade_date/cal_date`、`ann_date_eff`、单位元/股、复权 `_hfq/_qfq`、血缘 `source_system/ingested_at`。
- 物理设计敲定：按月分区 + `sec_code` 聚簇、行情表 `require_partition_filter=TRUE`。
- 2019 前数据范围敲定：财务/事件 `partition_date >= '20170101'`；行情 DWD/DWS 写 `trade_date >= 2019-01-01`、构建时按最大窗口读取 2018 lookback buffer；维度/日历取最新快照或全量历史事件。
- 新增 ODS/GCS 数据审查目录：`data_audit/ODS_GCS_DATA_AUDIT_PROMPT.md` 与 `data_audit/reports/`。提示词限定本次只审查 2019-01-01 及之后的数据，只读不补数据；审查 Agent 需自行编写审查脚本，并在请求、并发、限速、schema 检测或报告生成代码有问题时自行修正审查代码后继续；提示词已补 Tushare 官方文档链接、API 返回行数命中单次上限时的截断风险检查，以及按 endpoint/主题拆分脚本的代码组织规则。
- 新增 OQ-005 GCP 数据流水线 PRD：`docs/prd/PRD_20260603_03_GCP数据流水线方案.md`。长期目标架构为 Cloud Run Jobs 采集 Tushare 兼容 API 到 GCS Parquet，Dataform / BigQuery Studio pipeline 做 ODS→DIM/DWD/DWS/ADS，Cloud Composer 编排全流程；首批每日生产采集只覆盖当前实际消费的 14 张 ODS，当前未消费 endpoint 进入后续接入池；PRD 已按 owner 反馈收敛为只描述最终实现方式的陈述性方案，并已跟进 PR #39 review 两条低优先级建议：财务 empty-return 口径、Phase 1 Cloud Scheduler / Composer 触发入口。
- 新增并合并 OQ-005 剩余 ODS→ADS 生产调度链路 PRD：`docs/prd/PRD_20260605_01_OQ005剩余调度链路.md`。该 PRD 聚焦已完成生产采集之后的剩余链路，定义 ODS gate、BigQuery SQL 兼容路径、Dataform definitions、ADS 契约隔离、刷新窗口、metadata、QA、pipeline 状态、告警、补跑、策略 runner/report 可选分支和 OQ-005 关闭标准。
- OQ-005 Phase 2.0 BigQuery SQL 兼容路径已在 PR #61 分支 `codex/oq005-scheduler-phase2` 实现并跟进 review comment，待部署 smoke：`ashare_daily_pipeline_v0` 新增 `pipeline_run` / `pipeline_task_status` 状态回写、task callback、`warehouse_mode` 分支、legacy `ashare_enable_full_refresh=true` 到 `full_rebuild_compat` 的记录映射、`skip_ingestion` 跳过采集 smoke、`qa_only` 只读 QA、ADS 契约手工初始化隔离；已移除模块顶层 `Variable.get()`，支持单次 DAG run 用 `pipeline_dry_run` / `dry_run` 覆盖采集 dry/write 分支，`09_ods_daily_partition_readiness.sql` 按 dry-run 运行期参数推导分区门禁，QA TaskGroup 已抽出 helper；`sql/meta/01_create_meta_tables.sql` 扩展 pipeline 状态表和 task/job/execution URL 字段；OQ-006 单位映射脚本从 `sql/meta/01_ods_field_unit_map.sql` 整理为 `sql/meta/04_ods_field_unit_map.sql` 并同步引用。验证已通过 `python3 -m py_compile orchestration/composer/dags/ashare_daily_pipeline_v0.py`、`bq query --dry_run`（meta DDL、单位映射、`09` readiness dry/write 参数）和 `git diff --check`。尚未部署 Composer、未执行生产 BigQuery 转换、未关闭 OQ-005。
- OQ-005 Phase 0 实现分支已整合 review 修复：PR #42 分支新增首批 14 张当前消费 ODS 的采集 manifest、schema contract、meta 表 DDL、通用 API client / Parquet schema / manifest / logging 模块和每日采集脚本 stub；PR #44 已合入 #42 分支，PR #46 的 GCS 路径、API 行数上限、Parquet cast、日志脱敏修复已手动合入，并追加修复 `partition_endpoint` 路径契约与参数化日志脱敏泄露问题。后续 Phase 1+ 已实现 Cloud Run Jobs 与 Composer DAG，OQ-005 仍未关闭，仍需 Dataform / BigQuery SQL 生产转换、告警和补跑验收。
- OQ-005 Phase 1/1.5/1.6/1.7 endpoint worker、Cloud Run Jobs、固定出口与每日 Composer 生产采集已完成：新增 `scripts/ingestion/run_ingestion_job.py`、`scripts/ingestion/common/endpoint_runner.py`、`scripts/ingestion/common/status_writer.py`、`scripts/ingestion/requirements.txt`、`orchestration/cloud_run_jobs/` 和 `orchestration/composer/`；API client 支持 limit/offset 分页并对重复页 / 最大页数 fail-closed；worker 支持 4 个 endpoint group、`current_scope` 顺序执行入口、ODS 真实 index endpoint key、财务 `_vip` API 路径、血缘列、schema cast、GCS staging/publish、财务报告期 merge、live BigQuery meta 状态写入和 `--skip-gcs-write` 只读 smoke。Secret Manager `tushare-token` 已新增版本，Artifact Registry repo `ashare` 和 service account `sa-ashare-ingestion` 已创建并授予 Secret/GCS/BigQuery/Cloud Run/Composer 所需权限；镜像已推送到 `asia-east2-docker.pkg.dev/data-aquarium/ashare/ingestion@sha256:351dfd996b6ec066135d68c40f84eb1c2a52e43ea8e28208ba1711be90a7652d`；5 个 Cloud Run Jobs 已部署，模板默认 `--dry-run` 且通过 Direct VPC egress + Cloud NAT 固定出口。已用 `index_daily_000852_SH` / `partition_date=20260603` 完成 1 行 GCS 写入 smoke，并通过 BigQuery ODS 外部表读取验证。新增 `sql/qa/08_ods_external_readability_checks.sql` 和 `sql/qa/09_ods_daily_partition_readiness.sql`，14 张当前范围 ODS 外部表可读性检查通过，每日 readiness 已按生产日期逐日验证；2026-06-04 已用 BigQuery `INFORMATION_SCHEMA.TABLE_OPTIONS` 复核当前 14 张 ODS 与 10 张 schema repair 表 source URI 均符合 `api=<api>/endpoint=<partition_endpoint>/partition_date=*` 口径。Cloud Composer 3 环境 `ashare-composer` 已创建并部署 DAG `ashare_daily_pipeline_v0`；每日默认只检查业务日分区或近期小窗口，`sql/qa/06_ods_parquet_schema_checks.sql`、DIM/DWD/DWS/metadata/QA 全量链路已挂到 `ashare_enable_full_refresh=true` 显式分支。Composer default Celery queue smoke `manual_oq005_scheduler_smoke_default_queue_20260604_01` 成功；`2026-05-20` 至 `2026-06-05` SSE 开市日生产 GCS 写入 / 补写已完成并通过 `09` readiness；`manual_pr80_daily_current_20260605_20260606_01` 已按生产路径写入 `2026-06-05` 并成功完成 readiness、窗口刷新和窗口 QA。当前 Airflow 变量为 `ashare_pipeline_dry_run=false`、`ashare_enable_full_refresh=false`；scheduled 非交易日 skip gate 已在本分支实现但尚未部署，完整 Dataform 生产链路和补跑/resume 自动化仍待完成。
- 新增 ODS 外部表 Parquet schema 修复 PRD：`docs/prd/PRD_20260603_04_ODS外部表ParquetSchema修复.md`。PRD 覆盖 10 张 2019+ schema mismatch 外部表，定义从 GCS 原 Parquet 按 schema contract 做 schema-preserving rewrite、staging/backup/发布、QA 门禁和 ingestion 显式 cast 防复发；当前 P0 源表 `ods_tushare_stk_limit` 优先修复。
- 跟进 PR #40 review comment `issuecomment-4611909699`：PRD 已补 backup write-once / `ok` 文件跳过、临时 external table 显式 contract schema、INT→FLOAT64 `<2^53` 精度复核，并同步 ODS schema 修复决策与约束。
- 表/字段注释规范敲定：内联 DDL / 后置 ALTER / 继承 ODS 描述三法。
- 仓库初始化：`git init` + `.gitignore` + 首个 commit（`main`）。
- 建立 `.agent/` Agent 工作记忆体系 + 根 `AGENTS.md` 读写协议；推送 GitHub（gthbj/quant-ashare）；加模型署名协议。
- 按实测 review 整改建模方案（9 采纳 / 2 调整）：财务版本事实表、价格表「交易日历×在市」骨架（含停牌日）、表级可见日规则（`fina_indicator` 用 `ann_date`）、ODS 元数据矩阵、lookback buffer、方向性可交易、`visible_trade_date`、表数订正 54；写 `docs/reviews/…-review-response.md`。
- 修正早先“全历史写入”误读：`docs/reviews/数据仓库建模方案-DWD-DIM-review-2019前数据范围修正.md` 已改为 2019 前数据范围修正说明；主方案 §4.6 已新增范围表。
- 主方案文首和 TL;DR 已显式说明：当前建模范围是 2019-01-01 之后的 A 股日线 DWD/DWS；2019 年以前数据仅作为 PIT / lookback / 维度历史支撑。
- P0 建表 SQL 已落地到 `sql/`：`00_create_datasets.sql`、4 张 DIM、5 张 DWD、3 个 QA 脚本。脚本采用 `CREATE OR REPLACE TABLE`、月分区、`sec_code` 聚簇、范围参数 `dwd_start_date/fin_start_period/lookback_start_date`。
- SQL 校验完成（物化前历史阶段）：dataset/DIM 脚本 dry-run 通过；`dwd_stock_eod_valuation`、`dwd_index_eod` dry-run 通过；`dwd_stock_eod_price`、`dwd_fin_indicator` 曾因目标 DIM 未物化，使用临时空维表替换后 dry-run 通过。后续 P0 已实际物化并通过 QA。
- 采纳并修复 P0 SQL 评审发现：README 命令加 `--location=asia-east2`；`suspend_d` 只以 `suspend_type='S'` 标记停牌，复牌 `R` 不再误判停牌；`dim_stock` 加 `sec_code` 去重与派生退市 30 日宽限；`dwd_fin_indicator` 加版本键去重兜底；新增 `dwd_fin_indicator_latest` 与 `sql/qa/01_p0_smoke_checks.sql`。修复后相关脚本 dry-run 通过。
- P0 物化完成并通过 smoke QA：`dim_trade_calendar` 13,162 行；`dim_stock` 5,853 行；`dim_stock_name_hist` 3,776 行；`dim_index` 7 行；`dwd_stock_eod_price` 8,506,688 行（2019-01-02 至 2026-06-02 当前范围）；`dwd_stock_eod_valuation` 8,452,073 行；`dwd_index_eod` 11,922 行；`dwd_fin_indicator` 332,960 行；`dwd_fin_indicator_latest` 198,030 行。2026-06-01 OQ-004 实现后新增 `dim_index` 并重建 `dwd_index_eod`，OQ-004 专项 QA 通过；2026-06-02 PR #9 依赖链重建后，P0 smoke QA 通过。
- 上游修复 `ods_tushare_index_dailybasic` Parquet 类型后，`dwd_index_eod` 已恢复估值/股本字段并重建：2019+ 共 11,922 行，其中 8,899 行有 `pe/pe_ttm/pb/total_mv_cny/float_mv_cny/total_share/float_share` 等 dailybasic 字段；STAR50(`000688.SH`) 和 CSI1000(`000852.SH`) 因 ODS 无 dailybasic endpoint 仍为空。
- `dwd_index_eod` 脚本已按 owner 确认调整为 canonical `sec_code` + `source_sec_code` 血缘口径，并已重建 BigQuery 实表；重新执行 `sql/metadata/01_p0_table_column_descriptions.sql` 和 `sql/qa/01_p0_smoke_checks.sql` 通过。验证：沪深300 `sec_code='000300.SH'`、`source_sec_code='399300.SZ'`，STAR50/CSI1000 估值字段仍因 ODS 无 dailybasic endpoint 为空。
- 修复 P0 二轮评审发现：`dwd_stock_eod_price` 将 `is_suspended` 限定为全天停牌/无成交，新增 `has_intraday_halt` 与 `has_open_halt`，开盘临停影响 `can_buy_open/can_sell_open/is_tradable`；`dwd_fin_indicator_latest` 改为 `update_flag DESC, ann_date_eff DESC, ingested_at DESC, source_partition_date DESC` 排序。相关表已重建，QA 通过；验证指标：有成交但 `is_suspended=TRUE` 为 0，latest 排序差异为 0。
- P0 表/字段说明补齐：新增并执行 `sql/metadata/01_p0_table_column_descriptions.sql`，9 张 P0 DIM/DWD 表的 table description 和所有 schema field description 均已补齐；2026-06-02 复核 missing description = 0。
- OQ-007 已复核并关闭：`stock_basic_delisted.delist_date` 在 ODS 中已统一为 `STRING`，最新 delisted 分区 326 行全部可解析；`dim_stock` SQL 改为优先使用 ODS 退市日，daily 最后交易日加一天仅作缺值兜底，并补 P0 QA 断言（含退市股生命周期合法性）。2026-06-02 已按依赖重建 BigQuery 实表。
- PR #9 合并后的 OQ-007 依赖链已在 BigQuery 重建并通过 QA：`dim_stock` 5,853 行，其中 326 个退市股使用 `stock_basic_delist_date`；`dwd_stock_eod_price` 8,506,688 行；策略 1 DWS 六表已重建，`dws_stock_sample_daily` 8,506,688 行，`sample_trainable_default` 3,274,084 行；ADS 11 张契约表已重建（执行前均为空表）。已执行 `sql/metadata/01_p0_table_column_descriptions.sql`，P0 DIM/DWD 字段说明缺失数为 0；`sql/qa/01_p0_smoke_checks.sql` 与 `sql/qa/02_strategy1_dws_ads_checks.sql` 全部通过。
- DWS/ADS 表设计文档已完成：`docs/数据仓库建模方案-DWS-ADS.md`。定义 P0 DWS（universe、价格/估值/财务特征、市场状态、标签、样本）与 ADS（训练面板、模型预测、候选池、组合、订单计划、回测/监控）表体系。
- 策略方案文档已完成：`docs/A股中低频小资金机器学习策略方案.md`。定义首个 `ml_ranker_v0` 机器学习横截面排序策略，以及小盘质量反转、趋势延续、财务事件、资金筹码、行业轮动等后续策略族。
- 策略 1 PRD 已完成并通过 review 修订：`docs/prd/PRD_20260601_01_策略1价格量价基础分类模型.md`。首版落地名称为 `ml_pv_clf_v0`，范围限定价格量价 + 估值特征、open-to-close 标签和通用 DWS/ADS 表契约。
- 策略 1 DWS SQL 已落地并物化：`sql/dws/01_dws_stock_universe_daily.sql`、`02_dws_stock_feature_price_daily.sql`、`03_dws_stock_feature_valuation_daily.sql`、`04_dws_stock_label_daily.sql`、`05_dws_stock_feature_daily_v0.sql`、`06_dws_stock_sample_daily.sql`。2026-06-02 PR #9 依赖链重建后行数：universe 8,506,688 行、价格特征 8,506,688 行、估值特征 8,452,073 行、标签 8,506,688 行、特征宽表 8,506,688 行、样本表 8,506,688 行（默认可训练 3,274,084 行）。
- 策略 1/P0 ADS 表契约已落地并物化：`sql/ads/01_ads_strategy1_tables.sql` 创建训练面板、模型注册、预测、候选池、组合目标、订单计划、回测成交/持仓/NAV/绩效汇总、信号监控 11 张表。
- 策略 1 DWS/ADS QA 已落地并通过：`sql/qa/02_strategy1_dws_ads_checks.sql` 校验 DWS/ADS 表存在、DWS 主键唯一、universe 含退市股存活区间、不暴露 qfq 字段、2019 初 60 日历史不完整显式标记、默认可训练样本具备 universe-ranked `rank_pct_5d`、`fwd_ret_5d = close_hfq[t+5] / open_hfq[t+1] - 1`。
- PR #4 comment 跟进修复完成：`dws_stock_label_daily` 去掉 `ce/c1` 冗余日历 JOIN；补充 `label_valid_*d` 与 `exit_reachable_*d` 字段说明，明确 `label_valid` 检查入场可交易与标签价格可用，退出可卖性交给 `exit_reachable` 和回测撮合；`sql/qa/02_strategy1_dws_ads_checks.sql` 增加默认可训练样本最早日期断言（当前 `2019-04-03`，2019Q1 无默认可训练样本）；DWD-DIM/DWS-ADS 文档同步相关口径与 `volume_share`/`amount_cny` 实表字段名。已重建 label/sample 并重跑策略 1 QA 通过。
- PR #4 已合并到 `main`；已删除合并后不再使用的 `codex/implement-strategy1-prd` 本地分支和远端分支，并在 `KNOWN_CONSTRAINTS.md` 增加 PR 合并后清理无用 `codex/*` 分支的工程规则。
- 策略 1 BigQuery ML runner 设计已完成：`docs/策略1-ml_pv_clf_v0-runner设计.md`。设计限定 BigQuery SQL + BigQuery ML 执行路径，覆盖训练面板、`CREATE MODEL`、BQML `L1_REG/L2_REG` 手动候选网格、valid RankIC 选型、`ML.PREDICT`、候选池、组合、订单、回测、监控、GCS 报告产物、本地报告镜像、幂等、QA 和验收；`board` 保留为监控字段、不进 v0 主模型训练列。
- 策略 1 BigQuery ML runner 与回测闭环实现 PRD 已完成：`docs/prd/PRD_20260601_02_策略1BQML回测闭环.md`。PRD 定义 `sql/ml/strategy1/01-10` 脚本交付物、输入输出、运行参数、功能需求、QA、报告产物和验收标准；报告渲染脚本 `scripts/strategy1/render_report.py` 是必需交付物。（PRD 原 v0 卖出顺延 `next_sellable_trade_date` 口径已在 PR #12 实跑后废弃、升级为 v1 账户级 ledger，PRD/设计已同步。）
- 策略 1 BigQuery ML runner 脚本已合并入 `main`（PR #7）：`sql/ml/strategy1/01_build_training_panel.sql` 至 `10_qa_runner_outputs.sql`、`sql/ml/strategy1/README.md`、`scripts/strategy1/render_report.py` 与 `scripts/strategy1/requirements.txt`。
- 策略 1 runner 已在 BigQuery 端到端实跑并通过全部 QA（PR #12）：run_id `s1_bqml_20260601_01` / backtest `bt_s1_bqml_20260601_01`。01 训练面板 3,051,752 行；02 训练 5 个 BQML LOGISTIC_REG 候选；03 选 `l1_0_l2_1e_3`（valid rank_ic≈-0.10、AUC≈0.50，反向预测基线）；04 预测 1,052,687 行；05 候选 224,648 行；06 组合 520 行；07 订单 565 行；08 ledger 回测（NAV 485 天；不可交易腿记 `*_SKIPPED_UNTRADABLE` 意图行并 carry）；09 summary+monitor（buy/sell skip 指标从成交表 1:1 汇总）；render 本地报告（local-only `--skip-gcs-upload`：写 `local_report_path` + `report_upload_status=skipped`，`report_uri=NULL`）；`10_qa_runner_outputs.sql` 16 个断言全过（含模式感知报告断言）。实跑修复：03/07/08/09 BigQuery 相关子查询去相关 + 强制分区过滤、10 `cal_date`、render ADC 回退 + PARSE_JSON wide number；08 因 v0 set-based 守卫失败（现金 -34 万、gross 2803 倍）按 DECISION-20260601-07 重写为账户级有状态 ledger。PR #12 review 跟进：08 写 skip 意图行 + 09 改 ledger 口径成交诊断、render 仅在真实上传时写 `report_uri`、`ads_backtest_trade_daily.fill_status` 描述补 skip 枚举、文档/记忆收敛到 v1 ledger。NAV 收于≈0.02 属 v0 模型质量（OQ-010），非管线缺陷。
- 策略 1 中文报告与归因分析已实现并合并（PR #20）：`render_report.py` v2 生成中文 Markdown/HTML、交易/持仓/NAV/benchmark CSV 附件、图表、亏损证据包和 AI 诊断；`09_build_metrics_and_report_inputs.sql` 和 `10_qa_runner_outputs.sql` 已补报告字段与模式感知 QA；`sql/ml/strategy1/README.md` 已同步中证1000评估主基准、沪深300展示基准、成本拆分和报告产物说明。
- 策略 1 报告 GCS uploaded 模式已跑通：`docs/策略1报告GCS上传运行手册.md` 覆盖 `ashare-artifacts` bucket、ADC、IAM、local-only smoke、uploaded 模式运行、GCS/ADS/QA 验收和安全约束；2026-06-02 已创建 `gs://ashare-artifacts`（`ASIA-EAST2`）、配置本机 ADC（quota project=`data-aquarium`）、去掉 `--skip-gcs-upload` 重跑 `render_report.py`，并确认 GCS 文件、ADS `report_uri` 和 `sql/ml/strategy1/10_qa_runner_outputs.sql` 全部通过。
- 策略 1 模型质量诊断 PRD 已新增：`docs/prd/PRD_20260602_04_策略1模型质量诊断.md`。PRD 定义先诊断 signal / label / sample-universe / candidate / portfolio / cost / style，并产出 `model_diagnosis/` artifact、`metrics_json` 诊断状态和 QA；不在该 PRD 中直接改模型或调参。PR #24 comment feedback 已补入正文：RankIC 明确为 Spearman、bucket 明确为日截面 quantile、`sample_filter_risk` 阈值明确只针对不可解释排除率。
- 策略 1 分数方向校准 PRD 已新增：`docs/prd/PRD_20260603_01_策略1分数方向校准.md`。PRD 基于 `s1_bqml_livepool_20260602_01` 与 reverse-score shadow run 的事实对照，定义 `raw_score` 为 BQML 正类概率、`score` 为最终排序分、`score_orientation` 为 `identity` / `reverse_probability`，要求 03 选型使用 valid 期方向校准并持久化，04 预测阶段应用方向，QA/report/diagnosis 展示并校验方向。
- OQ-006 接口单位换算已实现并关闭（PR #16 已合并）：新增 `ashare_meta.ods_field_unit_map` 并填充 P0 + PR #13 首批单位映射；`dwd_index_eod.volume/amount` 已按 `vol*100` / `amount*1000` 修复换算并迁移为 `volume_share/amount_cny`；新增 `sql/qa/05_oq006_unit_checks.sql` 并纳入 DWD PR 必跑 QA；单位准入硬规则已写入 DWD-DIM §3.3-H 和 `KNOWN_CONSTRAINTS.md`。PR #16 review 后已修复 `source_name_passthrough` 例外、QA-UNIT-6e ODS 直接比对 + 非空 join 守卫、QA-UNIT-9 NULL-safe 约束；`05_oq006_unit_checks.sql` 已实际跑通全部 ASSERT。
- OQ-003 财务 `report_type` / 报表口径维度已采纳、关闭并实现：`docs/prd/PRD_20260601_03_财务报表口径维度.md`。P0 默认消费合并报表 `report_type='1'`，DWD 对三大财务表保留 `report_type`/`report_caliber`/`is_default_report_caliber`，DWS 财务特征默认只过滤默认口径并补 NULL-safe QA；实现已由 PR #13 合并入 `main`。
- OQ-003 §9 交付物已实现并在 BigQuery 物化（PR #13 / DECISION-20260602-03）：新增 `sql/dwd/06_dwd_fin_income.sql`+`07_*_latest`、`08_dwd_fin_balancesheet.sql`+`09_*_latest`、`10_dwd_fin_cashflow.sql`+`11_*_latest`、`sql/dws/07_dws_stock_feature_fin_daily.sql`、`sql/qa/04_finance_caliber_checks.sql`，并同步 DWD-DIM/DWS-ADS/README/PRD 文档。物化行数：`dwd_fin_income` 237,112 / `_latest` 173,068；`dwd_fin_balancesheet` 215,134；`dwd_fin_cashflow` 256,667（报告期 2017Q1–2026Q1，全 `report_caliber='consolidated'`，`visible_trade_date` 非空且不早于 `ann_date_eff`）；`dws_stock_feature_fin_daily` 8,506,688 行=universe、主键唯一、零 PIT 泄露、来源覆盖 indicator/income 100% / balancesheet 97.9% / cashflow 99.9%。`sql/qa/04`（25 ASSERT）及既有 `01/02/03` QA 全过；抽样 `600519.SH` FY2023 as-of 与真实披露一致。实测 ODS 三表仅含 `report_type='1'`。本 PR 已 rebase 到含 OQ-006 的 `main`，并按 OQ-006 单位契约在 `ashare_meta.ods_field_unit_map` 补全财务三表标准字段映射（金额=元、`multiplier=1`、`verification_status=verified`），跑通 `sql/qa/05_oq006_unit_checks.sql`（QA-UNIT-2 财务字段全部命中）。
- OQ-004 基准指数代码可用性已实现并关闭：`docs/prd/PRD_20260601_04_OQ004基准指数口径.md` 已更新为已实现；新增 `sql/dim/04_dim_index.sql`，更新 `sql/dwd/04_dwd_index_eod.sql` 从 `dim_index` 读取映射，新增 `sql/qa/03_oq004_index_checks.sql`，并在 `sql/ml/strategy1/08_run_backtest.sql` 增加 benchmark 可用性与窗口覆盖前置校验。BigQuery 已物化 `dim_index`、重建 `dwd_index_eod`、执行 metadata，OQ-004 QA 通过。
- PR #11 review feedback 已跟进：`sql/dim/04_dim_index.sql` 注明中证2000/国证2000等未见 ODS `index_daily` 端点时不 seed；`dim_index` 字段描述改由 `sql/metadata/01_p0_table_column_descriptions.sql` 统一维护；`sql/qa/03_oq004_index_checks.sql` 注明示例 benchmark/window 与 SSE 日历假设；`sql/ml/strategy1/08_run_backtest.sql` 注明 runner benchmark 窗口校验使用 SSE 作为全市场开市日历。复核结果：`dim_index` 与 `dwd_index_eod` 字段描述缺失数均为 0，OQ-004 QA 通过。
- PR #11 已合并到 `main`，本地 `main` 已 fast-forward 到远端合并结果；远端和本地 `codex/implement-oq004-index` 分支均已删除。
- 策略 1 首个基线股票池板块纳入口径已确认：仅沪深主板（`SSE_MAIN` / `SZSE_MAIN`），不含北交所、创业板、科创板。现有 `sql/dws/01_dws_stock_universe_daily.sql` 默认 `board_allowlist` 已符合该口径；OQ-010 成本子项已新增 PRD 并在 runner SQL 中实现，默认 profile 为佣金万一免五、卖出印花税 5 bps、买/卖滑点各 5 bps；OQ-010 剩余调仓、持股数/权重上限和模型质量迭代待确认。
- 评审协议已按 owner 最新要求更新：GitHub PR review 默认写 PR comment，一条写不下拆多条；只有 owner 明确要求或无 PR comment 承载面时才写 `docs/reviews/` 评审文档。`DECISION-20260531-13` 已被 `DECISION-20260601-03` supersede。
- ODS 已补采 `index_member_all` 和 `ci_index_member`；主方案、DWS/ADS 文档和策略文档已更新为可落地申万/中信行业时点映射，OQ-001 已关闭。
- 工作记忆瘦身完成：旧交接归档到 `.agent/memory/archive/AGENT_HANDOFF_2026-05.md`；已关闭问题迁移到 `.agent/memory/archive/CLOSED_QUESTIONS.md`；`OPEN_QUESTIONS.md` 仅保留 open 项；`UPDATE_PROTOCOL.md` 增加只读任务免追加交接和归档规则。
- `TODO.md` 已整理为下一步可执行清单：已移除 PR #13 / OQ-003 待合并旧项和 GCS uploaded 已完成项，当前保留 `dws_market_state_daily`、OQ-010 策略质量、P1 扩展与工程/调度待办；开放问题继续以 `OPEN_QUESTIONS.md` 为唯一来源。
- OQ-010 首轮实验 runner 实现已由 PR #37 合并进入 `main`：新增 `configs/strategy1/oq010_experiments_v0.json` 和 `scripts/strategy1/compare_oq010_experiments.py`；`sql/ml/strategy1/01-06/09-12` 支持实验身份、调仓频率、目标持股/权重、`p_label_horizon`、`feature_set_id` 和 portfolio-only `p_prediction_run_id`；`diagnose_model_quality.py`、`11`、`12` 改为 horizon-aware 且可用预测源 run 诊断组合层实验；`sql/dws/06_dws_stock_sample_daily.sql` 暴露 10d/20d 标签字段。验证：JSON 校验、Python `py_compile`、`git diff --check`、BigQuery dry-run（`sql/dws/06`、`sql/ml/strategy1/01-06/09-12`）均通过。尚未执行端到端实验。
- OQ-010 策略 1 实验并发调度与隔离 Phase 1 已实现（PR #45）：新增 `sql/meta/02_strategy1_experiment_run_status.sql`（状态表 DDL，`CREATE TABLE IF NOT EXISTS` 保留 audit/resume 历史）、`scripts/strategy1/run_oq010_experiments.py`（调度器，支持 --dry-run 展开完整计划、SQL `DECLARE p_* DEFAULT` 参数强校验、GCS `ifGenerationMatch=0` 原子锁、generation-guarded stale reclaim/release、lease/heartbeat/status-table expiry alignment、锁 finally 释放、heartbeat 停止后写 terminal status、全部 PRD 定义 CLI 参数）、`sql/qa/07_strategy1_experiment_concurrency_checks.sql`（12 个 QA-CONC 断言）、`docs/策略1实验并发调度器运行手册.md`。验证：Python `py_compile`、`git diff --check`、stage_a dry-run、单实验 dry-run、全 manifest dry-run、直接参数注入断言通过。尚未在 BigQuery 端到端实跑并发实验。
- OQ-010 Stage C runner/QA 修复已由 PR #47 合并后完成重跑；随后补齐 3*2*2*2 全因子网格中缺失的 19 个组合，24 个组合最终均通过 `12_qa_model_diagnosis_outputs`。全网格当前最优组合为 `pv_fin_quality + 30/5% + biweekly + 5d`（benchmark=`000852.SH`，total_return=41.10%、excess_return=12.09%、Sharpe=1.043、max_drawdown=-14.48%）。
- OQ-010 并发调度后续修复已由 PR #48 合并进入 `main`：同 stage dependency batching 现在尊重 `depends_on_experiment_id`，诊断完成状态与 GCS 上传状态已拆分。
- OQ-010 正式基线 run 已完成：`run_id=s1_bqml_baseline_pvfq_n30_bw_h5_v20260604_01` / `backtest_id=bt_s1_bqml_baseline_pvfq_n30_bw_h5_v20260604_01`，参数为 `pv_fin_quality + 30/5% + biweekly + 5d`，回测区间 `2024-01-02` 至 `2025-12-31`，benchmark=`000852.SH`，total_return=41.10%、excess_return=12.09%、Sharpe=1.043、max_drawdown=-14.48%、turnover_annual=19.38、cost_bps=17.0；report_uri 和 model_diagnosis_uri 均位于 `gs://ashare-artifacts/reports/strategy1/ml_pv_clf_v0/run_id=s1_bqml_baseline_pvfq_n30_bw_h5_v20260604_01/backtest_id=bt_s1_bqml_baseline_pvfq_n30_bw_h5_v20260604_01`；诊断结论 `usable_signal`、confidence=`low`，valid/test RankIC 均为正但 test topN 5d 平均收益为负，后续仍需稳健性检查。
- 工作记忆轻清理完成：`AGENT_HANDOFF.md` 按协议缩到当前摘要 + 最近 3 条交接，19 条旧交接已归档到 `.agent/memory/archive/AGENT_HANDOFF_2026-06.md`。
- 新增并改造策略 1 Ledger v1 交易执行语义 PRD：`docs/prd/PRD_20260604_01_策略1LedgerV1交易执行语义.md`，固化 t-1 信号 / t 开盘执行、pending sell 每日继续卖、实际持仓 netting、现金缩放、订单状态和每日 mark-to-market NAV；明确不改变模型训练/预测来源，并要求用正式 baseline 参数做 A/B；PR #49 review 后已补 T+1 卖出锁定非目标；2026-06-04 已进一步纳入 P1 fixed-model 连续扩展回测（`2024-01-02` 至 `2026-04-30`）和 P2 ledger state resume 验收。
- Ledger v1 P0 交易执行语义已进入 `main`（commit `602baea`）：`08_run_backtest.sql` 已改为 `ledger_exec_v1` 日级账户 ledger，`09`/`10`/`11`、ADS 字段说明、报告脚本、诊断脚本和 README 已同步；已通过 `08/09/10/11` BigQuery dry-run、Python `py_compile`、`git diff --check`，并用正式 baseline run_id 做短区间 BigQuery smoke（`backtest_id=bt_ledger_v1_p0_smoke_20260604_01`，2024-01-02 至 2024-02-29），`08`/`09`/本地报告/`10_qa_runner_outputs.sql` 均成功。
- Ledger v1 P1/P2 已完成当前正式口径验收：P1 extended fresh run `s1_bqml_baseline_pvfq_n30_bw_h5_extended_20260604_01` / `bt_s1_bqml_baseline_pvfq_n30_bw_h5_extended_20260604_01` 覆盖 `2024-01-02` 至 `2026-04-30`，复用正式 baseline 模型/参数/score orientation，不做重新训练；P2 resume run `s1_bqml_baseline_pvfq_n30_bw_h5_resume_20260604_01` / `bt_s1_bqml_baseline_pvfq_n30_bw_h5_resume_20260604_01` 从父回测 `2025-12-31` 状态恢复，`sql/ml/strategy1/15_qa_ledger_resume_consistency.sql` 6 个断言全部通过，full fresh 与 resume segment 在 NAV、现金、日收益、持仓和成交事实上一致。短区间 resume smoke `bt_ledger_resume_smoke_20260604_01` 也曾通过 `08`/`09`/本地报告/`10_qa_runner_outputs.sql`。
- 新增并改造策略 1 月度滚动重训 PRD：`docs/prd/PRD_20260604_02_策略1月度滚动重训.md`，定义月度重训 cadence、rolling 5 年训练窗口、12 个月 valid 窗口、月内固定模型、失败回退和 PIT-safe prediction stream；明确必须在 Ledger v1 P0/P1/P2 后单独实现；PR #49 review 后已补 oriented RankIC 通过标准和 test split 事后评价口径。
- 新增策略 1 因子贡献度分析 PRD：`docs/prd/PRD_20260604_03_策略1因子贡献度分析.md`，定义不做消融实验的因子分析：BQML 模型系数/标准化系数、单因子 RankIC/bucket lift、score contribution、组合因子暴露、组合因子归因 proxy 和因子相关性/共线性摘要；PR #51 review 后已补充单因子系数排名受共线性影响、组级解读优先、proxy 不可加总等限制说明；实施顺序建议放在 Ledger v1 P0 前，但不代表优先级高于 Ledger 或月度重训。
- 新增策略 1 Cloud Run 训练回测执行器 PRD：`docs/prd/PRD_20260604_04_策略1CloudRun训练回测.md`，定义用 Cloud Run Jobs 执行 sklearn logistic 训练 / 预测和 Python `ledger_exec_v1` 回测；保留 BigQuery DWS/ADS、GCS artifact、报告诊断和 QA 契约；默认并发数为 manifest 可执行实验数量，owner 可用 `--max-parallel-experiments` 显式限流。
- 新增策略 1 scikit-learn native 模型实验 PRD：`docs/prd/PRD_20260605_03_策略1Sklearn模型实验.md`，定义 Cloud Run sklearn parity 未通过后的新 baseline 实验方案：固定当前最优交易参数、并发训练 36 个 LogisticRegression 候选、valid-only 选 Top 5 完整回测，并用 native acceptance gate 决定是否建立 `cloud_run_sklearn_native_baseline_v1`。PR #69 review 后已补训练窗口精确日期、Top 5 / accepted 的 valid 稳定性约束，以及跨模型族复用 2025 test 的批准和最终 holdout 规则。本次只写文档，未实现代码、未执行 BigQuery。
- 策略 1 Cloud Run 真实 smoke 已完成：部署镜像 `asia-east2-docker.pkg.dev/data-aquarium/quant-ashare/strategy1-cloudrun-runner@sha256:6564434f9f216aec6c86cae3923bc44450c3ca26ead14a248b05ca77087d8ead`，Cloud Run Jobs 配置为 16Gi/4CPU/`--max-retries=0`；runtime service account 已授予 `ashare_ads` 写权限。smoke `cloudrun_smoke_pvfq_n30_bw_h5` 跑通 train/predict execution `strategy1-train-predict-job-s5725` 和 backtest/report execution `strategy1-backtest-report-job-6fzvr`，prediction 1,056,716 行，报告 uploaded 到 `gs://ashare-artifacts/reports/strategy1/ml_pv_clf_v0/run_id=s1_cloudrun_sklearn_smoke_20260604_02/backtest_id=bt_s1_cloudrun_sklearn_smoke_20260604_02`；`16`（smoke 模式）和 `17` QA 通过。结果：total_return 46.29%、Sharpe 1.111、max_drawdown -13.94%、excess_return 17.28% vs `000852.SH`；但 sklearn parity failed，`model_quality_status=model_quality_not_equivalent`。
- 策略 1 因子贡献度分析 P0 已实现并验证：新增 `scripts/strategy1/attribute_factor_contribution.py`，读取 selected BQML `ML.WEIGHTS`、冻结训练面板、预测池、候选池、持仓和回测 summary，生成 `factor_attribution/` 下模型系数、单因子 RankIC、bucket lift、score contribution、组合因子暴露、归因 proxy、因子组和相关性摘要；新增 `sql/ml/strategy1/14_qa_factor_attribution_outputs.sql`；`render_report.py` 可在第 13 步完成后展示因子贡献度摘要；正式 baseline local-only smoke 成功，`14` QA 全部通过。
- 新增 OQ-010 策略 1 实验并发调度与隔离 PRD：`docs/prd/PRD_20260603_05_策略1实验并发调度与隔离.md`。PRD 定义同阶段 portfolio-only / retrain 实验安全并发所需的状态表、GCS 原子锁、lease/heartbeat、调度器、runner 参数化和写隔离、08 ledger 并发边界、单实验 QA 和并发串号 QA。本次只写 PRD，未改 SQL runner、未跑 BigQuery、未触碰正在运行的 A3 实验。

## 进行中 / 部分（In Progress）

- 策略 1 模型质量诊断主流程已跑通 local smoke 和 uploaded 模式，GCS 诊断目录已生成，ADS 诊断字段已回写；`sql/ml/strategy1/12_qa_model_diagnosis_outputs.sql` 的 `split_tag` 歧义 bug 已修复（PR #27/28），`12` 全部断言通过（含 QA-DIAG-6 split_tag 日数校验、QA-POOL-1~6 live-available 预测池口径校验、QA-ORIENT-DIAG-1 方向登记校验）。
- valid/test live-available 预测池口径修正已实现并合并（PR #29/30）：`01_build_training_panel.sql` 中 valid/test 改用 `predict_live_available_mask`（`in_universe_default` + `has_full_history_60d` + `has_valuation_data`），标签有效性仅用于事后评价；QA-POOL-1~6 全部通过。
- score orientation 校准已实现并合并（PR #32）：`03_select_model_and_register.sql` 在选型阶段计算 `raw_score` 和 `reversed_score`，按 valid RankIC 和 bucket lift 决策 `score_orientation`（`identity` 或 `reverse_probability`），`04_predict_daily.sql` 应用方向校准；`ads_model_registry` 持久化 `score_orientation`，`12` 新增 QA-ORIENT-DIAG-1 断言。2026-06-03 已完成 livepool reverse-score shadow run（`s1_bqml_livepool_revscore_20260603_01`），验证反向后的 valid/test RankIC 转正且回测从亏损转为正收益（total_return=0.2787）。 oriented run（`s1_bqml_livepool_oriented_20260603_01`）的 `12` QA 全部通过。
- OQ-010 首轮实验 runner 实现 PR #37 已合并并验证 dry-run；review 后已补 portfolio-only `prediction_run_id` 路径和目标权重留现金口径。2026-06-03 已执行 A0（`run_id=s1_bqml_oq010_oq010_a0_n5_w20_20260603_01` / `backtest_id=bt_s1_bqml_oq010_oq010_a0_n5_w20_20260603_01`）01-12：`10` 和 `12` QA 均通过，诊断 artifact 已上传 GCS。2026-06-04 PR #47 合并后 Stage C 已重跑通过；随后补齐 19 个缺失全因子组合，形成 24 个 `3*2*2*2` 组合对比，所有组合均通过 `12`。PR #48 已合并并发 dependency batching 与诊断状态语义修复。该 SQL runner 结果现仅作历史 reference / audit；当前进行中的是寻找可接受的 Cloud Run Python baseline，并改造月度滚动重训 PRD。
- OQ-010 策略 1 实验并发调度与隔离已用于 Stage C 与全因子补跑；同 stage dependency batching 和诊断状态语义问题已由 PR #48 合入 `main`。
- Ledger v1 P0 已进入 `main`；Ledger v1 P1 fixed-model 2026 扩展回测与 P2 state resume 一致性验收已完成，`15_qa_ledger_resume_consistency.sql` 全部通过。因子贡献度分析已实现并通过正式 baseline local-only smoke + `14` QA。后续建议进入月度滚动重训 prediction stream，避免再把模型生命周期变化与已收口的交易执行语义混在一起。
- 策略 1 Cloud Run 训练回测执行器首版已由 PR #56 合并到 `main`，orchestrator 状态/锁增强已由 PR #57 合并到 `main`。真实 Cloud Run/BQ smoke 已在 2026-06-05 完成并通过 `16` / `17` QA（`16` 使用 smoke 模式允许 parity 未通过但证据完整）。`docs/prd/PRD_20260605_02_策略1CloudRun轻量Task并发.md` 的 task fan-out P0 也已完成低成本真实 smoke和正式全量验收：`prepare_matrix` 集中读取训练面板并生成 frozen matrix，candidate task 按 Cloud Run Jobs `--tasks` 并发训练，reducer 汇总选型写 registry/prediction，`cloudrun_backtest_report` 完成报告/诊断/`10`/`12` QA，Cloud Run 专项 `16`/`17` QA 均通过。边界仍为：01 训练面板由既有 BigQuery SQL 预先构建，05-07 复用 BigQuery SQL，Python ledger P0 仅支持 fresh-start；首轮 sklearn native search 未建立 accepted baseline，下一步应继续按 native acceptance / 新模型 PRD 寻找 Cloud Run Python baseline。

## 进行中 / 部分（In Progress）

- ODS Parquet schema repair 实现已启动：按 `docs/prd/PRD_20260603_04_ODS外部表ParquetSchema修复.md` 新增 10 个 endpoint 的 schema contract YAML（`configs/ods_schema_contracts/*.yml`）、修复脚本（`scripts/ods_repair/repair_parquet_schema.py`）、验证脚本（`scripts/ods_repair/validate_repair.py`）、QA SQL（`sql/qa/06_ods_parquet_schema_checks.sql`）和执行文档（`scripts/ods_repair/README.md`）。脚本支持幂等（manifest 跟踪）、backup write-once、INT->FLOAT64 `<2^53` fail-closed 精度复核、dry-run 模式；PR #43 review 后已修正 P0/full QA 命令示例格式、null count 变化阻断发布、BQ staging 行数/列可读验证和 staging 清理。2026-06-05 只读复核显示 `06_ods_parquet_schema_checks.sql` 对 P0 与 all 范围均通过，当前 BigQuery 读层未再暴露 schema mismatch；后续待 owner 决定是否关闭 OQ-012 或保留防复发工程项。

## 未开始 / 未来（Not Started / Future）

- `lookback_start_date` 从固定默认值升级为按最大滚动窗口计算/调度配置。
- 「从 ODS 继承字段描述」的脚本（bq show → 映射 → bq update）。
- 增量调度（dbt 或 Airflow + SQL）、数据质量断言。
- 策略 1 Cloud Run runner 后续验收：真实 Cloud Run smoke、GCS model/report artifact、ADS 回写、`strategy1_experiment_run_status` 和 `16` / `17` QA 已跑通；sklearn native search 首轮未建立 accepted baseline。PRD04 Cloud Run Python LightGBM baseline search 代码实现已进入 PR；下一步是合并部署后真实执行 40 候选 wave 2，并按 Top5 acceptance 结论决定是否进入 `lightgbm_regression` wave 3，同时继续补 Python ledger vs 历史 SQL ledger 的差异 / 一致性验收。
- P0 通用 DWS 扩展表：`dws_stock_feature_fin_daily` 已落地物化（OQ-003 默认合并报表口径）；`dws_market_state_daily` 仍待补。三大报表单季 `q_*` 派生延后 P1。
- 策略 1 runner v0 模型质量提升（独立于管线）：历史 raw/source run（`s1_bqml_livepool_20260602_01`）valid rank_ic≈-0.10（反向预测）、回测 NAV≈0.02，诊断结论为 `signal_inverted`。当前 oriented run（`s1_bqml_livepool_oriented_20260603_01`）已通过 score orientation 校准修正方向，valid/test RankIC 转正，shadow backtest total_return=0.2787。诊断 QA（`12`）已全部通过，live-available 预测池口径（PR #29/30）和 score orientation 校准（PR #32）已实现并验证。`docs/prd/PRD_20260603_02_策略1首轮质量迭代实验.md` 已由 PR #35 合并；OQ-010 首轮实验 runner 参数化、manifest、portfolio-only `prediction_run_id` 复用预测源、对比报告脚本和 horizon-aware 诊断/QA 已由 PR #37 合并进入 `main` 并 dry-run 通过。A0 已端到端跑通，A1-A3 待诊断稳定性修复合并后继续。阶段 A/B/C 基础执行不做全量笛卡尔积：先固定 weekly + 5d 跑 4 个持股/权重，再用 A 晋级组合跑 3 个频率，再用 A/B 晋级参数跑 3 个 horizon；阶段 D 只接晋级参数跑 2 个特征集合。`30/5%` 表示目标持股 30 只、上限 5%，等权目标约 3.33%，实际入选不足时剩余现金保留。A0 作为 5d 基线复现检查可重训，A1-A3/B0-B2 仅重跑 05-12 并复用预测源。如需要再补 A/B、A/C、B/C pairwise 小型交互复核，最终 `2 * 2 * 2` 仅作条件触发保底复核。并发执行需等 `PRD_20260603_05_策略1实验并发调度与隔离.md` 的状态表、GCS 原子锁、lease/heartbeat、调度器和 QA 实现后再启用。
- lookback-capable 价格构建输入：当前策略 1 DWS 只读取最终 DWD/DIM，不直接读 ODS；由于最终 DWD 价格表不落 2018 buffer 行，2019 年初 60 日价格窗口用 `has_full_history_60d=FALSE` 显式标记并由默认样本掩码剔除。若要求 2019-01 起 60 日窗口完整，需要补专用 lookback 构建输入或调整 DWD/DWS 构建方式。
- P1+ 资金面/事件/行业族 DWD。
- `dim_stock_sw_industry_hist` / `dim_stock_ci_industry_hist` 建表 SQL 与 QA（`out_date` 边界、区间重叠/缺口、2019+ 覆盖率）。

## Coverage Snapshot

| 能力 | 状态 | 备注 |
|---|---|---|
| ODS 理解 | 高 | 57 表字段+分区语义已探明 |
| DWD/DIM 设计 | 高 | 主文档已完成；§4.6 已修订 2019 前数据范围 |
| 命名/单位/分区/注释规范 | 高 | 已敲定并写入文档 |
| P0 建表 SQL | 已完成 | `sql/` 已新增 4 张 DIM + 5 张 DWD + QA；OQ-004 新增 `dim_index` 和专项 QA |
| P0 表物化/QA | 已完成 | 4 张 DIM + 5 张 DWD 已物化；PR #9 后 `dim_stock` 依赖链已重建，P0 smoke QA 通过；OQ-004 专项 QA 通过 |
| DWS/ADS 设计 | 已完成 | 两篇设计文档已完成；策略 1 DWS 六表、ADS 表契约与 runner 脚本已落地 |
| ETL/调度 | 部分完成 | OQ-005 Phase 0/1/1.5/1.6/1.7 采集 manifest/schema/meta/stub、endpoint worker、Cloud Run Jobs、Direct VPC egress + Cloud NAT 固定出口、`ashare-ingest-current-scope` 生产入口、GCS 小范围写入 smoke、ODS 外部表可读性 QA、每日轻量 readiness QA、Composer 3 环境、DAG default Celery queue smoke 和 `2026-05-20` 至 `2026-06-05` 当前范围 ODS 生产采集已完成；PR #80 后 `daily_current` 与 `qa_only` 生产 smoke 均通过；scheduled 非交易日 skip gate 已在本分支实现，待合并部署 smoke；完整 Dataform 生产链路、补跑/resume 自动化和完整 ODS→ADS 运维观测闭环仍待完成 |
| 财务三大报表 DWD | 已完成 | `dwd_fin_income/balancesheet/cashflow` + `_latest`（`sql/dwd/06-11`）已物化，保留 report_type 口径字段，`sql/qa/04` + OQ-006 `sql/qa/05` 通过 |
| DWS 特征/标签 SQL | 部分完成 | 策略 1 universe、价格/估值特征、标签、宽表、样本 + `dws_stock_feature_fin_daily`（默认合并口径财务特征）已物化并 QA；市场状态待补 |
| 策略/ADS 闭环 | 已跑通 | `ml_pv_clf_v0` runner 01-10 已在 BigQuery 端到端实跑（PR #12），08 为账户级 ledger，`10` 16 断言全过；模型质量待迭代（OQ-010） |
| 行业映射 | 可落地设计完成 | ODS 已有 index_member_all / ci_index_member；待 SQL 和 QA |
