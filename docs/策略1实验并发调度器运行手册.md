> 文档维护：GPT-5 Codex（最近更新 2026-06-10；原文 DeepSeek V4）

# 策略 1 实验并发调度器运行手册（Retired / historical）

本文记录旧 OQ-010 BigQuery SQL / BQML 调度器的历史背景。`scripts/strategy1/run_oq010_experiments.py` 已删除，不再作为当前 Strategy1 执行、fallback、诊断或新增开发入口。

当前 active path 为：

```text
Cloud Run Python training / prediction / ledger
  -> shared BigQuery SQL training panel / candidate / portfolio / order / report / QA
  -> v3 acceptance gate
```

## 当前状态

- 旧调度器依赖 BQML-only `02-04` 和 SQL ledger fallback `08`，这些文件已退役并删除。
- `--use-bq-ledger` 已从 Cloud Run Python runner / orchestrator 中移除。
- 历史 run / backtest / artifact 仍可在 ADS、GCS 和历史 PRD 中追溯，但不得作为新的生产 baseline 或 fallback 入口。

## 当前替代入口

- 单实验训练 / 预测：`python -m scripts.strategy1_cloudrun.train_predict` 或 task fan-out / native search 入口。
- 单实验回测 / 报告：`python -m scripts.strategy1_cloudrun.backtest_report`，默认使用 Python `ledger_exec_v1_lot100`。
- 多候选搜索：使用 `scripts.strategy1_cloudrun.orchestrate_*` 系列 Cloud Run Python orchestrator。
- 状态表：`ashare_meta.strategy1_experiment_run_status` 仍用于 Cloud Run orchestrator 审计和 resume 输入。

## 历史审计说明

如需理解旧 OQ-010 调度器曾经如何做锁、并发和状态记录，请查阅对应 PRD / 历史 commit，而不是恢复本文件中的旧命令。任何恢复 BQML-only 或 SQL ledger fallback 的需求都必须重新写 PRD 并经 owner 明确批准。
