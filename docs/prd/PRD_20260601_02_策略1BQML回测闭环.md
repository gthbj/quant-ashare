# PRD：策略 1 BigQuery ML Runner 与回测闭环

> 文档维护：GPT-5（最近更新 2026-06-01）
> 状态：草案 v0.1，等待 owner review。
> 范围声明：本文是 `ml_pv_clf_v0` 的**实现型 PRD**，目标是把已完成的 DWS/ADS 表契约和 BigQuery ML runner 设计落成可执行 SQL runner，并跑通训练、预测、组合、回测、报告闭环。
> 上游依据：`docs/prd/PRD_20260601_01_策略1价格量价基础分类模型.md`、`docs/策略1-ml_pv_clf_v0-runner设计.md`、`docs/数据仓库建模方案-DWS-ADS.md`。

---

## 1. 背景

策略 1 已完成：

- 6 张 DWS 表物化并通过 `sql/qa/02_strategy1_dws_ads_checks.sql`。
- 11 张 ADS 表契约物化。
- BigQuery ML runner 设计完成并合并。

但当前还不能直接跑回测，因为缺少实际执行脚本：

```text
sql/ml/strategy1/
  01_build_training_panel.sql
  02_train_bqml_logistic_candidates.sql
  03_select_model_and_register.sql
  04_predict_daily.sql
  05_build_candidates.sql
  06_build_portfolio_targets.sql
  07_build_order_plan.sql
  08_run_backtest.sql
  09_build_metrics_and_report_inputs.sql
  10_qa_runner_outputs.sql
```

本文定义这些脚本的需求、输入输出、验收标准和边界。

## 2. 目标与非目标

### 2.1 目标

1. 从 `ashare_dws.dws_stock_sample_daily` 生成冻结训练面板。
2. 用 BigQuery ML 训练 `ml_pv_clf_v0` 候选模型。
3. 按验证集 RankIC / 分层收益选择模型并登记。
4. 写入预测、候选池、目标组合、订单计划和回测 ADS 表。
5. 生成结构化绩效指标和报告输入，并输出 GCS 报告 URI 与本地镜像路径。
6. 提供 QA 脚本，证明无未来泄露、结果可复现、组合约束满足、NAV 连续。

### 2.2 非目标

- 不做 BigQuery 之外的模型训练。
- 不实现真实券商下单或实盘交易。
- 不新增策略 1 之外特征域，如财务、资金流、行业中性化。
- 不改变既有 DWS/ADS 表契约，除非实现中发现契约无法承载 runner 必要字段并单独提 PR。
- 不解决 OQ-010 的业务参数决策；未确认参数以 runner 配置项暴露。

## 3. 用户与使用场景

| 用户 | 使用场景 |
|---|---|
| 研究者 | 跑一次完整策略 1 基线，查看 RankIC、分层收益、NAV、换手、不可成交比例。 |
| 数据工程维护者 | 验证 DWS/ADS 是否能支撑端到端 ML 回测闭环。 |
| owner | 审阅策略 1 是否具备进入后续策略开发或参数决策的基础。 |

核心使用方式：

```text
给定 run_id / backtest_id / 时间窗口 / 参数
  -> 执行 sql/ml/strategy1/01-10 脚本
  -> BigQuery ADS 写入结构化结果
  -> GCS + 本地 reports/ 输出人读报告
```

## 4. 输入、输出与依赖

### 4.1 输入表

| 表 | 用途 |
|---|---|
| `data-aquarium.ashare_dws.dws_stock_sample_daily` | 训练/验证/测试/预测输入样本。 |
| `data-aquarium.ashare_dws.dws_stock_universe_daily` | 候选池过滤和调仓日 universe。 |
| `data-aquarium.ashare_dwd.dwd_stock_eod_price` | 开盘撮合、可买可卖、持仓估值。 |
| `data-aquarium.ashare_dim.dim_trade_calendar` | 周频调仓日、t+1、持有期交易日定位。 |
| `data-aquarium.ashare_dim.dim_index` | 基准指数 canonical 映射、端点可用性和 benchmark 候选校验。 |
| `data-aquarium.ashare_dwd.dwd_index_eod` | 基准收益，使用 canonical `sec_code`。 |

### 4.2 输出表

| 表 | 写入内容 |
|---|---|
| `ashare_ads.ads_ml_training_panel_daily` | 每个 `run_id` 的冻结训练面板。 |
| `ashare_ads.ads_model_registry` | 候选模型与胜出模型登记、参数、指标、URI。 |
| `ashare_ads.ads_model_prediction_daily` | `score`、`rank_raw`、`rank_pct`。 |
| `ashare_ads.ads_stock_candidate_daily` | 调仓日候选池、过滤原因、入选标记。 |
| `ashare_ads.ads_portfolio_target_daily` | 目标权重、目标金额、目标股数。 |
| `ashare_ads.ads_order_plan_daily` | BUY/SELL 订单计划。 |
| `ashare_ads.ads_backtest_trade_daily` | 撮合成交明细。 |
| `ashare_ads.ads_backtest_position_daily` | 每日持仓。 |
| `ashare_ads.ads_backtest_nav_daily` | 净值、现金、收益、基准超额。 |
| `ashare_ads.ads_backtest_performance_summary` | 汇总绩效和报告 URI。 |
| `ashare_ads.ads_signal_monitor_daily` | 样本数、预测数、信号分布和异常监控。 |

### 4.3 报告产物

GCS 持久产物：

```text
gs://<ashare-artifact-bucket>/reports/strategy1/ml_pv_clf_v0/run_id=<run_id>/backtest_id=<backtest_id>/
  report.md
  report.html
  metrics.json
  assets/
```

本地用户读取镜像：

```text
reports/strategy1/ml_pv_clf_v0/run_id=<run_id>/backtest_id=<backtest_id>/
```

`reports/` 默认被 `.gitignore` 忽略，不提交。

## 5. 运行参数

Runner 必须集中定义参数，避免脚本间硬编码漂移。

| 参数 | 必填 | 默认/示例 | 说明 |
|---|---|---|---|
| `run_id` | 是 | `s1_bqml_20260601_01` | 本次训练/预测运行唯一 id。 |
| `strategy_id` | 是 | `ml_pv_clf_v0` | 策略 id。 |
| `model_id_prefix` | 是 | `ml_pv_clf_v0_bqml_logit` | 候选模型名前缀。 |
| `backtest_id` | 是 | `bt_s1_bqml_20260601_01` | 回测 id。 |
| `feature_version` | 是 | `strategy1_pv_v0_20260601` | DWS 特征版本。 |
| `label_version` | 是 | `open_to_close_h1_5_10_20_v20260601` | 标签版本。 |
| `horizon` | 是 | `5` | 主标签 horizon。 |
| `train_start_date` | 是 | `2019-04-03` | 当前默认可训练样本最早日期。 |
| `train_end_date` | 是 | `2023-12-31` | 训练截止日。 |
| `valid_start_date` | 是 | `2024-01-01` | 验证开始日。 |
| `valid_end_date` | 是 | `2024-12-31` | 验证截止日。 |
| `test_start_date` | 是 | `2025-01-01` | 测试开始日。 |
| `test_end_date` | 是 | `2025-12-31` | 测试截止日。 |
| `predict_start_date` | 是 | `2024-01-01` | 需要落预测的开始日，至少覆盖 valid/test。 |
| `predict_end_date` | 是 | `2025-12-31` | 首版回测预测截止日。 |
| `board_allowlist` | 是 | `['SSE_MAIN','SZSE_MAIN']` | 首个基线已定为仅沪深主板，不含北交所、创业板、科创板。 |
| `target_holdings` | 是 | 待 owner 确认 | 持股数，OQ-010。 |
| `max_single_weight` | 是 | 待 owner 确认 | 单票权重上限，OQ-010。 |
| `cost_bps` | 是 | 待 owner 确认 | 成本假设，OQ-010。 |
| `benchmark_sec_code` | 是 | `000852.SH` 示例 | 基准指数 canonical 代码；执行前必须通过 `dim_index` 可用性和完整 NAV 窗口覆盖校验。 |
| `artifact_base_uri` | 是 | `gs://.../reports/strategy1` | GCS 报告根路径。 |
| `force_replace` | 是 | `FALSE` | 是否覆盖同 `run_id/backtest_id` 结果。 |

如果 OQ-010 的剩余参数尚未决策，实现 PR 可以提供示例配置，但不得把示例值写成业务定稿。

## 6. 功能需求

### FR-1 构建训练面板

脚本：`01_build_training_panel.sql`

要求：

- 读取 `dws_stock_sample_daily`。
- 过滤 `feature_version`、`label_version`、日期范围。
- 只把白名单模型特征写入训练面板的可训练列。
- `board` 保留为分组/暴露字段，但不进入模型训练列。
- 写入 `split_tag`：`train`、`valid`、`test`、`live`。
- `run_id, sec_code, trade_date` 唯一。
- 默认训练样本使用 `sample_trainable_default=TRUE`。

### FR-2 训练 BQML 候选模型

脚本：`02_train_bqml_logistic_candidates.sql`

要求：

- 模型类型：BQML `LOGISTIC_REG`。
- 标签：`label_top30_5d`。
- 正则化：使用 BQML 原生 `L1_REG` / `L2_REG` 候选网格。
- 每组候选参数生成独立 BigQuery ML model object。
- 候选模型登记到 `ads_model_registry`，状态为 `candidate`.
- 不使用随机 split；训练 SQL 只读 `split_tag='train'`。

候选网格首版至少包含：

| candidate_id | `L1_REG` | `L2_REG` |
|---|---:|---:|
| `l1_0_l2_0` | 0 | 0 |
| `l1_0_l2_1e_4` | 0 | 0.0001 |
| `l1_0_l2_1e_3` | 0 | 0.001 |
| `l1_1e_5_l2_1e_4` | 0.00001 | 0.0001 |
| `l1_1e_4_l2_1e_3` | 0.0001 | 0.001 |

### FR-3 验证、选择并登记模型

脚本：`03_select_model_and_register.sql`

要求：

- 对每个候选模型在 valid 窗口执行 `ML.PREDICT`。
- 计算 valid：
  - AUC / log_loss
  - RankIC 均值与按年统计
  - 分 5/10 层收益
  - TopN 未来收益
  - 样本数、预测数、缺失率
- 主选择准则：`valid_rank_ic_mean`。
- tie-breaker：分层单调性、TopN 收益稳定性、log_loss。
- 胜出模型状态更新为 `registered` 或 `selected`；失败候选标为 `candidate_rejected`。
- `model_params_json` 必须记录候选网格、胜出原因、特征列清单。

### FR-4 生成预测分

脚本：`04_predict_daily.sql`

要求：

- 对 selected model 执行 `ML.PREDICT`。
- 覆盖 valid/test，后续可覆盖 live。
- 从 BQML 输出中抽取正类概率为 `score`。
- 按 `predict_date` 做横截面排序：
  - `rank_raw`
  - `rank_pct`
- 写入 `ads_model_prediction_daily`。

### FR-5 生成候选池

脚本：`05_build_candidates.sql`

要求：

- 调仓日为每周最后一个开市日。
- 只使用 `rebalance_date=t` 当日已知字段。
- 禁止用 `t+1` 的 `can_buy_open`、`label_entry_tradable` 或未来字段预过滤。
- 输出完整候选池和最终入选标记。
- `filter_reason` 必须可解释。

### FR-6 生成目标组合

脚本：`06_build_portfolio_targets.sql`

要求：

- 默认按 `score DESC` 选 TopN。
- 等权或按配置生成权重。
- 单票权重不得超过 `max_single_weight`。
- 目标权重之和不得超过 1。
- 不因 `t+1` 不可买而事后替补低排名股票。

### FR-7 生成订单计划

脚本：`07_build_order_plan.sql`

要求：

- 对比上一期持仓和本期目标组合生成 BUY/SELL。
- 输出 `expected_price`、`expected_amount_cny`、`order_reason`。
- 订单计划只作为回测输入，不代表真实下单。

### FR-8 BigQuery SQL 回测

脚本：`08_run_backtest.sql`

要求：

> **实现说明（PR #12 落地为账户级 ledger）**：本节最初设计的「预计算 next-sellable / `SELL_BLOCKED_NO_NEXT_SELLABLE_60D`」属于 v0 set-based 口径。该 v0 在真机实跑时违反守卫（现金为负、gross 远超 1），已按 DECISION-20260601-07 升级为 v1 账户级有状态 ledger（DECISION-20260602-01）。下列要求按 v1 ledger 口径理解。

- 信号日 `t`，在 `t+1` 开盘撮合。
- 买入要求 `can_buy_open[t+1]=TRUE`；不可买则该腿本期跳过、记 `BUY_SKIPPED_UNTRADABLE` 意图行并保留现金。
- 仓位按**当前 NAV × 目标权重**定档；卖出先于买入；买入受可用现金约束（超出按比例缩放），现金不为负、gross ≤ 1。
- 卖出在调仓 `t+1 exec_date` 撮合；不可卖（`can_sell_open=FALSE` 或无开盘价）则该腿本期跳过、记 `SELL_SKIPPED_UNTRADABLE` 意图行，持仓 carry 到**下一个调仓执行日**再尝试。
- **v1 不做 60 交易日 daily next-sellable 顺延搜索，也没有 `SELL_BLOCKED_NO_NEXT_SELLABLE_60D`**（原 v0 方案已废弃）。
- 成交诊断（buy/sell 的 attempt/filled/skipped 计数与 skip rate）由 `09` 从 `ads_backtest_trade_daily` 的 FILLED / `*_SKIPPED_UNTRADABLE` 行 1:1 汇总，可对账。
- 持仓每日按收盘价估值（停牌无价用最近可用收盘前向填充）。
- 写入 trade / position / nav / summary 四类 ADS 回测表。
- 成本参数化，不硬编码业务最终值。

### FR-9 指标与报告输入

脚本：`09_build_metrics_and_report_inputs.sql`

报告渲染脚本：`scripts/strategy1/render_report.py`

要求：

- 生成回测汇总指标：
  - 年化收益、年化波动、Sharpe、最大回撤
  - 基准收益、超额收益、IR
  - 年化换手
  - 买入/卖出不可交易跳过率（v1 ledger：`buy_skip_rate` / `sell_skip_rate`，从 `ads_backtest_trade_daily` 的 `*_SKIPPED_UNTRADABLE` 意图行 1:1 汇总）
  - 成本敏感性
- SQL 只负责写 ADS 汇总和生成报告所需结构化数据，不直接生成 Markdown/HTML/PNG。
- `scripts/strategy1/render_report.py` 是必需交付物，负责：
  - 从 ADS 汇总或导出的 `metrics.json` 读取结构化指标。
  - 渲染 `report.md`、`report.html` 和 PNG 图表。
  - 默认上传报告产物到 GCS；`--skip-gcs-upload` 时仅写本地镜像。
  - 写入或回传本地 `reports/` 镜像路径。
- 报告状态回写 ADS `metrics_json`：始终写 `local_report_path` 与 `report_upload_status`（`uploaded` / `skipped`）；`report_uri` **仅在 GCS 上传成功时**写入，`skipped`（local-only）模式不写 `report_uri`，避免把不存在的 `gs://` 当成已产出。
- 报告脚本不得承担模型训练、预测或回测计算职责。

### FR-10 QA

脚本：`10_qa_runner_outputs.sql`

必须断言：

- 同一 `run_id, sec_code, trade_date` 训练面板唯一。
- train/valid/test 日期互斥且有序。
- 模型特征不含 `fwd_*`、`label_*`、`rank_pct_*`、`*_qfq`、`board`。
- 预测日 `rank_raw=1` 唯一，`rank_pct` 在 `[0,1]`。
- 每个调仓日目标权重和 `<=1`，单票权重 `<=max_single_weight`。
- 代码审查项：`05_build_candidates.sql` 禁止引用 `t+1` 可交易字段做预过滤；该项不作为运行时 SQL 断言。
- 数据侧 QA：统计 `t+1` 实际不可买但仍被 `is_selected_candidate=TRUE` 选中的样本数和占比；若长期为 0，报告中标记“可能存在事后过滤风险”，需要人工复核候选池 SQL。
- 成交可对账（v1 ledger）：09 的 `buy/sell` attempt/filled/skipped 计数与 skip rate 从 `ads_backtest_trade_daily` 的 `FILLED` / `*_SKIPPED_UNTRADABLE` 行 1:1 汇总（不再用旧 episode/60 日 next-sellable 口径）。
- NAV 覆盖开市日且无非预期断点；现金 `>= -1`、`gross_exposure <= 1.005`、持仓 `(trade_date, sec_code)` 唯一（ledger 守卫）。
- `ads_model_registry` 中 selected model 有唯一 `model_uri`。
- 报告已产出（模式感知）：summary `metrics_json` 有 `report_upload_status` 与 `local_report_path`；`report_uri` 仅在 `uploaded` 时存在、`skipped` 时为空。

## 7. 非功能需求

### 7.1 幂等

- 默认发现同 `run_id` 或 `backtest_id` 已存在结果时失败。
- `force_replace=TRUE` 时，脚本必须按 `run_id/backtest_id` 删除旧 ADS 行后重写。
- BigQuery model object 不覆盖 active model；同 run 重跑只允许覆盖同名候选模型。

### 7.2 成本控制

- 所有大表读取必须带日期分区过滤。
- 禁止训练 SQL 使用 `SELECT *`。
- 首次实现需提供 dry-run 或 bytes processed 记录方式。

### 7.3 安全

- 不写任何凭据、token、service account key。
- GCS bucket 名称通过参数注入，不写密钥。
- 本地 `reports/` 只保存生成型报告镜像，不提交 git。

### 7.4 可追溯

每个 run 必须能追溯：

- `run_id`
- `model_id`
- `backtest_id`
- git commit
- 参数 JSON
- feature / label / universe version
- 训练、验证、测试窗口
- GCS report URI

## 8. 交付物

| 交付物 | 类型 |
|---|---|
| `sql/ml/strategy1/01_build_training_panel.sql` | SQL |
| `sql/ml/strategy1/02_train_bqml_logistic_candidates.sql` | SQL |
| `sql/ml/strategy1/03_select_model_and_register.sql` | SQL |
| `sql/ml/strategy1/04_predict_daily.sql` | SQL |
| `sql/ml/strategy1/05_build_candidates.sql` | SQL |
| `sql/ml/strategy1/06_build_portfolio_targets.sql` | SQL |
| `sql/ml/strategy1/07_build_order_plan.sql` | SQL |
| `sql/ml/strategy1/08_run_backtest.sql` | SQL |
| `sql/ml/strategy1/09_build_metrics_and_report_inputs.sql` | SQL |
| `sql/ml/strategy1/10_qa_runner_outputs.sql` | SQL |
| `sql/ml/strategy1/README.md` | 执行说明 |
| `scripts/strategy1/render_report.py` | 报告渲染和 GCS 上传脚本 |
| `reports/strategy1/...` | 本地报告镜像，生成物，不提交 |
| GCS `reports/strategy1/...` | 持久报告产物 |

## 9. 验收标准

### 9.1 工程验收

- [ ] 01-10 脚本可按 README 顺序执行。
- [ ] 产生一个完整 `run_id/backtest_id`。
- [ ] ADS 表写入训练面板、模型登记、预测、候选、组合、订单、回测、监控结果。
- [ ] QA 脚本全部通过。
- [ ] `scripts/strategy1/render_report.py` 生成 Markdown/HTML/PNG 并写本地镜像；默认上传 GCS（`report_upload_status=uploaded` + 真实 `report_uri`），`--skip-gcs-upload` 为 local-only（`report_upload_status=skipped`、`report_uri` 为空）。
- [ ] 本地 `reports/` 有镜像；GCS 模式下 GCS 有报告产物且 `report_uri` 指向真实对象（local-only 模式不要求 GCS 产物）。
- [ ] `git diff --check` 通过。

> PR #12 的端到端实跑用 local-only（`--skip-gcs-upload`）模式验收：本地镜像 + `report_upload_status=skipped` + `report_uri=NULL` + `10` 模式感知 QA 通过。GCS bucket（`ashare-artifacts`）与 ADC 准备好后去掉 `--skip-gcs-upload` 重跑 render，即为 `uploaded` + 真实 `report_uri`。

### 9.2 量化验收

- [ ] 输出 valid/test AUC、log_loss、RankIC、分层收益。
- [ ] 输出 test 组合 NAV、最大回撤、换手、成本影响、不可成交比例。
- [ ] 报告明确列出参数、窗口、样本数、模型选择依据。
- [ ] 若策略无超额，报告仍需给出可信结论和失败位置。

## 10. 开放问题与默认处理

| 问题 | 状态 | PRD 默认处理 |
|---|---|---|
| OQ-010 成本、调仓频率、持股数、权重上限 | open | runner 参数化；实现可提供示例值，不写死为业务定稿。首个基线股票池已定为仅沪深主板，不含北交所、创业板、科创板。 |
| OQ-004 基准指数 | closed by implementation | `benchmark_sec_code` 参数化；只使用 `dwd_index_eod.sec_code` canonical 代码，并在 runner 前置校验 `dim_index` 可用性和 NAV 窗口覆盖。 |
| OQ-011 2019Q1 60 日窗口 | open | 当前按已物化 DWS 口径处理，默认可训练样本从 `2019-04-03` 起。 |
| OQ-005 调度工具 | open | 先交付可手工执行的 BigQuery SQL；不绑定 dbt/Airflow。 |

## 11. 里程碑

| 阶段 | 内容 | 退出条件 |
|---|---|---|
| M1 | 训练面板 + 候选模型训练 | 训练面板唯一性 QA 通过，BQML model object 创建成功。 |
| M2 | 模型选择 + 预测 | selected model 写 registry，预测表有 valid/test 分数和排名。 |
| M3 | 候选/组合/订单 | 每个调仓日候选和目标权重满足约束。 |
| M4 | 回测 | ADS 回测表完整写入，NAV 连续；现金 >=-1 / gross <=1.005 / 持仓唯一（v1 ledger 守卫）；skipped intent 与 skip rate 与 `ads_backtest_trade_daily` 可对账。 |
| M5 | 报告与 QA | 报告生成（local-only 镜像即可验收；GCS 产物仅 `uploaded` 模式要求），QA 全绿（含模式感知报告断言）。 |

## 12. 风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| BQML 训练成本超预期 | 查询成本上升 | 候选网格首版小规模；所有脚本带日期过滤；先 dry-run。 |
| valid RankIC 选择过拟合 | test 表现衰减 | test 只做最终评估；报告分开列 valid/test。 |
| OQ-010 成本 / 调仓 / 持仓参数未定 | 回测无法业务定稿 | 参数化并在报告标记为实验配置；板块纳入口径已定为仅沪深主板。 |
| 买卖不可成交处理复杂 | NAV 偏差 | v1 ledger：不可交易腿本期跳过并记 `*_SKIPPED_UNTRADABLE` 意图行、持仓 carry 到下一调仓执行日再试；停牌无价用 ffill 收盘估值（不做 60 日 next-sellable 搜索）。 |
| 报告与 ADS 不一致 | 审计困难 | 报告只由 ADS 汇总生成；写回 `local_report_path` + `report_upload_status`，`report_uri` 仅真实 GCS 上传成功时写。 |
