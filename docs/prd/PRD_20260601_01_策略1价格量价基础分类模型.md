# PRD：策略 1 · 价格量价基础分类模型（`ml_pv_clf_v0`）

> 文档维护：Claude Opus 4.8；GPT-5 合并评审修订（最近更新 2026-06-01）
> 状态：草案 v0.2，已合并 review 修订，策略默认参数仍待 owner 确认。
> 范围声明：**本 PRD 只覆盖策略 1**（价格量价基础分类模型）。资金流/筹码/财务/行业/事件/集成等策略不在本文范围。
> 上游依据：[A股中低频小资金机器学习策略方案](../A股中低频小资金机器学习策略方案.md) §1（交易与研究假设）、§5.1（`ml_ranker_v0`）、§6（模型训练方案）、§7（组合与回测）、§9（ADS 落表流程）。
> 落地原则：复用已物化的 P0 DWD/DIM 与既有建表标准（`sec_code` 主键、金额「元」量「股」、后复权、月分区 + `sec_code` 聚簇、`require_partition_filter`、`--location=asia-east2`、血缘/版本字段）。

---

## 1. 背景与目标

### 1.1 定位

策略 1 是整个量化平台的**第一个端到端闭环**，定位是「**探针 + 基线**」，而非追求高收益：

- **探针**：用最简单、最透明的模型，把「可交易股票池 → 特征 → 标签 → 切分 → 训练 → 预测 → 组合 → 回测」整条链路跑通一遍，**暴露数据穿越、复权错误、不可成交、回测撮合错误**等系统性问题。
- **基线**：建立一个可解释的收益/IC 基准，后续所有复杂模型（策略 2+）都必须证明自己**显著优于**这个基线，否则不值得增加复杂度。

### 1.2 目标与非目标

| | 内容 |
|---|---|
| **目标** | (1) 跑通端到端链路；(2) 验证数据正确性（复权可复现、universe 含退市股、ST/停牌/涨跌停掩码正确、标签无未来泄露）；(3) 产出一个**扣成本后可评估**的横截面选股基线，明确其 alpha 是否存在及失效位置 |
| **非目标** | 不追求高夏普；不做多因子/树模型/集成（留给策略 2+）；不做行业轮动与财务因子；不做实盘下单自动化（仅产出目标持仓清单）；不做分钟/盘中数据 |

### 1.3 范围（Scope）

- **In scope**：行情 + 日频估值两类特征；线性模型（Logistic / Ridge + ElasticNet）；周频调仓；建议 5–8 只等权小组合（待 owner 确认）；2019+ 回测。
- **Out of scope**：`fina_indicator`/`moneyflow`/`cyq_perf`/`forecast`/`limit_list_d`/`top_list` 等表；非线性模型；行业中性化（首版仅监控不强制）；多策略融合。

---

## 2. 成功标准与验收

首版验收**分两层**，工程正确性是硬门槛，策略表现只要求「可解释、可判断」，不设硬收益门槛（探针定位）。

### 2.1 工程验收（硬门槛，必须全过）

- [ ] 任取一只股票，能用 `close_hfq` 复现其后复权价与 `ret_1d`，与独立计算一致。
- [ ] universe **包含已退市股的存活区间**（抽查 ≥3 只已退市股，其历史样本存在）。
- [ ] ST / 停牌 / 一字涨跌停 在样本掩码中正确剔除（抽查若干已知事件日）。
- [ ] 标签严格使用 `t+1` 及以后数据；特征严格使用 `≤ t` 数据；**无任何 `t+1` 及未来字段进入特征**。
- [ ] 标签入场点 `t+1` 不可成交（一字板/停牌）的样本被正确标记并在回测中剔除。
- [ ] 训练/验证/测试切分之间有 **purge + embargo**，无标签窗口重叠泄露。
- [ ] 全程不使用前复权（`_qfq`）字段作为特征。

### 2.2 策略验收（评估门槛，要求可解释）

- [ ] **分层单调性**：按预测分 5 或 10 层，Top 层未来收益在**多数年份**高于 Bottom 层。
- [ ] **RankIC**：报告日频截面 RankIC 的均值、t 统计（按 §6.3 校正重叠）、ICIR、逐年分布。
- [ ] **扣成本超额**：Top 组合在中性成本假设下，相对**中证 1000**（见 §8.4）的超额与信息比率；明确正/负及失效年份。
- [ ] **换手与成本敏感性**：报告年换手、三档成本下的净值差异（§8.3）。

> 验收基调：**即使首版无超额也算成功**，只要链路正确、能定位 alpha 缺失的原因。失败定义是「链路有穿越/撮合错误导致结论不可信」。

---

## 3. 数据架构与依赖

### 3.1 可直接复用的已物化资产（无需新建）

| 表 | 层 | 行数 | 策略 1 用途 | 关键字段 |
|---|---|---|---|---|
| `ashare_dim.dim_trade_calendar` | DIM | 13,162 | 交易日对齐、t±k、上市天数、调仓日识别 | `cal_date`, `is_open`, `pre_trade_date`, `trade_date_seq` |
| `ashare_dim.dim_stock` | DIM | 5,853 | universe 生命周期、板块过滤、退市边界 | `sec_code`, `list_date`, `delist_date`, `board`, `market`, `is_delisted` |
| `ashare_dim.dim_stock_name_hist` | DIM | 3,776 | ST 时点判断（SCD2） | `valid_from`, `valid_to`, `is_st`, `is_star_st` |
| `ashare_dwd.dwd_stock_eod_price` | DWD | 8.5M | 行情特征、后复权收益、可成交掩码 | `close_hfq`, `ret_1d`, `amount_cny`, `can_buy_open`, `is_one_word_limit_up`, `is_tradable`, `is_suspended` |
| `ashare_dwd.dwd_stock_eod_valuation` | DWD | 8.45M | 估值/市值/换手特征 | `turnover_rate`, `volume_ratio`, `pe_ttm`, `pb`, `ps_ttm`, `total_mv_cny`, `circ_mv_cny` |
| `ashare_dwd.dwd_index_eod` | DWD | 11,922 | 回测基准、指数超额 | canonical `sec_code`, `source_sec_code`, `index_alias`(CSI1000/CSI500/CSI300...), `close`, `pct_chg` |

> **结论：策略 1 不新增任何 DWD/DIM。** 全部建设量在 DWS/ADS 两层。

### 3.2 需新建的表（本 PRD 交付物）

DWS（特征/标签/样本，ML 可直接消费）：

| 新建表 | 主键 | 说明 |
|---|---|---|
| `ashare_dws.dws_stock_universe_daily` | `(sec_code, trade_date)` | 可交易股票池**底座**：原子事实列 + `is_tradable_hard` + `in_universe_default`（解耦设计，全策略共享） |
| `ashare_dws.dws_stock_feature_price_daily` | `(sec_code, trade_date, feature_version)` | 价格量价原始特征（截面处理前） |
| `ashare_dws.dws_stock_feature_valuation_daily` | `(sec_code, trade_date, feature_version)` | 估值、市值、换手、流动性原始特征 |
| `ashare_dws.dws_stock_label_daily` | `(sec_code, trade_date, label_version)` | 前向收益标签 + 入场/退出可成交掩码 |
| `ashare_dws.dws_stock_feature_daily_v0` | `(sec_code, trade_date, feature_version)` | 策略 1 使用的 P0 特征宽表（原始特征，不固化模型预处理） |
| `ashare_dws.dws_stock_sample_daily` | `(sec_code, trade_date, feature_version, label_version)` | 样本表：universe∩特征∩标签 + 训练资格掩码 |

ADS（策略消费 / 回测）：

| 新建表 | 主键 | 说明 |
|---|---|---|
| `ashare_ads.ads_ml_training_panel_daily` | `(run_id, sec_code, trade_date)` | 冻结后的训练面板，固化本次 run 的特征预处理、标签和切分 |
| `ashare_ads.ads_model_registry` | `(model_id)` | 模型元数据和参数，不存模型二进制 |
| `ashare_ads.ads_model_prediction_daily` | `(model_id, predict_date, horizon, sec_code)` | 模型每日打分与横截面排序 |
| `ashare_ads.ads_stock_candidate_daily` | `(strategy_id, rebalance_date, sec_code)` | 调仓日候选池、原始排名、过滤原因 |
| `ashare_ads.ads_portfolio_target_daily` | `(strategy_id, rebalance_date, sec_code)` | 目标持仓权重 |
| `ashare_ads.ads_order_plan_daily` | `(strategy_id, rebalance_date, sec_code, side)` | 回测/模拟订单计划 |
| `ashare_ads.ads_backtest_*` | `backtest_id + 日期/股票` | 回测成交、持仓、NAV、绩效汇总 |

> 训练/预测/回测的**计算逻辑**（模型 fit/predict、组合构建、撮合）用 Python（pandas + scikit-learn）在 BigQuery 之外完成；BigQuery 负责 DWS 物化、样本导出、结果回写。详见 §10。

### 3.3 数据范围与起点

- **回测/训练范围：`trade_date >= 2019-01-01`**，最终 DWS/ADS 只写 2019+。
- **lookback**：特征最长窗口 60 个交易日。DWS 特征构建必须读取不落表的 2018 lookback buffer，保证 2019-01 起滚动特征可用；buffer 行不得写入最终 DWS/ADS。
- **实现要求**：DWS SQL 不应静默把 2019Q1 整段样本丢掉。若当前可用输入无法提供 2018 lookback，需先补 lookback-capable 构建输入或明确降级为策略实验，不得与通用 DWS 验收口径混用。

---

### 3.4 DWS 表的复用边界（为何这样拆）

本 PRD 的 DWS 按「数据域 / 职责」拆表，使通用部分可被策略 2+ 直接复用、特征按域水平扩展：

| 表 | 复用性 | 谁复用 |
|---|---|---|
| `dws_stock_universe_daily` | ★★★ 通用底座 | 所有横截面策略共享同一池子；解耦设计下各策略按原子列自定义门槛，不重建表 |
| `dws_stock_label_daily` | ★★★ 通用标签 | 所有策略共享；一次算全 horizon（5/10/20d，可扩 1/60d）按列取用 |
| `dws_stock_feature_price_daily` / `dws_stock_feature_valuation_daily` | ★★★ 公共因子族 | 量价与估值是公共特征，策略 2/3/4/5/6 均复用；后续策略**新增域表**（feature_fin / fundflow / chip / industry），不改本表 |
| `dws_stock_feature_daily_v0` | ★★ P0 特征宽表 | 供首个基线和后续对照复用；保留原始特征，不固化模型预处理 |
| `dws_stock_sample_daily` | ★★ 通用样本清单 | universe→JOIN 特征域→JOIN 标签→训练资格掩码；策略差异通过 `strategy_id/run_id` 和 ADS 训练面板表达 |

原则：DWS/ADS 回归既有通用表合约，不新增 `ads_pv_clf_*` 并行表。策略 1 通过 `strategy_id='ml_pv_clf_v0'`、`model_id`、`run_id`、`feature_version`、`label_version` 区分配置与结果。新策略「加域表」或「加 run/config」，不复制一套基础 ADS。

---

## 4. 详细设计

### 4.1 股票池 `dws_stock_universe_daily`

**口径**：以「交易日历开市日 × 个股生命周期」为骨架（含退市股存活区间），逐项打标。**剔除一律用掩码表达，不物理删行**——保留全样本可追溯。

**解耦设计（核心）**：universe 表把「客观事实」与「策略阈值」拆成三层，作为全平台共享的池子底座——后续策略无需重建表即可自定义门槛：

- **① 原子事实列**（客观、与策略无关；贵且易错的计算只做一次）：
  - 布尔：`is_listed`、`is_st`、`is_suspended`、`is_one_word_limit`（一字涨/跌停，派生自 `dwd` 的 up/down）
  - 连续量：`list_age_td`（上市交易日数）、`amount_ma20_cny`（20 日均额，元）、`close`（收盘价）
  - 类别：`board`
- **② 硬可交易性**（所有策略共识，无争议）：
  `is_tradable_hard = is_listed AND dwd.is_tradable`（`dwd.is_tradable` 已排除停牌 / 一字板 / 开盘临停）
- **③ 策略 1 默认池**（在 ② 上叠加策略 1 偏好阈值，见下表）：
  `in_universe_default = is_tradable_hard AND NOT is_st AND list_age_td≥120 AND amount_ma20_cny≥5e7 AND close≥3 AND board∈@board_allowlist`

**下游用法**：
- 策略 1：`WHERE in_universe_default`（与简单版一样方便）。
- 其他策略要不同门槛（更严流动性、更长上市期、放宽板块…）：`WHERE is_tradable_hard AND <自定义阈值>`，直接复用 ① 的原子列，**不重建表、不重算**。

**`in_universe_default` 的阈值组成**（「硬」= 进 `is_tradable_hard`；「阈值」= 策略可调）：

| 过滤项 | 规则 | 来源 | 层 |
|---|---|---|---|
| 在市 | `trade_date ∈ [list_date, delist_date)` | `dim_stock` | 硬 |
| 非停牌 | `t` 日 `is_suspended = FALSE` | `dwd_stock_eod_price` | 硬 |
| 非一字板 | `t` 日非一字涨 / 跌停 | `dwd_stock_eod_price` | 硬 |
| 非 ST | `t` 日 `is_st = FALSE`（SCD2 区间命中） | `dim_stock_name_hist` | 阈值 |
| 上市满 N 日 | `list_age_td ≥ 120`（默认，可配 180） | `dim_trade_calendar.trade_date_seq` 差 | 阈值 |
| 流动性 | `amount_ma20_cny ≥ 5000 万元`（默认，可配 3000 万 / 1 亿） | `dwd_stock_eod_price` | 阈值 |
| 价格 | `t` 日 `close ≥ 3 元`（默认，可配 2） | `dwd_stock_eod_price` | 阈值 |
| 板块 | `board IN @board_allowlist`，建议默认 `('SSE_MAIN','SZSE_MAIN')`，但科创/创业/北交所开关待 owner 确认 | `dim_stock.board` | 阈值 |

**输出列**：① 原子事实列全部 + ② `is_tradable_hard` + ③ `in_universe_default` + `universe_version`；各「阈值」单项布尔（如 `pass_liquidity`、`pass_price`、`pass_list_age`）可选物化，便于回测诊断与调参。

> **幸存者偏差红线**：universe 必须含已退市股在其存活期的样本。严禁用 `dim_stock` 当前 `list_status='L'` 过滤历史样本（详见 [KNOWN_CONSTRAINTS.md](../../.agent/memory/KNOWN_CONSTRAINTS.md)）。现有 `dim_stock` 已 UNION 退市股，直接用即可。

### 4.2 特征 `dws_stock_feature_price_daily` / `dws_stock_feature_valuation_daily`（截面处理前的原始特征）

全部基于 `≤ t` 数据，**收益/技术指标一律用后复权 `close_hfq`**。特征清单见 [附录 A](#附录-a特征字典)。分 6 组，约 28 个特征：

1. 动量/反转：`ret_1d/3d/5d/10d/20d/60d`
2. 波动/回撤：`vol_5d`, `vol_20d`, `vol_60d`, `maxdd_20d`, `hl_range_20d`
3. 成交活跃度：`amount_ma5/20`, `amount_zscore_20d`, `turnover_ma5/20`
4. 换手/量比：`turnover_rate`, `turnover_rate_free_float`, `volume_ratio`
5. 估值：`pe_ttm`, `pb`, `ps_ttm`, `dividend_yield_ttm`（取倒数 `ep_ttm`/`bp`/`sp_ttm` 见附录）
6. 规模/属性：`log_total_mv`, `log_circ_mv`, `board`（类别）, `list_age_td`

> 首版**不含** `industry`（`dim_stock.industry` 是最新快照、回填历史会穿越）。行业属性留待引入 `index_member_all` 时点行业后再加（策略 4）。首版用 `board`（板块，恒定）作类别特征。

### 4.3 标签 `dws_stock_label_daily`

主标签口径与 `docs/数据仓库建模方案-DWS-ADS.md` 的通用标签表对齐：**`t` 日收盘出信号 → `t+1` 开盘建仓 → `t+H` 收盘平仓**，使用后复权入场价和退出价：

```text
fwd_ret_Hd = close_hfq[t+H] / open_hfq[t+1] - 1     （H ∈ {1, 5, 10, 20}）
```

例如 H=5 时，标签为 `t+1` 开盘买入、`t+5` 收盘卖出；H=1 时，标签为 `t+1` 开盘买入、`t+1` 收盘卖出。

派生标签（每个 H 一套，主口径 H=5，见 §4.5 调仓对齐）：

| 标签 | 定义 | 用于 |
|---|---|---|
| `fwd_ret_5d/10d/20d` | 上式，默认指 open-to-close 通用标签 | 回归（Ridge）、IC |
| `fwd_xs_ret_Hd` | `fwd_ret_Hd − 当日 universe 内均值` | 截面去市场 beta 后的相对收益 |
| `rank_pct_Hd` | 当日 universe 内 `fwd_ret_Hd` 分位 [0,1] | 排序/分层 |
| `label_top30_Hd` | `rank_pct_Hd ≥ 0.7 → 1 else 0` | 分类（Logistic）主标签 |
| `label_above_median_Hd` | `fwd_ret_Hd > 当日中位数 → 1 else 0` | 分类备选 |

open-to-open 或 close-to-close 口径可作为研究对照标签保留为独立字段（如 `fwd_ret_5d_open_to_open` / `fwd_ret_5d_close_to_close`），但不得替代 v0 主训练/验收标签；如需进入通用 `dws_stock_label_daily`，必须用独立字段名和 `label_version` 标注。

**入场可成交掩码（关键，防纸面 alpha）**：

```text
label_entry_tradable = can_buy_open[t+1]
                       AND NOT is_one_word_limit_up[t+1]
                       AND NOT is_suspended[t+1]
```

- `label_entry_tradable = FALSE` 的样本：训练时默认剔除（可配置保留做对照），用于标记标签是否可执行。
- **禁止**在 `T` 日组合选股阶段用 `label_entry_tradable[t]` 或任何 `T+1` 实际成交字段预先过滤候选；这些字段只能在训练标签有效性、回测撮合和事后归因中使用。
- 平仓侧 `t+H` 若遇跌停无法卖出，回测撮合层顺延至下一可卖日（§8.2），标签本身不顺延（保持口径统一，差异计入回测）。

### 4.4 样本 `dws_stock_sample_daily`

`universe(in_universe_default=TRUE) ∩ feature_daily_v0 ∩ label`，输出：

- 原始特征、缺失标记、样本资格掩码
- 全部标签列
- `label_entry_tradable`
- `split_tag`（train/valid/test，按 §6 滚动 fold 赋值，含 fold 编号）
- 版本字段：`feature_version`, `label_version`, `universe_version`

模型训练专属的 winsorize、缺失填充、one-hot、z-score 不固化在 DWS；它们写入 `ads_ml_training_panel_daily` 的具体 `run_id/preprocess_version`，确保同一套 DWS 原始特征可被不同实验复用。

### 4.5 调仓与持有期对齐（消除原设计矛盾）

原文「每周调仓 + 持有 5 **或** 10 日」存在重叠歧义。**v0 定为：周频出信号、主标签持有期 = 5 个交易日（open-to-close，无重叠）**，主标签 `*_5d`。

- 调仓日 `T` = 每周最后一个开市日（用 `dim_trade_calendar` 按 ISO 周取 max 开市日）。
- `T+1` 开盘建仓，主标签持有至 `T+5` 收盘；下一期信号在 `T+5` 盘后生成，下一交易日开盘进入新一期组合。
- 组合回测的实际成交和持仓收益以 `ads_backtest_*` 的撮合结果为准；若组合调仓执行价与通用标签口径不同，报告中必须单列标签收益与真实 NAV 的差异。
- `fwd_ret_10d/20d` 仍计算，仅用于 IC 研究与后续策略，不驱动 v0 组合。
- 若后续要 10 日信号 → 改双周调仓持有 10 日（开放问题 OQ-2）。

### 4.6 模型 `ml_pv_clf_v0`

| 项 | v0 设定 |
|---|---|
| 主算法 | `LogisticRegression(penalty='elasticnet', solver='saga')`，标签 `label_top30_5d` |
| 对照算法 | `Ridge`/`ElasticNet` 回归 `fwd_xs_ret_5d`，按预测值排序 |
| 输入特征 | `ads_ml_training_panel_daily` 中按 `run_id/preprocess_version` 固化的预处理特征 |
| 训练单位 | 按 §6 滚动 fold，每 fold 独立 fit |
| 超参 | `l1_ratio ∈ {0.2,0.5,0.8}`、`C`/`alpha` 网格，按验证集 RankIC 选择 |
| 输出 | 每日每股 `score`（分类用正类概率 / 回归用预测超额）写入 `ads_model_prediction_daily` |

### 4.7 组合 `ads_stock_candidate_daily` / `ads_portfolio_target_daily`

- 每个调仓日 `T`，仅用 `T` 日已知的 `in_universe_default`、风险过滤和模型 `score` 形成候选池，取 `score` 最高的目标名单。建议默认 5 只（10 万资金 5–8 只可配），待 owner 确认。
- **等权**，建议单票权重 ≤ 20%，待 owner 确认。
- 输出目标权重 + 相对上期的买卖清单 + 预计成交额。
- `T+1` 开盘实际不可买的目标票由撮合层处理为买入失败并留现金；不得事后用低排名可买票替补，除非 owner 明确开启预先声明的候补规则。

### 4.8 回测 `ads_backtest_*`

见 §8。输出到通用 `ads_backtest_*` 表：净值曲线、逐年指标、换手、三档成本敏感性、相对中证 1000 的超额与信息比率、小盘 beta 归因后的纯 alpha。

---

## 5. 特征工程规范（每日截面内）

按上游策略文档 §5，**逐交易日截面**处理：

1. **缺失值**：
   - 新上市窗口不足 → 不进 universe（§4.1 已挡）。
   - 估值类（`pe_ttm` 等）缺失/为负 → 加 `is_missing_*` 标记；`pe_ttm` 用 `ep_ttm = 1/pe_ttm`（负 PE 映射为 0 或单独标记，见附录）规避符号问题。
   - 数值缺失填**截面中位数**并保留缺失标记。
2. **极端值**：截面 winsorize 到 [1%, 99%]。
3. **标准化**：稳健 z 分数 `z = (x − median) / mad`（输出 `*_z`）。
4. **中性化**：v0 **仅监控不强制**。监控市值/波动/流动性/价格暴露；行业中性留待策略 4。
   - 注：模型已含 `log_total_mv` 等特征，等价于让模型自行学习暴露；严格中性化作为 v0.1 实验项。

---

## 6. 样本切分与防泄露

### 6.1 滚动窗口（扩展窗口，逐年滚动）

| Fold | 训练 | 验证 | 测试 |
|---|---|---|---|
| 1 | 2019-04 ~ 2021-12 | 2022 | 2023 |
| 2 | 2019-04 ~ 2022-12 | 2023 | 2024 |
| 3 | 2019-04 ~ 2023-12 | 2024 | 2025 |

（受 warm-up 影响，样本有效起点约 2019-04；warm-up 口径见 §3.3。）

### 6.2 Purge + Embargo（关键）

- **Purge**：训练集中**剔除**标签窗口与验证/测试期重叠的样本——即剔除 `t` 使得 `[t+1, t+H]` 触及验证/测试起点的样本（H=20 时按最长 20 日 purge，保证不同 H 共用切分）。
- **Embargo**：验证/测试期**起点后再隔离 H 个交易日**不计入评估，避免边界泄露。

### 6.3 重叠标签的统计校正

日频滚动的 `fwd_ret_5d` 标签在相邻交易日高度重叠，直接算 RankIC 的 t 统计会**高估显著性**。要求：

- IC 显著性按 **Newey-West**（lag ≈ H）校正，或仅在**非重叠采样日**（每 H 个交易日取一次）上做统计检验。
- 报告 ICIR 时注明有效样本数。

---

## 7. 模型训练流程

1. 从 `dws_stock_sample_daily` 按 fold 导出 train/valid/test。
2. 仅用 `label_entry_tradable=TRUE` 训练（对照组保留全样本各跑一次）。
3. 网格搜索超参，**以验证集日频 RankIC 均值**为选择准则（非 accuracy/AUC——分类阈值对选股无意义）。
4. 在测试集 predict，分数写回 `ads_model_prediction_daily`，带 `model_id=ml_pv_clf_v0`、`run_id`、`feature_version`、训练窗口元信息。
5. 落地特征重要性（系数）与逐期 IC 衰减，供稳定性审查。

---

## 8. 回测与评估

### 8.1 执行假设

- 默认执行价 **`t+1` 开盘**（`open_hfq[t+1]`）；并跑 **`t+1` 收盘**对照（保守版）。
- 信号 `t` 收盘生成，绝不允许 `t` 日成交。

### 8.2 不可成交处理（撮合层强制）

- 买入：`t+1` `can_buy_open=FALSE` 或一字涨停 → 该票当期**放弃买入**，资金留现金（不顺延找替补，保持组合可复现）。
- 卖出：`t+H` 收盘或回测约定退出时点不可卖 → **顺延至下一可卖日**，记录顺延次数。
- 统计「想买买不到 / 想卖卖不掉」次数，作为可行性验收指标（§2.2）。

### 8.3 成本三档（单边）

| 情景 | 假设 |
|---|---|
| 乐观 | 5–10 bps |
| 中性（默认验收） | 10–20 bps |
| 悲观 | 20–40 bps |

含佣金（按最低 5 元/笔触发）、印花税（卖出，按现行费率）、过户费、滑点。小资金真实痛点是**最低佣金 + 涨跌停不可成交 + 开盘跳空**，需在报告中单列其影响。

### 8.4 基准与 alpha/beta 归因（必做）

- **基准取中证 1000（`index_alias='CSI1000'`）**，非沪深 300——策略 1 在小盘票里选股，beta 是小盘 beta。备选中证 500。
- 报告：组合绝对收益、相对中证 1000 超额、信息比率。
- **纯 alpha**：对中证 1000（及可得的中证 500）做收益回归，剥离 beta 后看截距 alpha 是否显著为正。明确区分「赚的是小盘 beta 还是选股 alpha」。

### 8.5 指标清单

收益（年化/超额/月胜率）、风险（最大回撤/年化波动/Calmar）、风险收益（Sharpe/Sortino/IR）、交易（年换手/持仓天数/单笔）、模型（IC/RankIC/ICIR/分层）、可行性（不可买卖次数/三档成本敏感性）。**全部逐年报告**，不只看全样本。

---

## 9. 风控与交易规则（v0）

| 项 | 设定 |
|---|---|
| 单票仓位 | 建议 ≤ 20%，默认等权（5 只 → 20%），待 owner 确认 |
| 持仓数 | 建议 5（10 万可配 5–8），待 owner 确认 |
| 流动性 | 近 20 日 `amount_ma20_cny ≥ 5000 万元` |
| 周换手 | 监控，目标 ≤ 100–150% |
| 单票止损 | v0 **不内置**，作为独立实验项评估是否改善收益回撤比（避免污染基线信号评估） |
| 组合止损 | 同上，v0 先不加，先看裸信号 |

> v0 刻意**不叠加止损/择时**，先评估裸选股信号质量；止损、组合回撤控制作为 v0.1 独立 A/B 实验。

---

## 10. 工程实现与规范

### 10.1 表物理规范（沿用 P0 标准）

- DWS 股票日频表：`PARTITION BY DATE_TRUNC(trade_date, MONTH)` + `CLUSTER BY sec_code` 或 `sec_code, feature_version`；事实/特征/标签表按需开启 `require_partition_filter=TRUE`。
- ADS 预测表：`PARTITION BY DATE_TRUNC(predict_date, MONTH)` + `CLUSTER BY model_id, sec_code`。
- ADS 候选、组合、订单计划表：`PARTITION BY DATE_TRUNC(rebalance_date, MONTH)` + `CLUSTER BY strategy_id, sec_code`；若订单计划以预计成交日查询为主，可增加 `expected_trade_date` 字段但不替代主调仓分区。
- ADS 回测 NAV / 持仓 / 成交表：NAV 按 `trade_date` 月分区，持仓/成交按各自事件日期月分区并聚簇 `backtest_id, sec_code`。
- ADS 模型注册、回测配置、绩效汇总等小表：默认不分区，或仅在数据量增长后按 `created_at` 月分区。
- 建表用 `CREATE OR REPLACE TABLE`，范围参数化（`DECLARE start_date/end_date`）。
- 表级 + 字段级中文 description（CTAS 后 `ALTER COLUMN SET OPTIONS`，集中进 `sql/metadata/`）。
- 所有 `bq query` 显式 `--location=asia-east2`。
- 血缘/版本字段：DWS/ADS 带 `feature_version`/`label_version`/`universe_version`/`model_id`/`strategy_id`/`run_id` 及 `created_at`。

### 10.2 目录与文件

```text
sql/dws/01_dws_stock_universe_daily.sql
sql/dws/02_dws_stock_feature_price_daily.sql
sql/dws/03_dws_stock_feature_valuation_daily.sql
sql/dws/04_dws_stock_label_daily.sql
sql/dws/05_dws_stock_feature_daily_v0.sql
sql/dws/06_dws_stock_sample_daily.sql
sql/ads/01_ads_ml_training_panel_daily.sql      # 训练面板，可由 SQL/Python 写入
sql/ads/02_ads_model_prediction_daily.sql       # 预测结果由 Python 回写
sql/ads/03_ads_stock_candidate_daily.sql
sql/ads/04_ads_portfolio_target_daily.sql
sql/ads/05_ads_order_plan_daily.sql
sql/ads/06_ads_backtest_nav_daily.sql
sql/ads/07_ads_backtest_performance_summary.sql
sql/qa/02_strategy1_dws_checks.sql              # 穿越/掩码/覆盖率断言
sql/metadata/02_strategy1_descriptions.sql
ml/pv_clf_v0/                                    # Python：导出→训练→预测→组合→回测→回写
```

### 10.3 职责分工

- **BigQuery（SQL）**：DWS 物化、样本导出、ADS 结果回写、QA 断言。
- **Python（`ml/pv_clf_v0/`）**：模型 fit/predict、组合构建、撮合回测、指标计算；特征截面标准化可由 Python 生成后写入训练面板。
- 标准化若用 SQL，应落在 `ads_ml_training_panel_daily` 的具体 `run_id/preprocess_version`，不写回通用 DWS 特征表；若用 Python，也必须把预处理参数和结果随 `run_id` 固化，避免两处口径漂移。

### 10.4 QA 断言（`sql/qa/02_*`，必须全绿）

- 特征列无 `t+1` 及未来依赖（人工 + 抽样数值核对）。
- universe 含退市股样本数 > 0；抽查退市股存活区间存在。
- `label_entry_tradable=FALSE` 占比合理（一字板/停牌比例量级正常）。
- 同 `(sec_code, trade_date)` 唯一，无重复。
- 切分 fold 间无标签窗口重叠（purge 生效）。

---

## 11. 里程碑与交付物

| 阶段 | 交付物 | 验收 |
|---|---|---|
| M1 数据底座 | `dws_stock_universe_daily` + QA | §2.1 universe/退市/ST/停牌项 |
| M2 特征+标签 | `dws_stock_feature_*` + `dws_stock_label_daily` + `dws_stock_sample_daily` + QA | §2.1 复权/泄露/可成交项 |
| M3 模型+预测 | `ml/pv_clf_v0/` + `ads_ml_training_panel_daily` + `ads_model_prediction_daily` | RankIC/分层产出 |
| M4 组合+回测 | `ads_stock_candidate_daily` + `ads_portfolio_target_daily` + `ads_backtest_*` + 回测报告 | §2.2 全部指标 + 三档成本 |
| M5 结论 | 回测评审文档（`docs/reviews/`） | alpha 是否存在/失效位置的明确结论 |

---

## 12. 风险与开放问题

| 编号 | 问题 | 默认取值 | 待 owner 决策 |
|---|---|---|---|
| OQ-1 | 回测起点 2019 vs 2017 | **已定：2019 起点**，先不回填 2017–2018；若将来回填，2017–2018 仅作 OOS 压测段、绝不进训练集（隔离 regime 漂移毒性，保留下行压测价值） | ✅ 已决策（先 2019；回填留作后续风险评估选项） |
| OQ-2 | 持有期 5d vs 10d | 周频持有 5d（无重叠） | 是否改双周持有 10d 以贴近原文并降换手 |
| OQ-3 | 基准 | 中证 1000 | 是否改/并列中证 500、中证 2000、万得微盘 |
| OQ-4 | 特征标准化落点 | 固化到 `ads_ml_training_panel_daily`；实现可用 SQL 或 Python | 确认首版工具链与 `preprocess_version` 记录方式 |
| OQ-5 | 价格/流动性/上市天数阈值 | 3 元 / 5000 万 / 120 交易日 | 是否调整 |
| OQ-6 | 北交所/科创/创业板纳入 | 建议默认仅沪深主板；保留 `board_allowlist` 参数 | 待 owner 确认是否纳入科创/创业/北交所或做独立对照 |
| R-1 | 小盘风格尾部风险（2024 微盘踩踏、退市新规） | — | v0 仅评估，需在结论中量化下行 |
| R-2 | 线性模型表达力弱、风格切换敏感 | — | 预期，故定位为基线 |

---

## 附录 A：特征字典

> 所有滚动窗口 = 交易日数；收益/价格类一律后复权 `close_hfq`。`amount_cny` 单位元。

| 特征 | 公式 | 来源 | 处理 |
|---|---|---|---|
| `ret_1d` | 直接取 | `dwd_stock_eod_price.ret_1d` | z |
| `ret_3d/5d/10d/20d/60d` | `close_hfq[t]/close_hfq[t-N]-1` | price | z |
| `vol_5d/20d/60d` | `std(ret_1d, N)` | price | z |
| `maxdd_20d` | `close_hfq[t]/max(close_hfq, 20)-1` | price | z |
| `hl_range_20d` | `mean((high_hfq-low_hfq)/close_hfq, 20)` | price | z |
| `amount_ma5/20` | `mean(amount_cny, N)` | price | log→z |
| `amount_zscore_20d` | `(amount_cny[t]-mean20)/std20` | price | z |
| `turnover_ma5/20` | `mean(turnover_rate, N)` | valuation | z |
| `turnover_rate` | 直接取 | valuation | z |
| `turnover_rate_free_float` | 直接取 | valuation | z |
| `volume_ratio` | 直接取 | valuation | z |
| `ep_ttm` | `1/pe_ttm`（pe_ttm≤0 → 标记+置0） | valuation | winsor→z |
| `bp` | `1/pb` | valuation | winsor→z |
| `sp_ttm` | `1/ps_ttm` | valuation | winsor→z |
| `dividend_yield_ttm` | 直接取 | valuation | z |
| `log_total_mv` | `ln(total_mv_cny)` | valuation | z |
| `log_circ_mv` | `ln(circ_mv_cny)` | valuation | z |
| `list_age_td` | 上市后开市交易日数 | calendar + dim_stock | log→z |
| `board` | 板块，按 `board_allowlist` 参数进入 universe；保留作类别/风险暴露字段 | `dim_stock.board` | one-hot |

> 估值类用倒数（`ep/bp/sp`）而非原值，避免 PE/PB 在 0 附近发散、负值符号倒置；负值单独标记。

## 附录 B：关键防穿越对照

| 风险点 | 现有字段直接解决 |
|---|---|
| 一字板买不进的纸面 alpha | `label_entry_tradable` 用 `can_buy_open[t+1]`/`is_one_word_limit_up[t+1]` |
| 幸存者偏差 | `dim_stock` 含退市股 + `delist_date` 半开区间 |
| ST 穿越 | `dim_stock_name_hist` SCD2 `is_st` 区间命中 `t` 日 |
| 前复权未来信息 | 仅用 `close_hfq`（后复权），禁用 `_qfq` |
| 标签未来泄露 | 标签 `≥ t+1`，特征 `≤ t`，fold 间 purge+embargo |
| 行业最新快照穿越 | 首版不用 `dim_stock.industry`，用恒定 `board` |
```
